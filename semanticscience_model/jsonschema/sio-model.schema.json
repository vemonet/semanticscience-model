{
   "$defs": {
      "1DCartesianPoint": {
         "additionalProperties": false,
         "description": "A 1D cartesian point is a point whose position is specified along a single dimension using Cartesian coordinates.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "1DCartesianPoint",
         "type": "object"
      },
      "1DExtentQuantity": {
         "additionalProperties": false,
         "description": "A quantity that extends in single dimension.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "1DExtentQuantity",
         "type": "object"
      },
      "2DCartesianPoint": {
         "additionalProperties": false,
         "description": "A 2D cartesian point is a point whose position is specified along two  dimensions using Cartesian coordinates.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "2DCartesianPoint",
         "type": "object"
      },
      "2DExtentQuantity": {
         "additionalProperties": false,
         "description": "A quantity that extends in two dimensions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "2DExtentQuantity",
         "type": "object"
      },
      "3%27SpliceSite": {
         "additionalProperties": false,
         "description": "The 3' splice site is the terminal region of an exon that is 3' to the intron that is to be excised.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "3%27SpliceSite",
         "type": "object"
      },
      "3%27UntranslatedRegion": {
         "additionalProperties": false,
         "description": "A three prime untranslated region (3'-UTR) is the section of messenger RNA (mRNA) that immediately follows the translation termination codon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "3%27UntranslatedRegion",
         "type": "object"
      },
      "3DCartesianCoordinate": {
         "additionalProperties": false,
         "description": "A 3D cartesian coordinate is a coordinate that is composed of an x, y and z coordinate.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "3DCartesianCoordinate",
         "type": "object"
      },
      "3DCartesianPoint": {
         "additionalProperties": false,
         "description": "A 3D cartesian point is a point whose position is specified along three  dimensions using Cartesian coordinates.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "3DCartesianPoint",
         "type": "object"
      },
      "3DExtentQuantity": {
         "additionalProperties": false,
         "description": "A quantity that extends in three dimensions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "3DExtentQuantity",
         "type": "object"
      },
      "3dStructureModel": {
         "additionalProperties": false,
         "description": "A 3D structure model is a representation of the spatial arrangement of one or more chemical entities.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "3dStructureModel",
         "type": "object"
      },
      "5%27SpliceSite": {
         "additionalProperties": false,
         "description": "The 5' splice site is the terminal region of an exon that is 5' to the intron that is to be excised.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "5%27SpliceSite",
         "type": "object"
      },
      "5%27UntranslatedRegion": {
         "additionalProperties": false,
         "description": "The five prime untranslated region (5' UTR) is a section of messenger RNA (mRNA) and the DNA that codes for it that starts at the +1 position (where transcription begins) and ends one nucleotide before the start codon (usually AUG) of the coding region.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "5%27UntranslatedRegion",
         "type": "object"
      },
      "ACollectionOfDuplicates": {
         "additionalProperties": false,
         "description": "a collection of duplicates is a collection composed of items that are an exact copy of other items in the collection.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ACollectionOfDuplicates",
         "type": "object"
      },
      "ACollectionOfReplicates": {
         "additionalProperties": false,
         "description": "a collection of replicates is a collection composed of items that are a facsimile, reproduction, or copy of other items in the collection.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ACollectionOfReplicates",
         "type": "object"
      },
      "ALittleQuantifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ALittleQuantifier",
         "type": "object"
      },
      "Abnormal": {
         "additionalProperties": false,
         "description": "A quality that has a value that is outside normal or average.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Abnormal",
         "type": "object"
      },
      "Aborted": {
         "additionalProperties": false,
         "description": "aborted is a process status in which a started process will not complete as intended.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Aborted",
         "type": "object"
      },
      "AbstractRole": {
         "additionalProperties": false,
         "description": "An abstract role is a role whose basis lies in spatial/temporal or comparative relations.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AbstractRole",
         "type": "object"
      },
      "AbstractSection": {
         "additionalProperties": false,
         "description": "An abstract section is a document section that provides brief summary of a document that explains the main argument(s), topic(s) or findings.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AbstractSection",
         "type": "object"
      },
      "Academic": {
         "additionalProperties": false,
         "description": "An academic is an individual that participates in education and scholarship.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Academic",
         "type": "object"
      },
      "AcademicDepartment": {
         "additionalProperties": false,
         "description": "An academic department is a division of a university or school faculty devoted to a particular academic discipline.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AcademicDepartment",
         "type": "object"
      },
      "AcademicOrganization": {
         "additionalProperties": false,
         "description": "An academic organization is a lawfully recognized organization that confers diplomas, degrees and other forms of recognition of academic achievement.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AcademicOrganization",
         "type": "object"
      },
      "AcademicRole": {
         "additionalProperties": false,
         "description": "An academic role is a social role that pertains to the academic institution.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AcademicRole",
         "type": "object"
      },
      "Acid": {
         "additionalProperties": false,
         "description": "An acid is a molecular entity in solution capable of donating a hydron (Bronsted acid) or capable of forming a covalent bond with an electron pair (Lewis acid).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Acid",
         "type": "object"
      },
      "Acid-baseReaction": {
         "additionalProperties": false,
         "description": "An acid-base reaction is a chemical reaction between an acid and a base.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Acid-baseReaction",
         "type": "object"
      },
      "AcknowledgementsSection": {
         "additionalProperties": false,
         "description": "An acknowledgements section is a document section that identifies individuals, groups or organizations for their support in the development of the work.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AcknowledgementsSection",
         "type": "object"
      },
      "ActiniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ActiniumAtom",
         "type": "object"
      },
      "ActionSpecification": {
         "additionalProperties": false,
         "description": "An action specification is a specification composed of a sequence of instructions to achieve some objective.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ActionSpecification",
         "type": "object"
      },
      "Activator": {
         "additionalProperties": false,
         "description": "A molecular activator is a molecular regulator that realizes its disposition to conformationally change a target molecule and increase its functionality.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Activator",
         "type": "object"
      },
      "ActivatorRole": {
         "additionalProperties": false,
         "description": "The role of a chemical entity that increases the rate of reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ActivatorRole",
         "type": "object"
      },
      "ActiveIngredient": {
         "additionalProperties": false,
         "description": "An active ingredient is a molecular entity that exhibits biological activity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ActiveIngredient",
         "type": "object"
      },
      "ActiveMovement": {
         "additionalProperties": false,
         "description": "active movement is the process in which an object is spatially displaced using some chemical energy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ActiveMovement",
         "type": "object"
      },
      "ActiveSite": {
         "additionalProperties": false,
         "description": "An active site is a molecular site in which a chemical event occurs (structural transformation or conformational change).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ActiveSite",
         "type": "object"
      },
      "ActiveTransport": {
         "additionalProperties": false,
         "description": "active transport is the movement of a substance across a membrane against its concentration gradient (from low to high concentration) and requires chemical energy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ActiveTransport",
         "type": "object"
      },
      "AdditionReaction": {
         "additionalProperties": false,
         "description": "An addition reaction is an organic reaction where two or more molecules combine to form a larger one. Addition reactions are limited to chemical compounds that have multiply-bonded atoms:\n    * Molecules with carbon-carbon double bonds or triple bonds\n    * Molecules with carbon - hetero double bonds like C=O or C=N",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AdditionReaction",
         "type": "object"
      },
      "Address": {
         "additionalProperties": false,
         "description": "An address is a position that indicates the physical location of some entity using a social convention.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Address",
         "type": "object"
      },
      "AdministrativeRole": {
         "additionalProperties": false,
         "description": "An administrative role is the role of an individual that performs administrative tasks for some organization.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AdministrativeRole",
         "type": "object"
      },
      "Affection": {
         "additionalProperties": false,
         "description": "affection is an emotion characterized with a feeling or type of love for another living thing.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Affection",
         "type": "object"
      },
      "Affiliation": {
         "additionalProperties": false,
         "description": "An affiliation is a social relation which indicates the partnership between two or more entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Affiliation",
         "type": "object"
      },
      "Age": {
         "additionalProperties": false,
         "description": "age is the length of time that a person has lived or a thing has existed.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Age",
         "type": "object"
      },
      "AggregatedCarbonNanorods": {
         "additionalProperties": false,
         "description": "aggregate of carbon nanorods is an allotrope of carbon considered to be the least compressible material known, as measured by its isothermal bulk modulus; aggregated diamond nanorods have a modulus of 491 gigapascals (GPa), while a conventional diamond has a modulus of 442 GPa. ADNRs are also 0.3% denser than regular diamond.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AggregatedCarbonNanorods",
         "type": "object"
      },
      "Agreement": {
         "additionalProperties": false,
         "description": "agreement is the result of consensus decision making when members of the group agree.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Agreement",
         "type": "object"
      },
      "AgreementQuality": {
         "additionalProperties": false,
         "description": "agreement quality is a quality that exhibits the degree of consensus for some set of assertions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AgreementQuality",
         "type": "object"
      },
      "Algorithm": {
         "additionalProperties": false,
         "description": "An algorithm is an effective method expressed as a finite list of well-defined instructions for calculating a function.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Algorithm",
         "type": "object"
      },
      "Alive": {
         "additionalProperties": false,
         "description": "alive is the state of a biological organism that exhibits biological functions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Alive",
         "type": "object"
      },
      "Allele": {
         "additionalProperties": false,
         "description": "An allele is one of a set of sequence variants of a gene.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Allele",
         "type": "object"
      },
      "AllostericSite": {
         "additionalProperties": false,
         "description": "An allosteric site is a binding site that when bound to particular ligand changes the conformational state and affects its functionality.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AllostericSite",
         "type": "object"
      },
      "Allotrope": {
         "additionalProperties": false,
         "description": "An allotrope is a structural variant of a chemical element.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Allotrope",
         "type": "object"
      },
      "AlphaHelix": {
         "additionalProperties": false,
         "description": "An alpha helix is structural region of a protein that is characterized by 3.6 residues per turn,  a translation of 1.5 angstroms along the helical axis in which backbone N-H groups form a hydrogen bond to the backbone carboxyl group of the amino acid four residues prior.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AlphaHelix",
         "type": "object"
      },
      "Altitude": {
         "additionalProperties": false,
         "description": "Altitude is a distance above sea level.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Altitude",
         "type": "object"
      },
      "AluminiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AluminiumAtom",
         "type": "object"
      },
      "Always": {
         "additionalProperties": false,
         "description": "always indicates that the event occurs at all times.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Always",
         "type": "object"
      },
      "AmericiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AmericiumAtom",
         "type": "object"
      },
      "AminoAcid": {
         "additionalProperties": false,
         "description": "An amino acid is an organic molecule composed of a carbon bonded to four different groups: a carboxyl group, an amino group, an R group, and a hydrogen atom. In the case of glycine, the R group is another hydrogen atom.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AminoAcid",
         "type": "object"
      },
      "AminoAcidPolymer": {
         "additionalProperties": false,
         "description": "an amino acid polymer is an organic polymer composed of two or more amino acid residues.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AminoAcidPolymer",
         "type": "object"
      },
      "AminoAcidPolymerSubmolecule": {
         "additionalProperties": false,
         "description": "An amino acid polymer submolecule is a submolecule of an amino acid polymer.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AminoAcidPolymerSubmolecule",
         "type": "object"
      },
      "AminoAcidResidue": {
         "additionalProperties": false,
         "description": "An amino acid residue is a submolecule of an amino acid that is part of a larger molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AminoAcidResidue",
         "type": "object"
      },
      "AmorphousCarbon": {
         "additionalProperties": false,
         "description": "amorphous carbon is an allotrope of carbon that does not have any crystalline structure. As with all glassy materials, some short-range order can be observed, but there is no long-range pattern of atomic positions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AmorphousCarbon",
         "type": "object"
      },
      "Anabolism": {
         "additionalProperties": false,
         "description": "Anabolism is the set of metabolic processes that construct larger chemical entities units from smaller chemical entities.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Anabolism",
         "type": "object"
      },
      "Analyte": {
         "additionalProperties": false,
         "description": "an analyte is a substance or chemical constituent of interest in an analytical procedure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Analyte",
         "type": "object"
      },
      "AnatomicalEntity": {
         "additionalProperties": false,
         "description": "an anatomical entity is an object that is a structural part (material or immaterial) of a biological entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AnatomicalEntity",
         "type": "object"
      },
      "Anger": {
         "additionalProperties": false,
         "description": "anger is disgust directed toward an equal status individual.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Anger",
         "type": "object"
      },
      "Angst": {
         "additionalProperties": false,
         "description": "angst is the intense feeling of apprehension, anxiety or inner turmoil.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Angst",
         "type": "object"
      },
      "Anion": {
         "additionalProperties": false,
         "description": "An anion is an atom or molecule with a net negative electrical charge.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Anion",
         "type": "object"
      },
      "Annotation": {
         "additionalProperties": false,
         "description": "An annotation is a written explanatory or critical description, or other in-context information (e.g., pattern, motif, link), that has been associated with data or other types of information.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Annotation",
         "type": "object"
      },
      "Annoyance": {
         "additionalProperties": false,
         "description": "Annoyance is an unpleasant emtion that is characterized by a abnormal or excessive sensitivity to some external stimulus.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Annoyance",
         "type": "object"
      },
      "Answer": {
         "additionalProperties": false,
         "description": "An answer is a reply to a question.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Answer",
         "type": "object"
      },
      "Antibody": {
         "additionalProperties": false,
         "description": "An antibody (also known as immunoglobulins, abbreviated Ig) are gamma globulin proteins that are used by the immune system to identify and neutralize foreign objects. They are typically made of two large heavy chains and two small light chains.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Antibody",
         "type": "object"
      },
      "Antigen": {
         "additionalProperties": false,
         "description": "An antigen is a molecule that can be bound by a major histocompatibility complex (MHC) and presented to a T-cell receptor.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Antigen",
         "type": "object"
      },
      "AntimonyAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AntimonyAtom",
         "type": "object"
      },
      "AntiportEnabledSecondaryActiveTransport": {
         "additionalProperties": false,
         "description": "antiport enabled secondary active transport is a secondary active transfort in which both ion and molecule are transported in opposite directions simultaneously.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AntiportEnabledSecondaryActiveTransport",
         "type": "object"
      },
      "Antonym": {
         "additionalProperties": false,
         "description": "An antonym is a word with opposite or nearly opposite meaning.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Antonym",
         "type": "object"
      },
      "Anxiety": {
         "additionalProperties": false,
         "description": "anxiety is an emotion charactersized by intense feeling of fear and concern coupled with a physical response.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Anxiety",
         "type": "object"
      },
      "ApartmentNumber": {
         "additionalProperties": false,
         "description": "An apartment number is the number assigned to identify an apartment in a building of apartments.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ApartmentNumber",
         "type": "object"
      },
      "Apathy": {
         "additionalProperties": false,
         "description": "apathy is an emotion characterized by lack of interest, enthusiasm, or concern.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Apathy",
         "type": "object"
      },
      "Apprehension": {
         "additionalProperties": false,
         "description": "apprehension is the negative emotion that something unpleasant will occur.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Apprehension",
         "type": "object"
      },
      "Arc": {
         "additionalProperties": false,
         "description": "an arc is a closed segment of a differentiable curve.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Arc",
         "type": "object"
      },
      "Area": {
         "additionalProperties": false,
         "description": "area is a quantity that pertains to the extent of a two-dimensional surface or shape, or planar lamina, in the plane.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Area",
         "type": "object"
      },
      "ArgonAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ArgonAtom",
         "type": "object"
      },
      "Argument": {
         "additionalProperties": false,
         "description": "An argument is a set of one or more declarative sentences (or propositions) known as the premises along with another declarative sentence (or proposition) known as the conclusion.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Argument",
         "type": "object"
      },
      "AromaticBond": {
         "additionalProperties": false,
         "description": "An aromatic bond is an interaction between a set of atoms across which pairs of electrons are shared.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AromaticBond",
         "type": "object"
      },
      "AromaticRing": {
         "additionalProperties": false,
         "description": "An aromatic ring is a ring in which the electrons are delocalized across all atoms in the ring.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AromaticRing",
         "type": "object"
      },
      "Arousal": {
         "additionalProperties": false,
         "description": "arousal is an emotion characterized by state of reactive to stimuli. It involves the activation of the reticular activating system in the brain stem, the autonomic nervous system and the endocrine system, leading to increased heart rate and blood pressure and a condition of sensory alertness, mobility and readiness to respond.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Arousal",
         "type": "object"
      },
      "ArrowedLineSegment": {
         "additionalProperties": false,
         "description": "An arrowed line is a directed line segment in which one or both endpoints is tangentially part of a triangle that bisects the line.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ArrowedLineSegment",
         "type": "object"
      },
      "ArsenicAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ArsenicAtom",
         "type": "object"
      },
      "Article": {
         "additionalProperties": false,
         "description": "An article is a publication that is stand-alone section of a larger work.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Article",
         "type": "object"
      },
      "AspectRatio": {
         "additionalProperties": false,
         "description": "The aspect ratio of a geometric shape is the ratio of its sizes in different dimensions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AspectRatio",
         "type": "object"
      },
      "Assay": {
         "additionalProperties": false,
         "description": "An assay is an investigative (analytic) procedure in laboratory medicine, pharmacology, environmental biology, and molecular biology for qualitatively assessing or quantitatively measuring the presence or amount or the functional activity of a target entity (the analyte) which can be a drug or biochemical substance or a cell in an organism or organic sample.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Assay",
         "type": "object"
      },
      "AssertionOvopub": {
         "additionalProperties": false,
         "description": "An assertion ovopub is an ovopub that contains and links to an assertion subgraph with one or more statements that may be true or false, as well as statements describing its provenance.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AssertionOvopub",
         "type": "object"
      },
      "AssertionalQualifier": {
         "additionalProperties": false,
         "description": "An assertional qualifier is the quality of affirmation, either being positive or negative.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AssertionalQualifier",
         "type": "object"
      },
      "Association": {
         "additionalProperties": false,
         "description": "An assocation is a relationship between two or more entities derived by some informational analysis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Association",
         "type": "object"
      },
      "AstatineAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AstatineAtom",
         "type": "object"
      },
      "Atom": {
         "additionalProperties": false,
         "description": "An atom is composed of a core of protons and/or neutrons which may be surrounded by a cloud of electrons.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Atom",
         "type": "object"
      },
      "Attribute": {
         "additionalProperties": false,
         "description": "An attribute is a characteristic of some entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Attribute",
         "type": "object"
      },
      "AudioRecording": {
         "additionalProperties": false,
         "description": "An audio recording is an electrical or mechanical inscription and re-creation of sound waves, such as spoken voice, singing, instrumental music, or sound effects.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AudioRecording",
         "type": "object"
      },
      "AuthorContributionSection": {
         "additionalProperties": false,
         "description": "An author contribution section is a document section that describes the contribution of the authors.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AuthorContributionSection",
         "type": "object"
      },
      "AuthorList": {
         "additionalProperties": false,
         "description": "an ordered list of authors.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AuthorList",
         "type": "object"
      },
      "AuthorRole": {
         "additionalProperties": false,
         "description": "An author role is the role of an individual that creates a creative, written work.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AuthorRole",
         "type": "object"
      },
      "AuthorSection": {
         "additionalProperties": false,
         "description": "An author section is a document section that lists the contributing authors.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "AuthorSection",
         "type": "object"
      },
      "Awe": {
         "additionalProperties": false,
         "description": "awe is an emotion produced by that which is grand, sublime or powerful and is characterized by a combination of joy, fear and admiration/reverence/respect.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Awe",
         "type": "object"
      },
      "Axis": {
         "additionalProperties": false,
         "description": "An axis is a line segment that is part of a statistical graph in which the \nposition along the line corresponds to a numeric or categorical value.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Axis",
         "type": "object"
      },
      "Bar": {
         "additionalProperties": false,
         "description": "A bar is a rectangle that is located in the plot of a statistical graph in which its length is proportional to the values that it represents.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Bar",
         "type": "object"
      },
      "BarGraph": {
         "additionalProperties": false,
         "description": "A bar graph is a statistical graph with rectangular bars of lengths proportional to that value that they represent.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BarGraph",
         "type": "object"
      },
      "BariumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BariumAtom",
         "type": "object"
      },
      "Base": {
         "additionalProperties": false,
         "description": "A base is a molecular entity dissolved in a solvent that is capable of accepting a proton (Bronsted base) or forming a covalent bond with a hydron (Lewis base) .",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Base",
         "type": "object"
      },
      "BasePair": {
         "additionalProperties": false,
         "description": "A base pair is a weak molecular interaction composed of hydrogen bonds between nucleobases.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BasePair",
         "type": "object"
      },
      "BaseStack": {
         "additionalProperties": false,
         "description": "A base stack is a stabilizing interaction of DNA and RNA between spatially adjacent nucleotides and possibly involving London dispersion, hydrophobic and electrostatic forces.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BaseStack",
         "type": "object"
      },
      "Behaviour": {
         "additionalProperties": false,
         "description": "Behaviour is the set of actions and mannerisms made by systems (biological or otherwise) in response to stimuli or inputs, whether internal or external, conscious or subconscious, overt or covert, and voluntary or involuntary.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Behaviour",
         "type": "object"
      },
      "Belief": {
         "additionalProperties": false,
         "description": "A belief is a proposition that is believed to be true.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Belief",
         "type": "object"
      },
      "Bent": {
         "additionalProperties": false,
         "description": "bent is the quality of a line being sharply curved or having an angle.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Bent",
         "type": "object"
      },
      "BerkeliumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BerkeliumAtom",
         "type": "object"
      },
      "BerylliumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BerylliumAtom",
         "type": "object"
      },
      "BetaStrand": {
         "additionalProperties": false,
         "description": "A beta strand is structural region of a protein that is characterized by a roughly planar sequence of amino acid residues forming hydrogen bonds between the N-O and the C=O  of another part of the peptide\nand having their side chains perpendicular to the planar axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BetaStrand",
         "type": "object"
      },
      "BibliographySection": {
         "additionalProperties": false,
         "description": "A bibliography section is a document section that is composed of a list of references used in the development of the work.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BibliographySection",
         "type": "object"
      },
      "Bill": {
         "additionalProperties": false,
         "description": "A bill is proposed legislation under consideration by a legislature.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Bill",
         "type": "object"
      },
      "BinaryCompound": {
         "additionalProperties": false,
         "description": "A binary compound is a mereological maximum sum of two kinds of weakly connected entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BinaryCompound",
         "type": "object"
      },
      "BinaryScale": {
         "additionalProperties": false,
         "description": "A binary scale is a measurement scale that specifies a choice between two values.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BinaryScale",
         "type": "object"
      },
      "BindingSite": {
         "additionalProperties": false,
         "description": "A binding site is a molecular site which when occupied with particular ligands leads to structural transformations that initiatiate new moelcular processes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BindingSite",
         "type": "object"
      },
      "BiochemicalActivation": {
         "additionalProperties": false,
         "description": "biochemical activation is a molecular interaction that increases the catalytic rate of the target enzyme.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiochemicalActivation",
         "type": "object"
      },
      "BiochemicalInhibition": {
         "additionalProperties": false,
         "description": "biochemical inhibition is a molecular interaction that decreases the catalytic rate of the target enzyme.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiochemicalInhibition",
         "type": "object"
      },
      "BiochemicalPathway": {
         "additionalProperties": false,
         "description": "A biochemical pathway specifies a series of biochemical modifications and transformations towards achieving some biological outcome.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiochemicalPathway",
         "type": "object"
      },
      "BiochemicalReaction": {
         "additionalProperties": false,
         "description": "A biochemical reaction is a biochemical process that involves the conversion of at least one chemical participant (target) into another (product) by an enzyme (agent).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiochemicalReaction",
         "type": "object"
      },
      "BioinformaticData": {
         "additionalProperties": false,
         "description": "bioinformatic data is data genereated or used for computer-based investigations of biological phenomena.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BioinformaticData",
         "type": "object"
      },
      "Biologic": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Biologic",
         "type": "object"
      },
      "BiologicalData": {
         "additionalProperties": false,
         "description": "biological data is scientific data relevant to biology.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiologicalData",
         "type": "object"
      },
      "BiologicalEntity": {
         "additionalProperties": false,
         "description": "A biological entity is a heterogeneous substance that contains genomic material or is the product of a biological process.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiologicalEntity",
         "type": "object"
      },
      "BiologicalFluid": {
         "additionalProperties": false,
         "description": "A biological fluid is a fluid of biological origin.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiologicalFluid",
         "type": "object"
      },
      "BiologicalQuality": {
         "additionalProperties": false,
         "description": "A biological quality is a quality held by a biological entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiologicalQuality",
         "type": "object"
      },
      "BiologicalReproduction": {
         "additionalProperties": false,
         "description": "biological reproduction is the biological process by which one or more biological organisms are produced from their \"parents\".",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiologicalReproduction",
         "type": "object"
      },
      "BiologicalSex": {
         "additionalProperties": false,
         "description": "biological sex is the quality of a biological organism based on reproductive function or organs.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiologicalSex",
         "type": "object"
      },
      "BiomolecularStructureDescriptor": {
         "additionalProperties": false,
         "description": "A biomolecular structure descriptor is structure description for organic compounds.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiomolecularStructureDescriptor",
         "type": "object"
      },
      "Biopolymer": {
         "additionalProperties": false,
         "description": "A biopolymer is an organic polymer that are typically produced by the cells of living organisms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Biopolymer",
         "type": "object"
      },
      "BiopolymerSequence": {
         "additionalProperties": false,
         "description": "A sequence is a primary structure descriptor in which each of the letters in the string represents a monomeric unit (residue) in which adjacent letters represent the connectivity between the monomeric units.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BiopolymerSequence",
         "type": "object"
      },
      "Biosynthesis": {
         "additionalProperties": false,
         "description": "biosynthesis is the synthesis of an organic compound in a living organism, usually aided by enzymes.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Biosynthesis",
         "type": "object"
      },
      "Birthing": {
         "additionalProperties": false,
         "description": "birthing is the process by which a biological organism is brought into existence.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Birthing",
         "type": "object"
      },
      "BismuthAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BismuthAtom",
         "type": "object"
      },
      "BlockHistogram": {
         "additionalProperties": false,
         "description": "A block histogram contains an x-axis that is divided into bins which correspond to value ranges. Each item in the data set is drawn as a rectangular block, and the blocks are piled into the bins to show how many values in each range.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BlockHistogram",
         "type": "object"
      },
      "Blog": {
         "additionalProperties": false,
         "description": "A blog is a publication accessible at some website and is typically about various experiences.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Blog",
         "type": "object"
      },
      "BohriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BohriumAtom",
         "type": "object"
      },
      "Boldness": {
         "additionalProperties": false,
         "description": "boldness is the trait of being willing to undertake things that involve risk or danger.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Boldness",
         "type": "object"
      },
      "Book": {
         "additionalProperties": false,
         "description": "A book is a publication composed of a large number of entries.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Book",
         "type": "object"
      },
      "BookSection": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BookSection",
         "type": "object"
      },
      "BookSeries": {
         "additionalProperties": false,
         "description": "A book series is a collection of books that have been sequentially published.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BookSeries",
         "type": "object"
      },
      "BookVolume": {
         "additionalProperties": false,
         "description": "A book volume is a book that is part of a collection.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BookVolume",
         "type": "object"
      },
      "Booklet": {
         "additionalProperties": false,
         "description": "A booklet is a document that lacks a named publisher or sponsoring institution.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Booklet",
         "type": "object"
      },
      "Boredom": {
         "additionalProperties": false,
         "description": "boredom is the emotion experience by those not interested in their surroundings or available activities.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Boredom",
         "type": "object"
      },
      "BoronAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BoronAtom",
         "type": "object"
      },
      "BottomValueAxis": {
         "additionalProperties": false,
         "description": "A bottom value axis is a value axis that is spatially positioned to the bottom of the plot area.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BottomValueAxis",
         "type": "object"
      },
      "Boxplot": {
         "additionalProperties": false,
         "description": "A boxplot (box-and-whisker diagram) is a convenient way of graphically depicting groups of numerical data through their five-number summaries: the smallest observation (sample minimum), lower quartile (Q1), median (Q2), upper quartile (Q3), and largest observation (sample maximum).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Boxplot",
         "type": "object"
      },
      "BrandName": {
         "additionalProperties": false,
         "description": "A brand name is a trademarked and marketed name of a product.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BrandName",
         "type": "object"
      },
      "Brief": {
         "additionalProperties": false,
         "description": "A brief is a written legal document used in various legal adversarial systems that is presented to a court arguing why one party to a particular case should prevail.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Brief",
         "type": "object"
      },
      "BromineAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BromineAtom",
         "type": "object"
      },
      "BrownianMotion": {
         "additionalProperties": false,
         "description": "Brownian motion is the seemlingly random movement of particles suspended in a fluid.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BrownianMotion",
         "type": "object"
      },
      "BubbleChart": {
         "additionalProperties": false,
         "description": "A bubble chart contains circles whose area corresponds to a value.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BubbleChart",
         "type": "object"
      },
      "Buffer": {
         "additionalProperties": false,
         "description": "A buffer is a dissolved chemical substance that resists change in pH upon addition of small amounts of acid or base.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Buffer",
         "type": "object"
      },
      "BufferRole": {
         "additionalProperties": false,
         "description": "A buffer role is the role of a chemical substance which maintains a pH at a near constant value.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "BufferRole",
         "type": "object"
      },
      "CadmiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CadmiumAtom",
         "type": "object"
      },
      "CaesiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CaesiumAtom",
         "type": "object"
      },
      "CalciumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CalciumAtom",
         "type": "object"
      },
      "CaliforniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CaliforniumAtom",
         "type": "object"
      },
      "Cancelled": {
         "additionalProperties": false,
         "description": "cancelled is a process status in which the process, while planned to occur, will not occur.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Cancelled",
         "type": "object"
      },
      "Capability": {
         "additionalProperties": false,
         "description": "A capability is a realizable entity whose basis lies in one or more parts or qualities and reflects possibility of an entity to behave in a specified way under certain conditions or in response to a certain stimulus (trigger).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Capability",
         "type": "object"
      },
      "Carbohydrate": {
         "additionalProperties": false,
         "description": "a carbohydrate is an organic molecule composed of two or more monosaccharides.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Carbohydrate",
         "type": "object"
      },
      "CarbohydrateResidue": {
         "additionalProperties": false,
         "description": "A carbohydrate residue is a part of a molecule that was derived from a monosaccharide molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CarbohydrateResidue",
         "type": "object"
      },
      "CarbonAllotrope": {
         "additionalProperties": false,
         "description": "A carbon allotrope is a chemical substance composed of carbon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CarbonAllotrope",
         "type": "object"
      },
      "CarbonAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CarbonAtom",
         "type": "object"
      },
      "CarbonNanofoam": {
         "additionalProperties": false,
         "description": "carbon nanofoam is an allotrope of carbon that consists of a low-density cluster-assembly of carbon atoms strung together in a loose three-dimensional web. Each cluster is about 6 nanometers wide and consists of about 4000 carbon atoms linked in graphite-like sheets that are given negative curvature by the inclusion of heptagons among the regular hexagonal pattern.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CarbonNanofoam",
         "type": "object"
      },
      "CartesianCoordinate": {
         "additionalProperties": false,
         "description": "A Cartesian coordinate is the signed distance of a point to some referent line.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CartesianCoordinate",
         "type": "object"
      },
      "CartesianCoordinateAxis": {
         "additionalProperties": false,
         "description": "A Cartesian coordinate axis is an axis whose behavior follows that of a Cartesian coordinate system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CartesianCoordinateAxis",
         "type": "object"
      },
      "CartesianCoordinateSystem": {
         "additionalProperties": false,
         "description": "A Cartesian coordinate system specifies each point uniquely in a plane by a pair of numerical coordinates, which are the signed distances from the point to two fixed perpendicular directed lines, measured in the same unit of length.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CartesianCoordinateSystem",
         "type": "object"
      },
      "Catabolism": {
         "additionalProperties": false,
         "description": "Anabolism is the set of metabolic processes that take apart larger chemical entities units into smaller chemical entities.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Catabolism",
         "type": "object"
      },
      "Catalog": {
         "additionalProperties": false,
         "description": "A catalog is a systemic collection of items of the same type.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Catalog",
         "type": "object"
      },
      "Catalyst": {
         "additionalProperties": false,
         "description": "A catalyst is a molecule that has the capability to reduce the activation energy of a reaction and hence increase the overall rate of reaction.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Catalyst",
         "type": "object"
      },
      "CatalyticRole": {
         "additionalProperties": false,
         "description": "The role of a chemical participant that serves to increase the rate of reaction by lowering the activiation energy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CatalyticRole",
         "type": "object"
      },
      "CatalyzedReaction": {
         "additionalProperties": false,
         "description": "A catalyzed reaction is a chemical reaction that is facilitated by a catalyst.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CatalyzedReaction",
         "type": "object"
      },
      "Category": {
         "additionalProperties": false,
         "description": "A category is a class of entities having particular shared characteristics.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Category",
         "type": "object"
      },
      "CategoryAxis": {
         "additionalProperties": false,
         "description": "A category axis is an axis in which the position along the line is partioned into categories.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CategoryAxis",
         "type": "object"
      },
      "Cation": {
         "additionalProperties": false,
         "description": "An anion is an atom or molecule with a net positive electrical charge.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Cation",
         "type": "object"
      },
      "CationPiInteraction": {
         "additionalProperties": false,
         "description": "A cation pi interaction is an ionic interaction between the localized negative charge of \u03c0 orbital electrons, located above and below the plane of an aromatic ring, and a positive charge.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CationPiInteraction",
         "type": "object"
      },
      "Cell": {
         "additionalProperties": false,
         "description": "A cell is a biological entity that is contained by a plasma membrane.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Cell",
         "type": "object"
      },
      "CellLine": {
         "additionalProperties": false,
         "description": "A cell line is a collection of genetically identifical cells.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CellLine",
         "type": "object"
      },
      "Cellinformational": {
         "additionalProperties": false,
         "description": "The minimal unit of a cellular automaton that can change state and has an associated behavior.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Cellinformational",
         "type": "object"
      },
      "CellularOrganism": {
         "additionalProperties": false,
         "description": "A cellular organism is an organism that contains one or more cells.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CellularOrganism",
         "type": "object"
      },
      "CellularPhoneNumber": {
         "additionalProperties": false,
         "description": "a cellular phone number is a number to connect to a mobile device",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CellularPhoneNumber",
         "type": "object"
      },
      "CellularQuality": {
         "additionalProperties": false,
         "description": "cellular quality is the quality of a cell",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CellularQuality",
         "type": "object"
      },
      "CenterOfMass": {
         "additionalProperties": false,
         "description": "The center of mass (aka barycenter) is the weighted average location of all the mass in a body or group of bodies.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CenterOfMass",
         "type": "object"
      },
      "CentralityMeasure": {
         "additionalProperties": false,
         "description": "A central tendency measure is a central value or a typical value for a probability distribution.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CentralityMeasure",
         "type": "object"
      },
      "CentrifugationPellet": {
         "additionalProperties": false,
         "description": "A centrifugation pellet is a solid substance that forms as a result of compaction by a centrifuge.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CentrifugationPellet",
         "type": "object"
      },
      "CentrifugationSubstance": {
         "additionalProperties": false,
         "description": "A centrifugation substance is a substance that is the target or product of centrifugation.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CentrifugationSubstance",
         "type": "object"
      },
      "Century": {
         "additionalProperties": false,
         "description": "A century is a period of one hundred years.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Century",
         "type": "object"
      },
      "CeriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CeriumAtom",
         "type": "object"
      },
      "Chaoite": {
         "additionalProperties": false,
         "description": "chaoite is an allotrope of carbon that is slightly harder than graphite with a reflection colour of grey to white.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Chaoite",
         "type": "object"
      },
      "Chapter": {
         "additionalProperties": false,
         "description": "A chapter is a document section of a book or thesis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Chapter",
         "type": "object"
      },
      "Character": {
         "additionalProperties": false,
         "description": "A character is a language symbol used to construct words.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Character",
         "type": "object"
      },
      "CharacterPosition": {
         "additionalProperties": false,
         "description": "The ordinal position of a character in a sequence of characters.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CharacterPosition",
         "type": "object"
      },
      "ChargeQuality": {
         "additionalProperties": false,
         "description": "charge quality is the quality pertaining to electric charge.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChargeQuality",
         "type": "object"
      },
      "Charged": {
         "additionalProperties": false,
         "description": "The quality of having a charge.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Charged",
         "type": "object"
      },
      "Chart": {
         "additionalProperties": false,
         "description": "A chart is a figure that displays the relationship among tabular numeric data, functions or some kinds of qualitative structures.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Chart",
         "type": "object"
      },
      "Chemical-diseaseAssociation": {
         "additionalProperties": false,
         "description": "A chemical-disease association is an association between a chemical and a disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Chemical-diseaseAssociation",
         "type": "object"
      },
      "Chemical-geneAssocation": {
         "additionalProperties": false,
         "description": "a chemical-gene assocation is an assocation between a chemical and a gene.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Chemical-geneAssocation",
         "type": "object"
      },
      "Chemical-pathwayAssociation": {
         "additionalProperties": false,
         "description": "a chemical-pathway association is an association between a chemical and a pathway.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Chemical-pathwayAssociation",
         "type": "object"
      },
      "ChemicalComplex": {
         "additionalProperties": false,
         "description": "A chemical complex is a chemical substance composed of weakly connected molecules and ions in a known stoichiometry.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalComplex",
         "type": "object"
      },
      "ChemicalData": {
         "additionalProperties": false,
         "description": "A chemical datum is a scientific data item which conforms to some specification, either for how it is calculated or for how it is measured, and is commonly used in the domain of chemistry to name and differentiate different numeric properties (both calculated and measured) which are about chemical entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalData",
         "type": "object"
      },
      "ChemicalDegradationPathway": {
         "additionalProperties": false,
         "description": "A chemical degradation pathway is a pathway involved in the disassembly of a chemical.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalDegradationPathway",
         "type": "object"
      },
      "ChemicalDestruction": {
         "additionalProperties": false,
         "description": "chemical destruction is destruction of a chemical entity to its chemical constituents through one ormore chemical interactions or reactions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalDestruction",
         "type": "object"
      },
      "ChemicalEffect": {
         "additionalProperties": false,
         "description": "A chemical effect is a chemical interaction in which a chemical elicits a marked characteristic of a biological system.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalEffect",
         "type": "object"
      },
      "ChemicalElement": {
         "additionalProperties": false,
         "description": "A chemical element is a (effectively) homogeneous substance composed of one type of atom.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalElement",
         "type": "object"
      },
      "ChemicalEntity": {
         "additionalProperties": false,
         "description": "A chemical entity is a material entity that pertains to chemistry.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalEntity",
         "type": "object"
      },
      "ChemicalEntityRole": {
         "additionalProperties": false,
         "description": "A chemical role is a processual role held by a chemical entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalEntityRole",
         "type": "object"
      },
      "ChemicalExposure": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalExposure",
         "type": "object"
      },
      "ChemicalFunctionalGroup": {
         "additionalProperties": false,
         "description": "A chemical functional group is a submolecule that confers specific chemical properties.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalFunctionalGroup",
         "type": "object"
      },
      "ChemicalIdentifier": {
         "additionalProperties": false,
         "description": "A chemical identifier is an identifier for a chemical entity",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalIdentifier",
         "type": "object"
      },
      "ChemicalInteraction": {
         "additionalProperties": false,
         "description": "A chemical interaction is a biochemical process in which chemical entities interact through some set of attractive forces.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalInteraction",
         "type": "object"
      },
      "ChemicalNotation": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalNotation",
         "type": "object"
      },
      "ChemicalQuality": {
         "additionalProperties": false,
         "description": "chemical quality is the quality of a chemical entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalQuality",
         "type": "object"
      },
      "ChemicalReaction": {
         "additionalProperties": false,
         "description": "A chemical reaction is a process that leads to the transformation of one set of chemical substances to another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalReaction",
         "type": "object"
      },
      "ChemicalReactionPathway": {
         "additionalProperties": false,
         "description": "A chemical reaction pathway specifies a series of chemical reactions towards producing some chemical product.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalReactionPathway",
         "type": "object"
      },
      "ChemicalSalt": {
         "additionalProperties": false,
         "description": "a chemical salt is a heterogenous substance composed of an ionic assembly of cations and anions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalSalt",
         "type": "object"
      },
      "ChemicalStructure": {
         "additionalProperties": false,
         "description": "chemical structure is the structure of a chemical entity in terms of its molecular geometry and electronic structure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalStructure",
         "type": "object"
      },
      "ChemicalSubstance": {
         "additionalProperties": false,
         "description": "A chemical substance is a chemical entity composed of two or more weakly (non-covalently) interacting chemical entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalSubstance",
         "type": "object"
      },
      "ChemicalSubstanceRole": {
         "additionalProperties": false,
         "description": "A chemical substance role is a chemical entity role held by a chemical substance.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalSubstanceRole",
         "type": "object"
      },
      "ChemicalSynthesis": {
         "additionalProperties": false,
         "description": "chemical synthesis is synthesis of a chemical entity from physical precursors through one or more chemical interactions or reactions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalSynthesis",
         "type": "object"
      },
      "ChemicalSynthesisPathway": {
         "additionalProperties": false,
         "description": "A chemical synthesis pathway is a pathway involved in the assembly of a chemical.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalSynthesisPathway",
         "type": "object"
      },
      "ChemicalTransport": {
         "additionalProperties": false,
         "description": "chemical transport is the directed movement of a chemical entity by some agent (e.g. transporter).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChemicalTransport",
         "type": "object"
      },
      "ChlorineAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChlorineAtom",
         "type": "object"
      },
      "ChromiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ChromiumAtom",
         "type": "object"
      },
      "Chromosome": {
         "additionalProperties": false,
         "description": "A chromosome is a molecular complex of circular or linear DNA and bound proteins.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Chromosome",
         "type": "object"
      },
      "CircularlyHelical": {
         "additionalProperties": false,
         "description": "a circularly helical quality is the quality of a helix that is connected in a circle or loop.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CircularlyHelical",
         "type": "object"
      },
      "Circumference": {
         "additionalProperties": false,
         "description": "circumference is the length of the outline of a circle or ellipse. it is defined as c = 2*pi*r, where r is the radius.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Circumference",
         "type": "object"
      },
      "CisRegulatoryElement": {
         "additionalProperties": false,
         "description": "A cisregulatory element is a DNA sequence located on the same DNA strand or chromosome as the gene whose expression it affects.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CisRegulatoryElement",
         "type": "object"
      },
      "Citation": {
         "additionalProperties": false,
         "description": "A citation is a textual entity which denotes a source described in the bibliography or reference section of a document.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Citation",
         "type": "object"
      },
      "City": {
         "additionalProperties": false,
         "description": "A city is a relatively large and permanent settlement.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "City",
         "type": "object"
      },
      "Class": {
         "additionalProperties": false,
         "description": "A class is a collection of sets which can be unambiguously defined by a property that all its members share.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Class",
         "type": "object"
      },
      "Clause": {
         "additionalProperties": false,
         "description": "A clause consists of a subject and a predicate.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Clause",
         "type": "object"
      },
      "ClinicalTrial": {
         "additionalProperties": false,
         "description": "A clinical trial is an intervention trial to determine the safety and efficacy of  medical interventions (e.g., drugs, diagnostics, devices, therapy protocols).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ClinicalTrial",
         "type": "object"
      },
      "ClosedInterval": {
         "additionalProperties": false,
         "description": "A closed interval is an interval that includes its endpoints, and is denoted with square brackets.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ClosedInterval",
         "type": "object"
      },
      "Co-enzymeRole": {
         "additionalProperties": false,
         "description": "A co-factor role in which the chemical entity is modified during catalysis and must be regenerated.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Co-enzymeRole",
         "type": "object"
      },
      "Co-substrateRole": {
         "additionalProperties": false,
         "description": "A co-enzyme role of a chemical entity that is transiently associated, and is regenerated in a separate reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Co-substrateRole",
         "type": "object"
      },
      "CobaltAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CobaltAtom",
         "type": "object"
      },
      "CodingFrameOffset": {
         "additionalProperties": false,
         "description": "a coding frame offset is a numeric value that indicates the number of bases from a reference start codon position.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CodingFrameOffset",
         "type": "object"
      },
      "CodingSequence": {
         "additionalProperties": false,
         "description": "a coding sequence is a sequence of nucleotides which encode a RNA or protein product..",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CodingSequence",
         "type": "object"
      },
      "CofactorRole": {
         "additionalProperties": false,
         "description": "The role of a chemical entity involved in the mechanism for enzyme-mediated catalysis.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CofactorRole",
         "type": "object"
      },
      "Collection": {
         "additionalProperties": false,
         "description": "A collection is a set for which there exists at least one member, although any member need not to exist at any point in the collection's existence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Collection",
         "type": "object"
      },
      "CollectionItem": {
         "additionalProperties": false,
         "description": "a collection item is an item in a collection.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CollectionItem",
         "type": "object"
      },
      "CollectionOf3dMolecularStructureModels": {
         "additionalProperties": false,
         "description": "A collection of 3D molecular structure models is just that.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CollectionOf3dMolecularStructureModels",
         "type": "object"
      },
      "CollectionOfDocuments": {
         "additionalProperties": false,
         "description": "A collection of documents is a non-zero set of documents.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CollectionOfDocuments",
         "type": "object"
      },
      "CollectionOfPoints": {
         "additionalProperties": false,
         "description": "A collection of points is a geometric entity that contains a non-zero set of geometric points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CollectionOfPoints",
         "type": "object"
      },
      "CollectionOvopub": {
         "additionalProperties": false,
         "description": "A collection ovopub is an ovopub that contains and links to one or more assertion subgraphs and/or ovopubs, as well as statements describing its provenance. It is used to share a specific set of items or restrict a search to the resources contained therein.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CollectionOvopub",
         "type": "object"
      },
      "Collective": {
         "additionalProperties": false,
         "description": "A collective is a group of entities that share or are motivated by at least one common issue or interest, or work together on a specific project(s) to achieve a common objective.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Collective",
         "type": "object"
      },
      "Column": {
         "additionalProperties": false,
         "description": "A column is a vertical sequence of cells in a cellular automata.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Column",
         "type": "object"
      },
      "Comment": {
         "additionalProperties": false,
         "description": "A comment is a verbal or written remark often related to an added piece of information, or an observation or statement.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Comment",
         "type": "object"
      },
      "CommonName": {
         "additionalProperties": false,
         "description": "A common name is a name that is commonly used.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CommonName",
         "type": "object"
      },
      "Communicating": {
         "additionalProperties": false,
         "description": "communicating is the process of conveying information through the exchange of thoughts, messages, or information, as by speech, visuals, signals, writing, or behaviour.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Communicating",
         "type": "object"
      },
      "CommunicationDevice": {
         "additionalProperties": false,
         "description": "A communication device is a device that facilitates the transmission of information through encoded in an audio or digital signal between a sender and a receiver.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CommunicationDevice",
         "type": "object"
      },
      "Community": {
         "additionalProperties": false,
         "description": "A community is a sizeable social unit that shares common values.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Community",
         "type": "object"
      },
      "ComparativeRole": {
         "additionalProperties": false,
         "description": "A comparative role is an abstract role which holds by comparing some attribute of another object of reference.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ComparativeRole",
         "type": "object"
      },
      "Comparing": {
         "additionalProperties": false,
         "description": "comparing is the process of examining a set of objects and determining their equality or inequality based on one or more features.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Comparing",
         "type": "object"
      },
      "CompleteCharge": {
         "additionalProperties": false,
         "description": "A complete charge is a charge where the value of the charge is a multiple of 1.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CompleteCharge",
         "type": "object"
      },
      "Completed": {
         "additionalProperties": false,
         "description": "completed is that status of a process that successfully unfolds.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Completed",
         "type": "object"
      },
      "CompletelyQuantifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CompletelyQuantifier",
         "type": "object"
      },
      "CompositionalQuality": {
         "additionalProperties": false,
         "description": "composition quality is a quality that describes its composition or anatomy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CompositionalQuality",
         "type": "object"
      },
      "ComputationalEntity": {
         "additionalProperties": false,
         "description": "A computational entity is an information content entity operated on using some computational system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ComputationalEntity",
         "type": "object"
      },
      "Concentration": {
         "additionalProperties": false,
         "description": "concentration is the amount of substance per unit volume of a solution",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Concentration",
         "type": "object"
      },
      "Concept": {
         "additionalProperties": false,
         "description": "A concept is term that refers to a generalization of a set of attributes or entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Concept",
         "type": "object"
      },
      "Conclusion": {
         "additionalProperties": false,
         "description": "A conclusion is a proposition which is reached after considering the evidence, arguments or premises.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Conclusion",
         "type": "object"
      },
      "ConferenceProceedings": {
         "additionalProperties": false,
         "description": "A conference proceedings is a book composed of papers presented at a conference.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ConferenceProceedings",
         "type": "object"
      },
      "Conjunctionand": {
         "additionalProperties": false,
         "description": "AND is a logical operator that has the value true if both of its operands are true, otherwise a value of false.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Conjunctionand",
         "type": "object"
      },
      "Consensus": {
         "additionalProperties": false,
         "description": "consensus is an acceptable resolution, one that can be supported, even if not the preferred outcome for each individual.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Consensus",
         "type": "object"
      },
      "Consonant": {
         "additionalProperties": false,
         "description": "A consonant is a verbal entity of language that is articulated with complete or partial closure of the vocal tract.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Consonant",
         "type": "object"
      },
      "ContainmentQuality": {
         "additionalProperties": false,
         "description": "a containment quality is a quality of being able to contain another entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ContainmentQuality",
         "type": "object"
      },
      "Contempt": {
         "additionalProperties": false,
         "description": "contempt is disgust towards a lower status individual.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Contempt",
         "type": "object"
      },
      "Contentment": {
         "additionalProperties": false,
         "description": "contentment is an emotion characterized by acknowledgement and satisfaction of the current state of affairs.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Contentment",
         "type": "object"
      },
      "ControlGroup": {
         "additionalProperties": false,
         "description": "A control group is a group of individuals that are not subject to an intervention of interest, but rather serve as a baseline to compare the outcomes in the intervention group.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ControlGroup",
         "type": "object"
      },
      "ControlRole": {
         "additionalProperties": false,
         "description": "A control role is the role of an individual that is part of a study, but is not subject to the intervention that is to be tested.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ControlRole",
         "type": "object"
      },
      "ControlVariable": {
         "additionalProperties": false,
         "description": "A control variable that is believed to alter the dependent or independent variables, but may not actually be the focus of the experiment. So that variable will be kept constant or monitored to try to minimise its effect on the experiment.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ControlVariable",
         "type": "object"
      },
      "ControlledObservationalCohortStudy": {
         "additionalProperties": false,
         "description": "In a controlled observational cohort study, two groups of subjects are selected from two populations that are thought to differ in only one characteristic. The groups of subjects are studied for a specific period and contrasted at the end of the study period.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ControlledObservationalCohortStudy",
         "type": "object"
      },
      "Conversing": {
         "additionalProperties": false,
         "description": "conversing a form of interactive, spontaneous communication between two or more agents who are following rules of etiquette.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Conversing",
         "type": "object"
      },
      "Coordinate": {
         "additionalProperties": false,
         "description": "A coordinate is a measurement of position in n-dimensional space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Coordinate",
         "type": "object"
      },
      "CoordinateSystem": {
         "additionalProperties": false,
         "description": "A coordinate system is a specification for spatial location that uses a set of numbers, or coordinates, to uniquely determine the position of a point or other geometric element.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CoordinateSystem",
         "type": "object"
      },
      "CoperniciumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CoperniciumAtom",
         "type": "object"
      },
      "CopperAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CopperAtom",
         "type": "object"
      },
      "CopyNumberVariation": {
         "additionalProperties": false,
         "description": "copy number variation refers to the number of deletions/duplications of a DNA region as compared to some reference state.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CopyNumberVariation",
         "type": "object"
      },
      "CopyrightSection": {
         "additionalProperties": false,
         "description": "A copyright section is a document section that contains a notice of copyright.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CopyrightSection",
         "type": "object"
      },
      "Corporation": {
         "additionalProperties": false,
         "description": "A corporation is an organization that is granted a charter recognizing it as a separate legal entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Corporation",
         "type": "object"
      },
      "Correlation": {
         "additionalProperties": false,
         "description": "A correlation is a statistical relationship involving dependence between two random variables or datasets.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Correlation",
         "type": "object"
      },
      "CorrespondenceSection": {
         "additionalProperties": false,
         "description": "A correspondence section is a document section that contains the details for contacting the corresponding author.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CorrespondenceSection",
         "type": "object"
      },
      "Count": {
         "additionalProperties": false,
         "description": "The number of elements of a finite set of objects.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Count",
         "type": "object"
      },
      "Country": {
         "additionalProperties": false,
         "description": "A country is a region legally identified as a distinct entity in political geography.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Country",
         "type": "object"
      },
      "CovalentBond": {
         "additionalProperties": false,
         "description": "A covalent bond is a strong submolecular interaction between atoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CovalentBond",
         "type": "object"
      },
      "Creating": {
         "additionalProperties": false,
         "description": "creating is the process in which an entity comes into existence.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Creating",
         "type": "object"
      },
      "Criterion": {
         "additionalProperties": false,
         "description": "A criterion is a specification to describe properties used for evaluation.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Criterion",
         "type": "object"
      },
      "CrystalStructure": {
         "additionalProperties": false,
         "description": "A crystal structure is the arrangement of atoms or molecules in a crystalline liquid or solid.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CrystalStructure",
         "type": "object"
      },
      "Curiosity": {
         "additionalProperties": false,
         "description": "curiosity is the strong desire to know or learn something.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Curiosity",
         "type": "object"
      },
      "CuriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CuriumAtom",
         "type": "object"
      },
      "Curvature": {
         "additionalProperties": false,
         "description": "curvature is a quality of a bearer that relates to the presence of curves, bends, or angles.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Curvature",
         "type": "object"
      },
      "Curve": {
         "additionalProperties": false,
         "description": "A curve is a geometric entity that may be located in n-dimensional spatial region whose extension may be n-dimensional,  is composed of at least two fully connected points and does not intersect itself.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Curve",
         "type": "object"
      },
      "CurveSegment": {
         "additionalProperties": false,
         "description": "A curve segment is a part of a curve that consists of at least three points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CurveSegment",
         "type": "object"
      },
      "Curved": {
         "additionalProperties": false,
         "description": "curved is the quality of a line that deviates from straightness in a smooth, continuous fashion.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Curved",
         "type": "object"
      },
      "CylindricalCoordinateSystem": {
         "additionalProperties": false,
         "description": "A cylindrical coordinate system is a three-dimensional coordinate system that specifies point positions by the distance from a chosen reference axis, the direction from the axis relative to a chosen reference direction, and the distance from a chosen reference plane perpendicular to the axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "CylindricalCoordinateSystem",
         "type": "object"
      },
      "DarmstadtiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DarmstadtiumAtom",
         "type": "object"
      },
      "DataAnalysis": {
         "additionalProperties": false,
         "description": "data analysis is a process of inspecting, cleaning, transforming, and modeling data with the goal of highlighting useful information, suggesting conclusions, and supporting decision making.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataAnalysis",
         "type": "object"
      },
      "DataCollection": {
         "additionalProperties": false,
         "description": "data collection is the process of acquiring information.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataCollection",
         "type": "object"
      },
      "DataCollectionDevice": {
         "additionalProperties": false,
         "description": "A data collection device is a device that collects information about one or more objects.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataCollectionDevice",
         "type": "object"
      },
      "DataItem": {
         "additionalProperties": false,
         "description": "A data item consists of information that has been collected/generated towards some purpose.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataItem",
         "type": "object"
      },
      "DataPoint": {
         "additionalProperties": false,
         "description": "A data point is a point that which corresponds to the projection of the values of measurement data against the axes of a statistical graph.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataPoint",
         "type": "object"
      },
      "DataSeries": {
         "additionalProperties": false,
         "description": "A data series is a data set composed of related values displayed in a statistical graph.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataSeries",
         "type": "object"
      },
      "DataStorageDevice": {
         "additionalProperties": false,
         "description": "A data storage device is a device that is capable of storing information.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataStorageDevice",
         "type": "object"
      },
      "DataTransformation": {
         "additionalProperties": false,
         "description": "data transformation is the process of applying an algorithmic procedure to some input data and producing some output data.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DataTransformation",
         "type": "object"
      },
      "Database": {
         "additionalProperties": false,
         "description": "A database is a set of tables.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Database",
         "type": "object"
      },
      "DatabaseColumn": {
         "additionalProperties": false,
         "description": "A database collumn is a column in a database table.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DatabaseColumn",
         "type": "object"
      },
      "DatabaseCross-reference": {
         "additionalProperties": false,
         "description": "A database cross-reference is an association between one data item and another.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DatabaseCross-reference",
         "type": "object"
      },
      "DatabaseEntry": {
         "additionalProperties": false,
         "description": "A database entry is a single, implicitly structured data item in a table.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DatabaseEntry",
         "type": "object"
      },
      "DatabaseKey": {
         "additionalProperties": false,
         "description": "A database key is an informational entity whose value is constructed from one or more database columns.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DatabaseKey",
         "type": "object"
      },
      "DatabaseRow": {
         "additionalProperties": false,
         "description": "A database row is a row that is part of a database table.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DatabaseRow",
         "type": "object"
      },
      "DatabaseTable": {
         "additionalProperties": false,
         "description": "A database table is a set of named columns with zero or more rows composed of cells that contain column values and is part of a database.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DatabaseTable",
         "type": "object"
      },
      "Dataset": {
         "additionalProperties": false,
         "description": "A dataset is a data item that is a collection of data items.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasDataItem": {
               "$ref": "#/$defs/Entity",
               "description": "has data item is a relation between a dataset and any described / referrenced entity.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dataset",
         "type": "object"
      },
      "DateOfDatabaseSubmission": {
         "additionalProperties": false,
         "description": "A date of database submission refers to the moment in time in which some information was submitted/received to a database system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isEndTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isStartTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isTimeBoundaryOf": {
               "$ref": "#/$defs/Entity"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DateOfDatabaseSubmission",
         "type": "object"
      },
      "DateOfIssue": {
         "additionalProperties": false,
         "description": "the date at which an information content entity was made public.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isEndTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isStartTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isTimeBoundaryOf": {
               "$ref": "#/$defs/Entity"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DateOfIssue",
         "type": "object"
      },
      "Day": {
         "additionalProperties": false,
         "description": "A day is a period of 24 hours.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Day",
         "type": "object"
      },
      "Dead": {
         "additionalProperties": false,
         "description": "dead is the quality of an object in which there is a cessation of all biological functions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dead",
         "type": "object"
      },
      "DecimalScale": {
         "additionalProperties": false,
         "description": "A decimal scale of measurement is one that only specifies decimal values.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecimalScale",
         "type": "object"
      },
      "DecompositionReaction": {
         "additionalProperties": false,
         "description": "A decomposition reaction is an inorganic reaction in which molecule is fragmented into submolecules or atoms.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecompositionReaction",
         "type": "object"
      },
      "DecreaseInNumberOfObjectsProduced": {
         "additionalProperties": false,
         "description": "An decrease in the number of objects produced is the negative value of a difference in number of objects produced.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreaseInNumberOfObjectsProduced",
         "type": "object"
      },
      "Decreased": {
         "additionalProperties": false,
         "description": "A quality that has a value that is decreased compared to normal or average.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Decreased",
         "type": "object"
      },
      "DecreasedChemicalDestruction": {
         "additionalProperties": false,
         "description": "decreased chemical destruction is a process in which there is a decrease in the amount of chemical destroyed relative to some reference process",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedChemicalDestruction",
         "type": "object"
      },
      "DecreasedDurationOfProcessFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "decreased duration of process from increased regulation is a process in which the duration of the target process is decreased as a result of increased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedDurationOfProcessFromIncreasedRegulation",
         "type": "object"
      },
      "DecreasedFrequencyOfProcessFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "The increase of regulation leads to a decreased occurence of processes of the target process type.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedFrequencyOfProcessFromIncreasedRegulation",
         "type": "object"
      },
      "DecreasedMolecularDegradationFromDecreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that decreases the frequency, rate or extent of process involved in the destruction of a molecule as a result of decreased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedMolecularDegradationFromDecreasedRegulation",
         "type": "object"
      },
      "DecreasedMolecularDegradationFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that decreases the frequency, rate or extent of process involved in the destruction of a molecule as a result of increased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedMolecularDegradationFromIncreasedRegulation",
         "type": "object"
      },
      "DecreasedMolecularProductionFromDecreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that decreases the frequency, rate or extent of process involved in the production of a molecule as a result of decreased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedMolecularProductionFromDecreasedRegulation",
         "type": "object"
      },
      "DecreasedMolecularProductionFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that decreases the frequency, rate or extent of process involved in the production of a molecule as a result of increased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedMolecularProductionFromIncreasedRegulation",
         "type": "object"
      },
      "DecreasedObjectConsumptionFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "increased regulation leads to an decrease in the consumption of an object of specified type.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedObjectConsumptionFromIncreasedRegulation",
         "type": "object"
      },
      "DecreasedObjectProductionFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "increased regulation leads to a decrease in the number of target objects of a specified type.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedObjectProductionFromIncreasedRegulation",
         "type": "object"
      },
      "DecreasedSpatialExtentOfProcessFromDecreasedRegulation": {
         "additionalProperties": false,
         "description": "The increase of regulation leads to a decreased spatial extent of the target process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasedSpatialExtentOfProcessFromDecreasedRegulation",
         "type": "object"
      },
      "DecreasingLine": {
         "additionalProperties": false,
         "description": "An decreasing line is a line segment in which the startpoint and endpoint are ordered along one dimension and the difference of values in a second dimension is negative.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DecreasingLine",
         "type": "object"
      },
      "DeductiveArgument": {
         "additionalProperties": false,
         "description": "A deductive argument is an argument that asserts that the truth of the conclusion is a logical consequence of the premises.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DeductiveArgument",
         "type": "object"
      },
      "DefaultParameter": {
         "additionalProperties": false,
         "description": "A default parameter is a parameter which has a default value.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DefaultParameter",
         "type": "object"
      },
      "Definition": {
         "additionalProperties": false,
         "description": "A definition is a description that succintly characterizes an entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Definition",
         "type": "object"
      },
      "DeletionVariantRole": {
         "additionalProperties": false,
         "description": "A deletion variant role is the role of an sequence that lacks a sub-sequence relative to the frame of reference.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DeletionVariantRole",
         "type": "object"
      },
      "Dendrogram": {
         "additionalProperties": false,
         "description": "A dendrogram is a tree diagram used to illustrate the arrangement of the clusters produced by hierarchical clustering.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dendrogram",
         "type": "object"
      },
      "Density": {
         "additionalProperties": false,
         "description": "Density (volumetric mass density) is the quantity of mass per unit volume of a substance.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Density",
         "type": "object"
      },
      "DentistRole": {
         "additionalProperties": false,
         "description": "A dentist role is the role of an individual that that specializes in the diagnosis, prevention, and treatment of diseases and conditions of the oral cavity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DentistRole",
         "type": "object"
      },
      "DeoxyribonucleicAcid": {
         "additionalProperties": false,
         "description": "A deoxyribonucleic acid is an organic polymer composed of a sequence of deoxyribonucleotide residues.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DeoxyribonucleicAcid",
         "type": "object"
      },
      "DeoxyribonucleicAcidPrimer": {
         "additionalProperties": false,
         "description": "A deoxyribonucleic acid primer is a deoxyribonucleic acid that enables the synthesis of a complement strand of DNA by binding to it and acting as a point of transcription initiation.",
         "properties": {},
         "required": [],
         "title": "DeoxyribonucleicAcidPrimer",
         "type": "object"
      },
      "DeoxyribonucleicAcidSequence": {
         "additionalProperties": false,
         "description": "A deoxyribonucleic acid sequence is a symbolic representation of the sequence of deoxyribonucleic acid residues in a deoxyribonucleic acid.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DeoxyribonucleicAcidSequence",
         "type": "object"
      },
      "DeoxyribonucleicAcidTemplate": {
         "additionalProperties": false,
         "description": "A deoxyribonucleic acid template is a deoxyribonucleic acid that provides the template to synthesize a complementary strand of DNA through transcription.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DeoxyribonucleicAcidTemplate",
         "type": "object"
      },
      "DeoxyribonucleotideResidue": {
         "additionalProperties": false,
         "description": "A deoxyribonucleotide residue is a part of a molecule that derives from a deoxyribonucleotide.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DeoxyribonucleotideResidue",
         "type": "object"
      },
      "DepartmentChairRole": {
         "additionalProperties": false,
         "description": "A department chain role is the role of an individual that heads a department at a academic organization.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DepartmentChairRole",
         "type": "object"
      },
      "DependentVariable": {
         "additionalProperties": false,
         "description": "A dependent variable is one whose value changes as a consequence of changes in other values in the system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DependentVariable",
         "type": "object"
      },
      "Depression": {
         "additionalProperties": false,
         "description": "depression is an unpleasant emotion linked to aversion to activity that can affect a person's thoughts, behavior, feelings and physical well-being. Depressed individuals may feel sad, anxious, empty, hopeless, worried, helpless, worthless, guilty, irritable, or restless.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Depression",
         "type": "object"
      },
      "Depth": {
         "additionalProperties": false,
         "description": "depth is the dimensional extent into a plane of a 3D projection of the object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Depth",
         "type": "object"
      },
      "Description": {
         "additionalProperties": false,
         "description": "A description is language entity in which elements of a language (formal or natural) are used to characterize an entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Description",
         "type": "object"
      },
      "Descriptor": {
         "additionalProperties": false,
         "description": "A descriptor (index term, subject term, subject heading) is a term that captures the essence of the topic of a document.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Descriptor",
         "type": "object"
      },
      "Design": {
         "additionalProperties": false,
         "description": "A specification of an object, manifested by an agent, intended to accomplish goals, in a particular environment, using a set of primitive components, satisfying a set of requirements, subject to constraints.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Design",
         "type": "object"
      },
      "DesignSpecification": {
         "additionalProperties": false,
         "description": "A design specification is a specification that provides precise and explicit information about the requirements for a product design.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DesignSpecification",
         "type": "object"
      },
      "Desire": {
         "additionalProperties": false,
         "description": "desire is a strong emotion of wanting to have something or wishing for something to happen.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Desire",
         "type": "object"
      },
      "Despair": {
         "additionalProperties": false,
         "description": "despair is depression, hopelessness or lack of hope.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Despair",
         "type": "object"
      },
      "Destroying": {
         "additionalProperties": false,
         "description": "destroying is a process in which something is broken down and/or ceases to exist.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Destroying",
         "type": "object"
      },
      "Device": {
         "additionalProperties": false,
         "description": "A device is usually a constructed tool.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Device",
         "type": "object"
      },
      "Diagnosis": {
         "additionalProperties": false,
         "description": "A diagnosis is the result of a medical investigation to identify a disorder from its signs and symptoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Diagnosis",
         "type": "object"
      },
      "DiagnosticOpinion": {
         "additionalProperties": false,
         "description": "A diagnostic opinion is an opinion resulting from a medical diagnostic procedure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DiagnosticOpinion",
         "type": "object"
      },
      "DiagnosticTest": {
         "additionalProperties": false,
         "description": "A diagnostic test is a procedure performed to confirm, or determine the presence of disease in an individual suspected of having the disease, usually following the report of symptoms, or based on the results of other medical tests.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DiagnosticTest",
         "type": "object"
      },
      "Diamond": {
         "additionalProperties": false,
         "description": "diamond is a carbon allotrope in which each carbon atom in diamond is covalently bonded to four other carbons in a tetrahedron. These tetrahedrons together form a 3-dimensional network of puckered six-membered rings of atoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Diamond",
         "type": "object"
      },
      "Diary": {
         "additionalProperties": false,
         "description": "A diary is a document which describes day-to-day experiences.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Diary",
         "type": "object"
      },
      "Diastereomer": {
         "additionalProperties": false,
         "description": "A diastereomer is a stereoisomer that is not a mirror image of its isomer.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Diastereomer",
         "type": "object"
      },
      "DifferenceInNumberOfObjectsProduced": {
         "additionalProperties": false,
         "description": "A difference in number of objects produced is a count of the number of objects produced with respect to a second variable (space, time, etc)",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DifferenceInNumberOfObjectsProduced",
         "type": "object"
      },
      "DifferentialDiagnosis": {
         "additionalProperties": false,
         "description": "A differential diagnosis (sometimes abbreviated DDx, ddx, DD, D/Dx, or \u0394\u0394) is a systematic diagnostic method used to identify the presence of an entity where multiple alternatives are possible (and the process may be termed differential diagnostic procedure), and may also refer to any of the included candidate alternatives (which may also be termed candidate condition).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DifferentialDiagnosis",
         "type": "object"
      },
      "DifferentialEquation": {
         "additionalProperties": false,
         "description": "A differential equation is a mathematical equation for an unknown function of one or several variables that relates the values of the function itself and its derivatives of various orders.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DifferentialEquation",
         "type": "object"
      },
      "DifferentialGeneExpressionRatio": {
         "additionalProperties": false,
         "description": "A differential gene expression ratio is the ratio of gene expression values from a test sample compared to a control sample.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DifferentialGeneExpressionRatio",
         "type": "object"
      },
      "Diffusion": {
         "additionalProperties": false,
         "description": "diffusion is motion of particles at temperatures above absolute zero.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Diffusion",
         "type": "object"
      },
      "DiffusionEquation": {
         "additionalProperties": false,
         "description": "A diffusion equation describes density fluctuations in a material undergoing diffusion.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DiffusionEquation",
         "type": "object"
      },
      "DimensionalQuantity": {
         "additionalProperties": false,
         "description": "A dimensional quantity is a quantity that has an associated unit.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DimensionalQuantity",
         "type": "object"
      },
      "DimensionlessQuantity": {
         "additionalProperties": false,
         "description": "A dimensionless quantity is a quantity that has no associated unit.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DimensionlessQuantity",
         "type": "object"
      },
      "Dipole-dipoleInteraction": {
         "additionalProperties": false,
         "description": "A dipole-dipole interaction is a weak submolecular interaction between strongly electronegative atoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dipole-dipoleInteraction",
         "type": "object"
      },
      "DirectedAcyclicGraph": {
         "additionalProperties": false,
         "description": "a directed acyclic graph or DAG is a network digram that contains directed edges in which nodes may have multiple parents, but there are no cycles.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DirectedAcyclicGraph",
         "type": "object"
      },
      "DirectedLineSegment": {
         "additionalProperties": false,
         "description": "A directed line segment is a line segment that is contained by an ordered pair \nof endpoints (a start point and an endpoint).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DirectedLineSegment",
         "type": "object"
      },
      "DisGeNETDiseaseSpecificity": {
         "additionalProperties": false,
         "description": "DisGeNET Disease specificity is a measure of disease coverage. It is calculated from the negative base 2 log of the ratio of number of diseases associated to the total number of diseases.\n\nThe measure is described here: http://www.disgenet.org/web/DisGeNET/menu/dbinfo#specificity",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DisGeNETDiseaseSpecificity",
         "type": "object"
      },
      "DisGeNETPleiotropyIndex": {
         "additionalProperties": false,
         "description": "The DisGeNET pleiotropy index is a measure of specificity as it pertains to classes of disease. The disease pleotropy index is computed from the ratio of the number of disease classes associated with an entity over the total number of disease classes multplied by 100. \n\nThe measure is defined here: http://www.disgenet.org/web/DisGeNET/menu/dbinfo#pleiotropy",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DisGeNETPleiotropyIndex",
         "type": "object"
      },
      "Disagreement": {
         "additionalProperties": false,
         "description": "agreement is the result of consensus decision making when members of the group do not all agree.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Disagreement",
         "type": "object"
      },
      "Disappointment": {
         "additionalProperties": false,
         "description": "disappointment is the feeling of dissatisfaction that follows the failure of expectations or hopes to manifest.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Disappointment",
         "type": "object"
      },
      "Discouragement": {
         "additionalProperties": false,
         "description": "Discouragement is the emtion of having lost confidence or enthusiasm.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Discouragement",
         "type": "object"
      },
      "DiscussionSection": {
         "additionalProperties": false,
         "description": "The discussion section is a document section containing a summary of the findings, a reflection on the significance of findings, comparison with related work, among others.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DiscussionSection",
         "type": "object"
      },
      "Disease": {
         "additionalProperties": false,
         "description": "disease is the outward manifestation of one or more disorders.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Disease",
         "type": "object"
      },
      "Disgust": {
         "additionalProperties": false,
         "description": "Disgust is a feeling of revulsion or profound disapproval aroused by something unpleasant or offensive.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Disgust",
         "type": "object"
      },
      "Disjunctionor": {
         "additionalProperties": false,
         "description": "OR is a logical operator that results in true whenever one or more of its operands are true.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Disjunctionor",
         "type": "object"
      },
      "Disordered": {
         "additionalProperties": false,
         "description": "disordered is a structural quality in which the parts of an object are non-rigid.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Disordered",
         "type": "object"
      },
      "DisplacementReaction": {
         "additionalProperties": false,
         "description": "A displacement reaction is an inorganic reaction in which a elementary substance displaces and sets free a constituent atom from a molecule.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DisplacementReaction",
         "type": "object"
      },
      "Disposition": {
         "additionalProperties": false,
         "description": "A disposition is the tendency of a capability to be exhibited under certain conditions or in response to a certain stimulus (trigger).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isDispositionOf": {
               "$ref": "#/$defs/Entity",
               "description": "is disposition of is a relation between a disposition and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Disposition",
         "type": "object"
      },
      "DissatisfiedQualifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DissatisfiedQualifier",
         "type": "object"
      },
      "DisulfideBond": {
         "additionalProperties": false,
         "description": "A disulfide bond is a bond between two sulfur atoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DisulfideBond",
         "type": "object"
      },
      "DnaGene": {
         "additionalProperties": false,
         "description": "A gene that is located on DNA.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DnaGene",
         "type": "object"
      },
      "Doctor": {
         "additionalProperties": false,
         "description": "A doctor is an individual who practices medicine, which is concerned with promoting, maintaining or restoring human health through the study, diagnosis, and treatment of disease, injury, and other physical and mental impairments.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Doctor",
         "type": "object"
      },
      "DoctorRole": {
         "additionalProperties": false,
         "description": "A doctor role is the role of an individual who practices medicine, which is concerned with promoting, maintaining or restoring human health through the study, diagnosis, and treatment of disease, injury, and other physical and mental impairments.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DoctorRole",
         "type": "object"
      },
      "Document": {
         "additionalProperties": false,
         "description": "A document is a bounded physical or digital representation of a body of information designed with the capacity (and usually intent) to communicate.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Document",
         "type": "object"
      },
      "DocumentComponent": {
         "additionalProperties": false,
         "description": "A bibliographic attribute is an attribute related to publications.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DocumentComponent",
         "type": "object"
      },
      "DocumentSection": {
         "additionalProperties": false,
         "description": "A document section is a component of a document.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DocumentSection",
         "type": "object"
      },
      "DocumentTitle": {
         "additionalProperties": false,
         "description": "A document title is a textual entity that summarizes the topic of the document in one sentence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DocumentTitle",
         "type": "object"
      },
      "DocumentVersion": {
         "additionalProperties": false,
         "description": "A document version is a version of a work in some sequence of derivative works.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DocumentVersion",
         "type": "object"
      },
      "Dose": {
         "additionalProperties": false,
         "description": "A dose is the quantity of a chemical substance administered to a biological system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dose",
         "type": "object"
      },
      "DoubleArrowedLineSegment": {
         "additionalProperties": false,
         "description": "A double arrowed line is an arrowed line in which both terminal points are  tangentially part of different triangles that bisect the line.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DoubleArrowedLineSegment",
         "type": "object"
      },
      "DoubleBond": {
         "additionalProperties": false,
         "description": "A double bond is a covalent bond between a pair of atoms in which two pairs of electrons are shared.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DoubleBond",
         "type": "object"
      },
      "DoubleDisplacementReaction": {
         "additionalProperties": false,
         "description": "A double displacement reaction is a displacement reaction in which two molecules swap ions, effectively displacing each other to form two new molecules.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DoubleDisplacementReaction",
         "type": "object"
      },
      "DoubleStrandedDNA": {
         "additionalProperties": false,
         "description": "double stranded nucleic acid is a molecular complex composed of two weakly connected deoxyribonucleic acids.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DoubleStrandedDNA",
         "type": "object"
      },
      "DoubleStrandedNucleicAcid": {
         "additionalProperties": false,
         "description": "double stranded nucleic acid is a molecular complex composed of two weakly connected nucleic acids.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DoubleStrandedNucleicAcid",
         "type": "object"
      },
      "DoubleStrandedRNA": {
         "additionalProperties": false,
         "description": "double stranded nucleic acid is a molecular complex composed of two weakly connected ribonucleic acids.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DoubleStrandedRNA",
         "type": "object"
      },
      "Draft": {
         "additionalProperties": false,
         "description": "draft is the quality of text that has not yet complete.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Draft",
         "type": "object"
      },
      "Dread": {
         "additionalProperties": false,
         "description": "dread is the instense negative emotion that induces fear and apprehension.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dread",
         "type": "object"
      },
      "DropLine": {
         "additionalProperties": false,
         "description": "A drop line is a statistical graph line that vertically or horizontally connects a data series line with a value axis in a statistical graph.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DropLine",
         "type": "object"
      },
      "Drug": {
         "additionalProperties": false,
         "description": "A drug is a chemical substance that contains one or more active ingredients that regulate one or more biological processes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Drug",
         "type": "object"
      },
      "DrugDrugInteraction": {
         "additionalProperties": false,
         "description": "A drug-drug interaction is an interaction in which two drugs interact in such a way to produce a non-additive biological response.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DrugDrugInteraction",
         "type": "object"
      },
      "DrugRegulatoryAuthority": {
         "additionalProperties": false,
         "description": "A drug regulatory authority is a regulatory authority which acts to control what substances may be used to treat individuals.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DrugRegulatoryAuthority",
         "type": "object"
      },
      "DubniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DubniumAtom",
         "type": "object"
      },
      "Duplicate": {
         "additionalProperties": false,
         "description": "a duplicate is an object that is an exact copy of another item",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Duplicate",
         "type": "object"
      },
      "DuplicateRole": {
         "additionalProperties": false,
         "description": "a duplicate role is a comparative role that holds when one item is an exact copy of another in a specific set.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DuplicateRole",
         "type": "object"
      },
      "Dying": {
         "additionalProperties": false,
         "description": "dying is a process in which a biological entity ceases to exist.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dying",
         "type": "object"
      },
      "Dysfunction": {
         "additionalProperties": false,
         "description": "dysfunction is a capability to act in a manner that is abnormal or opposite  to the object's typical function.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Dysfunction",
         "type": "object"
      },
      "DysprosiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "DysprosiumAtom",
         "type": "object"
      },
      "E.coli": {
         "additionalProperties": false,
         "description": "Escherichia coli (e coli) is a Gram-negative, rod-shaped bacterium that is commonly found in the lower intestine of warm-blooded organisms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "E.coli",
         "type": "object"
      },
      "Ecstasy": {
         "additionalProperties": false,
         "description": "ecstacy is an emotion characterized by a heightened state of consciousness with total involvement of a subject.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ecstasy",
         "type": "object"
      },
      "Edge": {
         "additionalProperties": false,
         "description": "an edge is a line connecting two graph vertices.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Edge",
         "type": "object"
      },
      "EditedPublication": {
         "additionalProperties": false,
         "description": "An edited publication is a publication that has been examined and potentially changed by an editor.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EditedPublication",
         "type": "object"
      },
      "EditionNumber": {
         "additionalProperties": false,
         "description": "An edition number is count of a literary work edited and published, as by a certain editor or in a certain manner including being printed during some interval of time.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EditionNumber",
         "type": "object"
      },
      "EditorReviewed": {
         "additionalProperties": false,
         "description": "editor reviewed is the quality of a textual entity that has been examined and commented on by an editor.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EditorReviewed",
         "type": "object"
      },
      "EffectiveDose": {
         "additionalProperties": false,
         "description": "effective dose is the amount of a substance required to produce an effect on a predefined percentage of a population.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EffectiveDose",
         "type": "object"
      },
      "EinsteiniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EinsteiniumAtom",
         "type": "object"
      },
      "ElectronicStructure": {
         "additionalProperties": false,
         "description": "electronic structure is the electron configuration is the distribution of electrons of an atom or molecule (or other physical structure) in atomic or molecular orbitals.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ElectronicStructure",
         "type": "object"
      },
      "ElectrophilicAdditionReaction": {
         "additionalProperties": false,
         "description": "An electrophilic addition reaction is a polar addition reaction where a pi bond is removed by the creation of two new covalent bonds.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ElectrophilicAdditionReaction",
         "type": "object"
      },
      "Email": {
         "additionalProperties": false,
         "description": "Email message is a digital document that is composed of a header and a body and is transmitted using the SMTP protocol.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Email",
         "type": "object"
      },
      "EmailAddress": {
         "additionalProperties": false,
         "description": "an email address is an identifier to send mail to particular electronic mailbox.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EmailAddress",
         "type": "object"
      },
      "Embarassment": {
         "additionalProperties": false,
         "description": "Embarrassment is the emotion of intense discomfort with oneself, experienced upon having a socially unacceptable act or condition witnessed by or revealed to other.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Embarassment",
         "type": "object"
      },
      "Emotion": {
         "additionalProperties": false,
         "description": "An emotion is a process (experience) that arises internally or from an involuntary physiological response to a stimulus.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Emotion",
         "type": "object"
      },
      "Empty": {
         "additionalProperties": false,
         "description": "empty is the quality of not containing some thing",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Empty",
         "type": "object"
      },
      "EmptySet": {
         "additionalProperties": false,
         "description": "An empty set is a set for which there are exactly 0 members.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EmptySet",
         "type": "object"
      },
      "Enantiomer": {
         "additionalProperties": false,
         "description": "An enantiomer is a stereoisomer that is a mirror image of its isomer.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Enantiomer",
         "type": "object"
      },
      "EndDate": {
         "additionalProperties": false,
         "description": "An end date is a time instant pertaining to date of the end of a process.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isEndTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isStartTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isTimeBoundaryOf": {
               "$ref": "#/$defs/Entity"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EndDate",
         "type": "object"
      },
      "EndPosition": {
         "additionalProperties": false,
         "description": "An end position is the distal position of an object relative to an origin in a linear system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EndPosition",
         "type": "object"
      },
      "EndTime": {
         "additionalProperties": false,
         "description": "An end time is a time instant pertaining to the time at which a process ends.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isEndTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isStartTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isTimeBoundaryOf": {
               "$ref": "#/$defs/Entity"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EndTime",
         "type": "object"
      },
      "Endpoint": {
         "additionalProperties": false,
         "description": "An endpoint is a terminal point that is the last of an ordered \npair of points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Endpoint",
         "type": "object"
      },
      "Entity": {
         "additionalProperties": false,
         "description": "Every thing is an entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Entity",
         "type": "object"
      },
      "Environment": {
         "additionalProperties": false,
         "description": "An environment is a geographic region that hosts certain processes or objects.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Environment",
         "type": "object"
      },
      "Envy": {
         "additionalProperties": false,
         "description": "envy is an emotion that occurs when a person lacks another's (perceived) superior quality, achievement or possession and wishes that the other lacked it.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Envy",
         "type": "object"
      },
      "Enzyme": {
         "additionalProperties": false,
         "description": "An enzyme is a protein or protein complex that realizes its disposition to covalently modify some molecule during a chemical reaction.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Enzyme",
         "type": "object"
      },
      "Epimer": {
         "additionalProperties": false,
         "description": "An epimer is a stereoisomer that differs in configuration at only one stereogenic center.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Epimer",
         "type": "object"
      },
      "EpimerAssociation": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EpimerAssociation",
         "type": "object"
      },
      "Equation": {
         "additionalProperties": false,
         "description": "An equation is a mathematical statement that asserts the equality of two expressions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Equation",
         "type": "object"
      },
      "ErbiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ErbiumAtom",
         "type": "object"
      },
      "EthnicGroup": {
         "additionalProperties": false,
         "description": "An ethnic group is a group of people whose members identify with each other through a common heritage, consisting of a common culture, including a shared language or dialect.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EthnicGroup",
         "type": "object"
      },
      "Ethnicity": {
         "additionalProperties": false,
         "description": "ethnicity is the biological quality of membership in a social group based on a common heritage.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ethnicity",
         "type": "object"
      },
      "Euphoria": {
         "additionalProperties": false,
         "description": "euphoria is an emotion characterized by intense feelings of well-being, elation, happiness, ecstasy, excitement, and joy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Euphoria",
         "type": "object"
      },
      "EuropiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EuropiumAtom",
         "type": "object"
      },
      "EvaluationRole": {
         "additionalProperties": false,
         "description": "An evaluation role is a processual role held by an entity during some evaluation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EvaluationRole",
         "type": "object"
      },
      "Evidence": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Evidence",
         "type": "object"
      },
      "EvolutionaryLineage": {
         "additionalProperties": false,
         "description": "evolutionary lineage is a sequence of species, that form a line of descent, each new species the direct result of speciation from an immediate ancestral species.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "EvolutionaryLineage",
         "type": "object"
      },
      "Evolving": {
         "additionalProperties": false,
         "description": "evolving is a process that elicits change across successive generations in the inherited characteristics of biological populations.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Evolving",
         "type": "object"
      },
      "ExactCross-reference": {
         "additionalProperties": false,
         "description": "An exact cross-reference is a database cross-reference in which one entity is equivalent to the other based on all the entitie's attributes (minus the source).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ExactCross-reference",
         "type": "object"
      },
      "ExcellentQuality": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ExcellentQuality",
         "type": "object"
      },
      "Excerpt": {
         "additionalProperties": false,
         "description": "An excerpt is a contiguous or discontiguous portion of a document.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Excerpt",
         "type": "object"
      },
      "Excitement": {
         "additionalProperties": false,
         "description": "excitement is a positive emotion of feeling great enthusiasm and eagerness.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Excitement",
         "type": "object"
      },
      "ExclusionCriterion": {
         "additionalProperties": false,
         "description": "An exclusion criterion is a criterion that must be absent to satistify the objective.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ExclusionCriterion",
         "type": "object"
      },
      "ExclusiveDisjunctionxor": {
         "additionalProperties": false,
         "description": "XOR, also called exclusive disjunction or (symbolized XOR, EOR, EXOR, or \u2295), is a type of logical disjunction on two operands that results in a value of true if exactly one of the operands has a value of true.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ExclusiveDisjunctionxor",
         "type": "object"
      },
      "ExistenceQuality": {
         "additionalProperties": false,
         "description": "existence quality is the quality of an entity that describe in what environment it is known to exist.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ExistenceQuality",
         "type": "object"
      },
      "Exon": {
         "additionalProperties": false,
         "description": "An exon is a nucleotide sequence encoded by a gene that remains present within the final mature RNA product of that gene after introns have been removed by RNA splicing.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Exon",
         "type": "object"
      },
      "ExpectedValue": {
         "additionalProperties": false,
         "description": "An expected value (or e-value) is the weighted average of all possible values that a random variable can take on.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ExpectedValue",
         "type": "object"
      },
      "Experiment": {
         "additionalProperties": false,
         "description": "An experiment is an investigation that has the goal of verifying, falsifying, or establishing the validity of a hypothesis.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Experiment",
         "type": "object"
      },
      "ExperimentalProtocol": {
         "additionalProperties": false,
         "description": "An experimental protocol is an action specification with respect to the design and implementation of experiments. In addition to providing a detailed set of procedures and lists of required equipment and instruments, experimental protocols often include information on safety precautions, the calculation of results and reporting standards, including statistical analysis and rules for predefining and documenting excluded data to avoid bias.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ExperimentalProtocol",
         "type": "object"
      },
      "False": {
         "additionalProperties": false,
         "description": "false is a truth value in that indicates that it is not true.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "False",
         "type": "object"
      },
      "Family": {
         "additionalProperties": false,
         "description": "A group of people affiliated by consanguinity, affinity, or co-residence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Family",
         "type": "object"
      },
      "FamilyHistory": {
         "additionalProperties": false,
         "description": "family history is the systematic narrative and research of past events relating to a specific family, or specific families.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FamilyHistory",
         "type": "object"
      },
      "Fatal": {
         "additionalProperties": false,
         "description": "fatal is a qualitative intensity value that is more intense than severe, leading to the death/non-functioning of a system.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Fatal",
         "type": "object"
      },
      "FaxNumber": {
         "additionalProperties": false,
         "description": "a fax number is a number to connect to fax device",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FaxNumber",
         "type": "object"
      },
      "Fear": {
         "additionalProperties": false,
         "description": "Fear is a negative emotion induced by a perceived threat that induces one to hide or move quickly away from the location of the perceived threat.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Fear",
         "type": "object"
      },
      "Female": {
         "additionalProperties": false,
         "description": "female is a biological sex of an individual with female sexual organs.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Female",
         "type": "object"
      },
      "FermiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FermiumAtom",
         "type": "object"
      },
      "Fictional": {
         "additionalProperties": false,
         "description": "fictional is the quality of an entity that exists only in a creative work of fiction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Fictional",
         "type": "object"
      },
      "Figure": {
         "additionalProperties": false,
         "description": "A figure is a graphical entity which consists of a visual (n-dimentional) arrangement of information entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Figure",
         "type": "object"
      },
      "FigurePart": {
         "additionalProperties": false,
         "description": "A figure part is a part of a figure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FigurePart",
         "type": "object"
      },
      "File": {
         "additionalProperties": false,
         "description": "A file is an information-bearing object that contains a physical embodiment of some information using a particular character encoding.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "File",
         "type": "object"
      },
      "Finalized": {
         "additionalProperties": false,
         "description": "finalized is the quality of a textual entity that is in its final form.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Finalized",
         "type": "object"
      },
      "FirstName": {
         "additionalProperties": false,
         "description": "A first name is a name that denotes a specific individual between members of a group of individuals, whose members usually share the same surname.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FirstName",
         "type": "object"
      },
      "Fitness": {
         "additionalProperties": false,
         "description": "fitness is the quality of an object with respect to some stated functions or evolutionary adaptation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Fitness",
         "type": "object"
      },
      "Flowchart": {
         "additionalProperties": false,
         "description": "A flowchart is a diagram that represents an algorithm or process, showing the steps as boxes of various kinds, and their order by connecting these with arrows.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Flowchart",
         "type": "object"
      },
      "FluorineAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FluorineAtom",
         "type": "object"
      },
      "ForeignDatabaseKey": {
         "additionalProperties": false,
         "description": "A foreign database key is a database key that refers to a key in some table.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ForeignDatabaseKey",
         "type": "object"
      },
      "FormalSpecification": {
         "additionalProperties": false,
         "description": "A formal specification is a mathematical description of software or hardware that may be used to develop an implementation.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FormalSpecification",
         "type": "object"
      },
      "FranciumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FranciumAtom",
         "type": "object"
      },
      "FreeRadicalAddition": {
         "additionalProperties": false,
         "description": "A free radical addition is a non-polar addition reaction involving free radicals.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FreeRadicalAddition",
         "type": "object"
      },
      "Frequency": {
         "additionalProperties": false,
         "description": "Frequency is the number of occurrences of a repeating event per unit time",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Frequency",
         "type": "object"
      },
      "Frustration": {
         "additionalProperties": false,
         "description": "Frustration is an emotion that arises from the perceived resistance to the fulfillment of individual will.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Frustration",
         "type": "object"
      },
      "Full": {
         "additionalProperties": false,
         "description": "full is the quality of contain an entity such that there is no more space for any additional entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Full",
         "type": "object"
      },
      "FullAgreement": {
         "additionalProperties": false,
         "description": "full agreement is the result of consensus decision making when members of the group unanimously agree.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FullAgreement",
         "type": "object"
      },
      "FullDisagreement": {
         "additionalProperties": false,
         "description": "full agreement is the result of consensus decision making when members of the group unanimously disagree.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FullDisagreement",
         "type": "object"
      },
      "Fullerene": {
         "additionalProperties": false,
         "description": "fullerene is a carbon allotrope which take the form of a hollow sphere, ellipsoid, or tube.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Fullerene",
         "type": "object"
      },
      "Function": {
         "additionalProperties": false,
         "description": "A function is a capability that satisfies some agentive objective, or (evolutionary) optimization.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isFunctionOf": {
               "$ref": "#/$defs/Entity",
               "description": "is function of is a relation between a function and an entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Function",
         "type": "object"
      },
      "FunctionalRnaCodingGene": {
         "additionalProperties": false,
         "description": "A gene that codes for a functional RNA molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FunctionalRnaCodingGene",
         "type": "object"
      },
      "FunctionalSpecification": {
         "additionalProperties": false,
         "description": "A functional specification is a specification that describes the characteristics of an object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FunctionalSpecification",
         "type": "object"
      },
      "FusionGene-diseaseAssociation": {
         "additionalProperties": false,
         "description": "A gene-disease association in which the fusion between two different genes (promotor and/or other coding DNA regions) is associated with the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "FusionGene-diseaseAssociation",
         "type": "object"
      },
      "GadoliniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GadoliniumAtom",
         "type": "object"
      },
      "GalliumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GalliumAtom",
         "type": "object"
      },
      "GanttChart": {
         "additionalProperties": false,
         "description": "A Gantt chart is a bar chart that illustrates a project schedule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GanttChart",
         "type": "object"
      },
      "Gene": {
         "additionalProperties": false,
         "description": "A gene is part of a nucleic acid that contains all the necessary elements to encode a functional transcript.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene",
         "type": "object"
      },
      "Gene-diseaseAssociation": {
         "additionalProperties": false,
         "description": "A gene-disease association is an association between a gene and a disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithAlteredGeneExpression": {
         "additionalProperties": false,
         "description": "A gene-disease association in which the disease phenotype is associated with an altered expression of the gene.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithAlteredGeneExpression",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithCausalMutation": {
         "additionalProperties": false,
         "description": "A gene-variant disease association in which a mutation in the gene/protein results in the development or maintenance of the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithCausalMutation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithChromosomalRearrangement": {
         "additionalProperties": false,
         "description": "A gene-disease association in which the gene is included in a chromosomal rearrangement associated with a particular manifestation of the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithChromosomalRearrangement",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithGeneticVariation": {
         "additionalProperties": false,
         "description": "A gene-disease association in which a sequence variation (a mutation, a SNP) is associated with the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithGeneticVariation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithGenomicAlterations": {
         "additionalProperties": false,
         "description": "a gene-disease association that is linked with some genomic alteration",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithGenomicAlterations",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithGermlineCausalMutation": {
         "additionalProperties": false,
         "description": "A gene-variant disease association in which a germline mutation in the gene/protein results in the development or maintenance of the disease, and it may be passed on to offspring.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithGermlineCausalMutation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithGermlineModifyingMutation": {
         "additionalProperties": false,
         "description": "A gene-variant disease association in which a germline mutation in the gene modifies the clinical presentation of the disease, and it may be passed on to offspring.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithGermlineModifyingMutation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithModifyingMutation": {
         "additionalProperties": false,
         "description": "A gene-disease association in which a gene mutation is known to modify the clinical presentation of the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithModifyingMutation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithPost-translationalModification": {
         "additionalProperties": false,
         "description": "A gene-disease association in which the disease phenotype is associated with post-translational modifications in the protein product.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithPost-translationalModification",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithSomaticCausalMutation": {
         "additionalProperties": false,
         "description": "A gene-variant disease association in which a somatic mutation in the gene/protein results in the development or maintenance of the disease, and it may not be passed on to offspring.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithSomaticCausalMutation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithSomaticModifyingMutation": {
         "additionalProperties": false,
         "description": "A gene-variant disease association in which a somatic mutation in the gene modifies the clinical presentation of the disease, and it may not be passed on to offspring.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithSomaticModifyingMutation",
         "type": "object"
      },
      "Gene-diseaseAssociationLinkedWithSusceptibilityMutation": {
         "additionalProperties": false,
         "description": "A gene-disease association in which a germline mutation in the gene predisposes to the development of the disease, and it is necessary but not sufficient for the manifestation of the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseAssociationLinkedWithSusceptibilityMutation",
         "type": "object"
      },
      "Gene-diseaseBiomarkerAssociation": {
         "additionalProperties": false,
         "description": "A gene-disease association in which the gene/protein is involved in the etiology or maintenance of the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gene-diseaseBiomarkerAssociation",
         "type": "object"
      },
      "GeneComponent": {
         "additionalProperties": false,
         "description": "A gene component is a component of a gene.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeneComponent",
         "type": "object"
      },
      "GeneEnhancer": {
         "additionalProperties": false,
         "description": "A gene enhancer is a short region of DNA that can be bound with proteins to enhance transcription levels of genes in a gene cluster.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeneEnhancer",
         "type": "object"
      },
      "GeneExpressionValue": {
         "additionalProperties": false,
         "description": "A gene expression value is a measured value obtained from a gene expression experiment.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeneExpressionValue",
         "type": "object"
      },
      "GenePromoter": {
         "additionalProperties": false,
         "description": "A gene promoter is a region of DNA that initiates transcription of a particular gene.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GenePromoter",
         "type": "object"
      },
      "GeneRegulatoryComponent": {
         "additionalProperties": false,
         "description": "A gene regulatory component is a gene component that exerts a regulatory function.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeneRegulatoryComponent",
         "type": "object"
      },
      "GeneSymbol": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeneSymbol",
         "type": "object"
      },
      "GenerationNumber": {
         "additionalProperties": false,
         "description": "generation number is a count of the number of biological reproduction events elapsed from some starting reference point.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GenerationNumber",
         "type": "object"
      },
      "GenericName": {
         "additionalProperties": false,
         "description": "A generic name is the preferred name provided by manufacturer.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GenericName",
         "type": "object"
      },
      "GeneticData": {
         "additionalProperties": false,
         "description": "genetic data is data pertaining to genetics.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeneticData",
         "type": "object"
      },
      "GeneticPolymorphism": {
         "additionalProperties": false,
         "description": "genetic polymorphism is the description of a difference in genetic composition at some location.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeneticPolymorphism",
         "type": "object"
      },
      "Genome": {
         "additionalProperties": false,
         "description": "A genome is a collection of nucleic acids.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Genome",
         "type": "object"
      },
      "GenomicSequenceVariant": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GenomicSequenceVariant",
         "type": "object"
      },
      "Genotype": {
         "additionalProperties": false,
         "description": "A genotype is a functional specification of a biological entity in terms of its genetic composition (or lack thereof).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Genotype",
         "type": "object"
      },
      "GeographicHeatmap": {
         "additionalProperties": false,
         "description": "A geographic heatmap is a graphical representation of data over a geographic region where individual values are represented as colors.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeographicHeatmap",
         "type": "object"
      },
      "GeographicImage": {
         "additionalProperties": false,
         "description": "A geographic image is a photograph of some geographical area.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeographicImage",
         "type": "object"
      },
      "GeographicPosition": {
         "additionalProperties": false,
         "description": "A geographic position is the coordinate of an entity against some geographic coordinate system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeographicPosition",
         "type": "object"
      },
      "GeographicRegion": {
         "additionalProperties": false,
         "description": "A geographic region is a spatial region whose boundaries are typically defined against some material frame of reference (like the earth).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeographicRegion",
         "type": "object"
      },
      "GeolegalRegion": {
         "additionalProperties": false,
         "description": "A geolegal region is a geographic region which has causal powers confered by a legal entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeolegalRegion",
         "type": "object"
      },
      "GeometricEntity": {
         "additionalProperties": false,
         "description": "A geometric entity is an information content entity that pertains to the structure and topology of a space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeometricEntity",
         "type": "object"
      },
      "GeopoliticalRegion": {
         "additionalProperties": false,
         "description": "a geopolitical region is a geographic region recognized by social or legal convention.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GeopoliticalRegion",
         "type": "object"
      },
      "GermaniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GermaniumAtom",
         "type": "object"
      },
      "Gesturing": {
         "additionalProperties": false,
         "description": "gesturing is a form of non-verbal communication in which visible bodily actions communicate particular messages, either in place of speech or together and in parallel with spoken words. Gestures include movement of the hands, face, or other parts of the body.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gesturing",
         "type": "object"
      },
      "GlassyCarbon": {
         "additionalProperties": false,
         "description": "glassy carbon is an allotrope of carbon which is widely used as an electrode material in electrochemistry, as well as for high temperature crucibles and as a component of some prosthetic devices.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GlassyCarbon",
         "type": "object"
      },
      "GlobalMaximalStationaryPoint": {
         "additionalProperties": false,
         "description": "A global maximum stationary point is a data point that corresponds to a measurement value is larger than that of all other plotted datapoints.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GlobalMaximalStationaryPoint",
         "type": "object"
      },
      "GlobalMinimalStationaryPoint": {
         "additionalProperties": false,
         "description": "A global minimum data point is a data point that corresponds to a measurement value is smaller than that of all other plotted datapoints.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GlobalMinimalStationaryPoint",
         "type": "object"
      },
      "GoldAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GoldAtom",
         "type": "object"
      },
      "GoodQuality": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GoodQuality",
         "type": "object"
      },
      "GraduateStudentAdvisorRole": {
         "additionalProperties": false,
         "description": "A graduate student advisor role is the role of an individual employed at an academic organization that is involved in advising graduate students.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GraduateStudentAdvisorRole",
         "type": "object"
      },
      "GraphTitle": {
         "additionalProperties": false,
         "description": "A graph title is a title that describes a graph.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "GraphTitle",
         "type": "object"
      },
      "Graphite": {
         "additionalProperties": false,
         "description": "graphite is an allotrope of carbon which is a conductor, and is the most stable form of solid carbon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Graphite",
         "type": "object"
      },
      "Gratitude": {
         "additionalProperties": false,
         "description": "Gratitude, thankfulness, gratefulness, or appreciation is a feeling, emotion or attitude in acknowledgment of a benefit that one has received or will receive.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Gratitude",
         "type": "object"
      },
      "Grief": {
         "additionalProperties": false,
         "description": "grief is an emotion in response to loss, whether physical or abstract including death, unemployment, ill health or the end of a relationship.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Grief",
         "type": "object"
      },
      "Guilt": {
         "additionalProperties": false,
         "description": "Guilt is the emotion borne from feeling responsible for the commission of an offense and arises out of public humiliation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Guilt",
         "type": "object"
      },
      "HafniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HafniumAtom",
         "type": "object"
      },
      "Haplotype": {
         "additionalProperties": false,
         "description": "A haplotype is one of a set of genomic sequence variants.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Haplotype",
         "type": "object"
      },
      "Happiness": {
         "additionalProperties": false,
         "description": "happiness is an emotion characterized by positive or pleasant emotions ranging from contentment to intense joy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Happiness",
         "type": "object"
      },
      "HardDiskDrive": {
         "additionalProperties": false,
         "description": "A hard disk drive (HDD) is a non-volatile, random access device for digital data. It features rotating rigid platters on a motor-driven spindle within a protective enclosure. Data is magnetically read and written on the platter by read/write heads that float on a film of air above the platters.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HardDiskDrive",
         "type": "object"
      },
      "HassiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HassiumAtom",
         "type": "object"
      },
      "Hate": {
         "additionalProperties": false,
         "description": "Hate is a deep and emotional extreme dislike, directed against a certain object or class of objects.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hate",
         "type": "object"
      },
      "Healthy": {
         "additionalProperties": false,
         "description": "healthy is an organismal state of complete physical, mental and social well-being.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Healthy",
         "type": "object"
      },
      "Heatmap": {
         "additionalProperties": false,
         "description": "A heatmap is a graphical representation of data where the individual values contained in a matrix are represented as colors.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Heatmap",
         "type": "object"
      },
      "Height": {
         "additionalProperties": false,
         "description": "height is the one dimensional extent along the vertical projection of a 3D object from a base plane of reference.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Height",
         "type": "object"
      },
      "Helicity": {
         "additionalProperties": false,
         "description": "helicity is the quality of being helical",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Helicity",
         "type": "object"
      },
      "HeliumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HeliumAtom",
         "type": "object"
      },
      "Hemizygous": {
         "additionalProperties": false,
         "description": "hemizygous is the quality of a biological organism in which, based on the ploidy of the organism, there is half the number of alleles than normal at a given locus.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hemizygous",
         "type": "object"
      },
      "Hermaphrodite": {
         "additionalProperties": false,
         "description": "hermaphrodite is a biological sex of an individual with both male and female sexual organs.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hermaphrodite",
         "type": "object"
      },
      "HeterocyclicRing": {
         "additionalProperties": false,
         "description": "A heterocyclic ring is a ring containing a hetero atom.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HeterocyclicRing",
         "type": "object"
      },
      "Heterogeneous": {
         "additionalProperties": false,
         "description": "homogeneous is a quality that describes the varied composition of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Heterogeneous",
         "type": "object"
      },
      "HeterogeneousSubstance": {
         "additionalProperties": false,
         "description": "A heterogeneous substance is a chemical substance that is composed of more than one different kind of component.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HeterogeneousSubstance",
         "type": "object"
      },
      "Heterozygous": {
         "additionalProperties": false,
         "description": "homozygous is the quality of a biological organism in which there are two different alleles at a given locus.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Heterozygous",
         "type": "object"
      },
      "HgvsNotation": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HgvsNotation",
         "type": "object"
      },
      "Histogram": {
         "additionalProperties": false,
         "description": "A histogram is a graphical representation of data which consists of tabular frequencies, shown as adjacent rectangles, over discrete intervals (bins) , with an area equal to the frequency of the observations in the interval.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Histogram",
         "type": "object"
      },
      "History": {
         "additionalProperties": false,
         "description": "history is a sequence of past events.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "History",
         "type": "object"
      },
      "Hole": {
         "additionalProperties": false,
         "description": "A hole is a site that is opening into or through something.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hole",
         "type": "object"
      },
      "HolmiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HolmiumAtom",
         "type": "object"
      },
      "HomePhoneNumber": {
         "additionalProperties": false,
         "description": "a home phone number is the number to connect to an phone at a place of residence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HomePhoneNumber",
         "type": "object"
      },
      "HomocyclicRing": {
         "additionalProperties": false,
         "description": "A homocyclic ring is a ring where the atoms are of a single type.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HomocyclicRing",
         "type": "object"
      },
      "Homogeneous": {
         "additionalProperties": false,
         "description": "homogeneous is a quality that describes the uniform composition of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Homogeneous",
         "type": "object"
      },
      "HomogeneousSubstance": {
         "additionalProperties": false,
         "description": "A homogeneous substance is a substance that is composed of a uniform type of entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HomogeneousSubstance",
         "type": "object"
      },
      "Homonym": {
         "additionalProperties": false,
         "description": "A homonym is a word that sounds the same but has different meaning.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Homonym",
         "type": "object"
      },
      "Homozygous": {
         "additionalProperties": false,
         "description": "homozygous is the quality of a biological organism in which there are two identical alleles at a given locus.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Homozygous",
         "type": "object"
      },
      "Honor%27sThesis": {
         "additionalProperties": false,
         "description": "An honor's thesis is a thesis prepared as a requirement for an honor's undergraduate degree.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Honor%27sThesis",
         "type": "object"
      },
      "Hope": {
         "additionalProperties": false,
         "description": "hope is an emotion of belief in a positive outcome related to events and circumstances in one's life.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hope",
         "type": "object"
      },
      "HorizontalBarGraph": {
         "additionalProperties": false,
         "description": "A horizontal bar graph is a bar graph in which the rectangular bars are horizontally oriented in space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HorizontalBarGraph",
         "type": "object"
      },
      "HorizontalLine": {
         "additionalProperties": false,
         "description": "A horizontal line is a line that is positionally oriented with the horizon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HorizontalLine",
         "type": "object"
      },
      "Host": {
         "additionalProperties": false,
         "description": "A host is an organism that harbors a parasite, or a mutual or commensal symbiont, typically providing nourishment and shelter.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Host",
         "type": "object"
      },
      "HostRole": {
         "additionalProperties": false,
         "description": "The role of an organism in providing resources to maintain the survival and/or reproduction of another organism.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HostRole",
         "type": "object"
      },
      "Hostility": {
         "additionalProperties": false,
         "description": "Hostility is the intense negative emotion of being in conflict or opposition to someone or something.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hostility",
         "type": "object"
      },
      "Hour": {
         "additionalProperties": false,
         "description": "An hour is a period of 60 minutes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hour",
         "type": "object"
      },
      "Human": {
         "additionalProperties": false,
         "description": "A human is a primates of the family Hominidae and are characterized by having a large brain relative to body size, with a well developed neocortex, prefrontal cortex and temporal lobes, making them capable of abstract reasoning, language, introspection, problem solving and culture through social learning.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Human",
         "type": "object"
      },
      "HumanPopulation": {
         "additionalProperties": false,
         "description": "A human population refers to a collection of human beings.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HumanPopulation",
         "type": "object"
      },
      "Humiliation": {
         "additionalProperties": false,
         "description": "Humiliation is the abasement of pride, which creates mortification or leads to a state of being humbled or reduced to lowliness or submission.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Humiliation",
         "type": "object"
      },
      "Hunger": {
         "additionalProperties": false,
         "description": "hunger is the craving for food.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hunger",
         "type": "object"
      },
      "Hurt": {
         "additionalProperties": false,
         "description": "hurt is an unpleasant feeling, emotion or sensation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hurt",
         "type": "object"
      },
      "HydrogenAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HydrogenAtom",
         "type": "object"
      },
      "HydrogenBond": {
         "additionalProperties": false,
         "description": "A hydrogen bond is a weak submolecular interaction formed between a hydrogen atom and a electronegative atom.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "HydrogenBond",
         "type": "object"
      },
      "Hypernym": {
         "additionalProperties": false,
         "description": "A hypernym is a term with a broader meaning.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hypernym",
         "type": "object"
      },
      "Hyponym": {
         "additionalProperties": false,
         "description": "A hyponym is a term with a narrower meaning.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hyponym",
         "type": "object"
      },
      "Hypothesis": {
         "additionalProperties": false,
         "description": "A hypothesis is a proposed explanation for a phenomenon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hypothesis",
         "type": "object"
      },
      "Hypothetical": {
         "additionalProperties": false,
         "description": "hypothetical is the quality of an entity that is conjectured to exist.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hypothetical",
         "type": "object"
      },
      "Hysteria": {
         "additionalProperties": false,
         "description": "Hysteria is an unmanageable emotion.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Hysteria",
         "type": "object"
      },
      "IPNumber": {
         "additionalProperties": false,
         "description": "an IP number is an number to connect to a device on the internet.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IPNumber",
         "type": "object"
      },
      "Idea": {
         "additionalProperties": false,
         "description": "An idea is a proposition about some object of conceptual thought.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Idea",
         "type": "object"
      },
      "Identifier": {
         "additionalProperties": false,
         "description": "An identifier is a label that specifically refers to (identifies) an entity (instance/type).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Identifier",
         "type": "object"
      },
      "Image": {
         "additionalProperties": false,
         "description": "An image is an affine projection of a visual entity to a two dimensional surface.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Image",
         "type": "object"
      },
      "Implies-%3E": {
         "additionalProperties": false,
         "description": "Implication is a logical operator that holds between a set T of propositions and a proposition B, when every model (or interpretation or valuation) of T is also a model of B.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Implies-%3E",
         "type": "object"
      },
      "InCHINotation": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InCHINotation",
         "type": "object"
      },
      "InactiveIngredient": {
         "additionalProperties": false,
         "description": "Aninactive ingredient is a molecular entity that does not exhibit biological activity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InactiveIngredient",
         "type": "object"
      },
      "InclusionCriterion": {
         "additionalProperties": false,
         "description": "An inclusion criterion is a criterion that must be present to satisfy some objective.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InclusionCriterion",
         "type": "object"
      },
      "IncreaseInNumberOfObjectsProduced": {
         "additionalProperties": false,
         "description": "An increase in the number of objects produced is the positive value of a difference in number of objects produced.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreaseInNumberOfObjectsProduced",
         "type": "object"
      },
      "Increased": {
         "additionalProperties": false,
         "description": "A quality that has a value that is increased compared to normal or average.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Increased",
         "type": "object"
      },
      "IncreasedChemicalDestruction": {
         "additionalProperties": false,
         "description": "increased chemical destruction is a process in which there is an increase in the amount of chemical destroyed relative to some reference process",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedChemicalDestruction",
         "type": "object"
      },
      "IncreasedDurationOfProcessFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "increased duration of process from increased regulation is a process in which the duration of the target process is increased as a result of increased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedDurationOfProcessFromIncreasedRegulation",
         "type": "object"
      },
      "IncreasedFrequencyOfProcessFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "The increase of regulation leads to a increased occurence of processes of the target type.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedFrequencyOfProcessFromIncreasedRegulation",
         "type": "object"
      },
      "IncreasedMolecularDegradationFromDecreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that increases the frequency, rate or extent of process involved in the destruction of a molecule as a result of decreased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedMolecularDegradationFromDecreasedRegulation",
         "type": "object"
      },
      "IncreasedMolecularDegradationFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that increases the frequency, rate or extent of process involved in the destruction of a molecule as a result of increased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedMolecularDegradationFromIncreasedRegulation",
         "type": "object"
      },
      "IncreasedMolecularProductionFromDecreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that increases the frequency, rate or extent of process involved in the production of a molecule as a result of decreased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedMolecularProductionFromDecreasedRegulation",
         "type": "object"
      },
      "IncreasedMolecularProductionFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "A process that increases the frequency, rate or extent of process involved in the production of a molecule as a result of increased regulation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedMolecularProductionFromIncreasedRegulation",
         "type": "object"
      },
      "IncreasedObjectConsumptionFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "increased regulation leads to an increase in the consumption of an object of specified type.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedObjectConsumptionFromIncreasedRegulation",
         "type": "object"
      },
      "IncreasedObjectProductionFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "increased regulation leads to an increase in the number of target objects of a specified type.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedObjectProductionFromIncreasedRegulation",
         "type": "object"
      },
      "IncreasedSpatialExtentOfProcessFromIncreasedRegulation": {
         "additionalProperties": false,
         "description": "The increase of regulation leads to a increased spatial extent of the target process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasedSpatialExtentOfProcessFromIncreasedRegulation",
         "type": "object"
      },
      "IncreasingLine": {
         "additionalProperties": false,
         "description": "An increasing line is a line segment in which the startpoint and endpoint are ordered along one dimension and the difference of values in a second dimension is positive.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IncreasingLine",
         "type": "object"
      },
      "Indecision": {
         "additionalProperties": false,
         "description": "indecision is the emotion of being unable to choose between two or more possible courses of action.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Indecision",
         "type": "object"
      },
      "IndependentVariable": {
         "additionalProperties": false,
         "description": "An independent variable is a variable that may take on different values independent of other elements in a system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IndependentVariable",
         "type": "object"
      },
      "Indifference": {
         "additionalProperties": false,
         "description": "indifference is an emotion characterized by lack of interest, concern, or sympathy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Indifference",
         "type": "object"
      },
      "IndiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IndiumAtom",
         "type": "object"
      },
      "InductiveArgument": {
         "additionalProperties": false,
         "description": "An inductive argument is an argument that asserts that the truth of the conclusion is supported by the premises.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InductiveArgument",
         "type": "object"
      },
      "InfiniteLine": {
         "additionalProperties": false,
         "description": "An infinite line is a line that extends outwards in both directions of a single dimensional and is not bounded by terminal points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InfiniteLine",
         "type": "object"
      },
      "InformationContentEntity": {
         "additionalProperties": false,
         "description": "information content entity is an object that requires some background knowledge or procedure to correctly interpret.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationContentEntity",
         "type": "object"
      },
      "InformationDecoding": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationDecoding",
         "type": "object"
      },
      "InformationEncoding": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationEncoding",
         "type": "object"
      },
      "InformationMaintenance": {
         "additionalProperties": false,
         "description": "the regulation or maintenance of information",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationMaintenance",
         "type": "object"
      },
      "InformationProcessing": {
         "additionalProperties": false,
         "description": "information processing is a process that involves the generation or use of information.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationProcessing",
         "type": "object"
      },
      "InformationTranslation": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationTranslation",
         "type": "object"
      },
      "InformationalEntityIdentifier": {
         "additionalProperties": false,
         "description": "An informational entity identifier is an identifier for an informational entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationalEntityIdentifier",
         "type": "object"
      },
      "InformationalQuality": {
         "additionalProperties": false,
         "description": "An informational quality is a quality that pertains to information.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InformationalQuality",
         "type": "object"
      },
      "Ingredient": {
         "additionalProperties": false,
         "description": "an ingredient is a chemical substance that forms part of a mixture.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ingredient",
         "type": "object"
      },
      "Inhibitor": {
         "additionalProperties": false,
         "description": "A molecular inhibitor is a molecular regulator that realizes its disposition to conformationally change a target molecule and decrease its functionality.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Inhibitor",
         "type": "object"
      },
      "InhibitorRole": {
         "additionalProperties": false,
         "description": "The role of a chemical entity that reduces the rate of reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InhibitorRole",
         "type": "object"
      },
      "InorganicReaction": {
         "additionalProperties": false,
         "description": "An inorganic reaction is a chemical reaction that involves the transformation of inorganic molecules.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InorganicReaction",
         "type": "object"
      },
      "InsertionVariantRole": {
         "additionalProperties": false,
         "description": "An insertion variant role is the role of an sequence that contains a sub-sequence that is considered to be an addition relative to the frame of reference.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InsertionVariantRole",
         "type": "object"
      },
      "Institute": {
         "additionalProperties": false,
         "description": "institute is a society or organization having a object or common factor, and is normally applied to those with a scientific, educational, or social objective.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Institute",
         "type": "object"
      },
      "IntegerScale": {
         "additionalProperties": false,
         "description": "An integer scale of measurement is one that only specifies integer values.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IntegerScale",
         "type": "object"
      },
      "Intensity": {
         "additionalProperties": false,
         "description": "intensity is a quality that represents the strength or degree of something.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Intensity",
         "type": "object"
      },
      "Intent": {
         "additionalProperties": false,
         "description": "intent is a desire to realize a particular outcome.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Intent",
         "type": "object"
      },
      "Interacting": {
         "additionalProperties": false,
         "description": "interacting is a process characterized by the interaction between two or more entities.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Interacting",
         "type": "object"
      },
      "Interest": {
         "additionalProperties": false,
         "description": "interest is the emotion of wanting to know or learn about something or someone.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Interest",
         "type": "object"
      },
      "Intersection": {
         "additionalProperties": false,
         "description": "An intersection is a list of only the values of an attribute for the entities in the defined set where all entities have that value.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Intersection",
         "type": "object"
      },
      "Intersex": {
         "additionalProperties": false,
         "description": "intersex is a biological sex characterised by birth with genitalia and/or secondary sexual characteristics of indeterminate sex, or which combine features of both sexes.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Intersex",
         "type": "object"
      },
      "Interval": {
         "additionalProperties": false,
         "description": "an interval is a set of real numbers with the property that any number that lies between two numbers in the set is also included in the set.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Interval",
         "type": "object"
      },
      "InterventionGroup": {
         "additionalProperties": false,
         "description": "An intervention group is a group of individuals that are subject to an intervention.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InterventionGroup",
         "type": "object"
      },
      "InterventionStudy": {
         "additionalProperties": false,
         "description": "An intervention study has the objective of improving the condition of an individual or a group of individuals, and demonstrates the magnitude of that capability by comparing it to a control group.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InterventionStudy",
         "type": "object"
      },
      "IntroductionSection": {
         "additionalProperties": false,
         "description": "An introduction section is a document section that generally provides background, motivation and goals of the work.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IntroductionSection",
         "type": "object"
      },
      "Intron": {
         "additionalProperties": false,
         "description": "An intron is a region of a gene that is removed from the final protein open reading frame.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Intron",
         "type": "object"
      },
      "InvalidArgument": {
         "additionalProperties": false,
         "description": "An invalid argument is an argument where the truth of the conclusion is false because it is not a logical consequence of the premises.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InvalidArgument",
         "type": "object"
      },
      "Investigation": {
         "additionalProperties": false,
         "description": "investigation is the process of carrying out a plan or procedure so as to discover facts or information about the object of study.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Investigation",
         "type": "object"
      },
      "InvestigationalRole": {
         "additionalProperties": false,
         "description": "An investigational role is a role held by participants involved in an investigation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "InvestigationalRole",
         "type": "object"
      },
      "IodineAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IodineAtom",
         "type": "object"
      },
      "Ion": {
         "additionalProperties": false,
         "description": "An ion is an atom or molecule in which the total number of electrons is not equal to the total number of protons, giving it a net positive or negative electrical charge.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ion",
         "type": "object"
      },
      "IonicCompound": {
         "additionalProperties": false,
         "description": "An ionic compound is a mereological maximal sum of weakly connected paired positive and negative ions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IonicCompound",
         "type": "object"
      },
      "IonicInteraction": {
         "additionalProperties": false,
         "description": "An ionic interaction is a weak submolecular interaction between a charged submolecules.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IonicInteraction",
         "type": "object"
      },
      "Ionsdaleite": {
         "additionalProperties": false,
         "description": "ionsdaleite is a hexagonal allotrope of the carbon allotrope diamond.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ionsdaleite",
         "type": "object"
      },
      "IridiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IridiumAtom",
         "type": "object"
      },
      "IronAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IronAtom",
         "type": "object"
      },
      "Irritability": {
         "additionalProperties": false,
         "description": "irritability is the negative emotion of quick excitability to annoyance, impatience, or anger.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Irritability",
         "type": "object"
      },
      "Isomer": {
         "additionalProperties": false,
         "description": "An isomer is a molecule that is compositionally identical to another molecule as a result of a different atomic connectivity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Isomer",
         "type": "object"
      },
      "IsomerizationReaction": {
         "additionalProperties": false,
         "description": "An isomerization reaction is a chemical reaction in which a molecule is converted into its isomer.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "IsomerizationReaction",
         "type": "object"
      },
      "Issue": {
         "additionalProperties": false,
         "description": "An issue is a single instance of a periodically published journal, magazine, or newspaper.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Issue",
         "type": "object"
      },
      "Jealousy": {
         "additionalProperties": false,
         "description": "jealousy is an emotion and typically refers to the negative thoughts and feelings of insecurity, fear, and anxiety over an anticipated loss of something that the person values, particularly in reference to a human connection.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Jealousy",
         "type": "object"
      },
      "Journal": {
         "additionalProperties": false,
         "description": "A journal is a a peer-reviewed periodical in which scholarship relating to a particular academic discipline is published.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Journal",
         "type": "object"
      },
      "Joy": {
         "additionalProperties": false,
         "description": "joy is an emotion of intense happiness.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Joy",
         "type": "object"
      },
      "Justification": {
         "additionalProperties": false,
         "description": "A justification is a proposition that defends, explains or excuses some argument.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Justification",
         "type": "object"
      },
      "Keyword": {
         "additionalProperties": false,
         "description": "A keyword is a descriptor in which the association of the word with the entity facilitates information retrieval.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Keyword",
         "type": "object"
      },
      "KryptonAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "KryptonAtom",
         "type": "object"
      },
      "Label": {
         "additionalProperties": false,
         "description": "a label is a term that is associated with some entity",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Label",
         "type": "object"
      },
      "Language": {
         "additionalProperties": false,
         "description": "Language is a language entity which is the result of encoding and decoding information through systematic creation and usage of systems of symbols, each pairing a specific sign with an intended meaning, established through social conventions",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Language",
         "type": "object"
      },
      "LanguageEntity": {
         "additionalProperties": false,
         "description": "A language entity implements some language specification for the visual interpretation and is part of some document.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LanguageEntity",
         "type": "object"
      },
      "LanguageLabel": {
         "additionalProperties": false,
         "description": "A language label is a label that denotes the language of a textual entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LanguageLabel",
         "type": "object"
      },
      "LanthanumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LanthanumAtom",
         "type": "object"
      },
      "LastName": {
         "additionalProperties": false,
         "description": "A last name (surname) is a name added to a given name and is part of a personal name and is often the family name.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LastName",
         "type": "object"
      },
      "Latitude": {
         "additionalProperties": false,
         "description": "Latitude is a geographic coordinate which refers to the angle from a point on the Earth's surface to the equatorial plane",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Latitude",
         "type": "object"
      },
      "LawrenciumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LawrenciumAtom",
         "type": "object"
      },
      "LeadAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LeadAtom",
         "type": "object"
      },
      "Learning": {
         "additionalProperties": false,
         "description": "learning is the agentive process of acquiring knowledge.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Learning",
         "type": "object"
      },
      "Left-handedHelical": {
         "additionalProperties": false,
         "description": "left-handed helical quality is the quality in which a clockwise screwing motion moves the helix towards the observed along the line of sight along the helix's axis.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Left-handedHelical",
         "type": "object"
      },
      "LeftClosedInterval": {
         "additionalProperties": false,
         "description": "a left closed interval is an interval in which there is a real number that is smaller than all its elements.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LeftClosedInterval",
         "type": "object"
      },
      "LeftOpenInterval": {
         "additionalProperties": false,
         "description": "a left open interval is an interval in which there is no element that is smaller than all other elements.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LeftOpenInterval",
         "type": "object"
      },
      "LeftValueAxis": {
         "additionalProperties": false,
         "description": "A left value axis is a value axis that is spatially positioned to the left of the plot area.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LeftValueAxis",
         "type": "object"
      },
      "LegalDocument": {
         "additionalProperties": false,
         "description": "A legal document is a formally executed written document that can be formally attributed to its author, records and formally expresses a legally enforceable act, process, or contractual duty, obligation, or right, and therefore evidences that act, process, or agreement.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LegalDocument",
         "type": "object"
      },
      "LegalName": {
         "additionalProperties": false,
         "description": "A legal name is a name given at birth, or which appears on their birth certificate, marriage certificate, or change of name certificate.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LegalName",
         "type": "object"
      },
      "Legend": {
         "additionalProperties": false,
         "description": "A legend is a part of a figure that associates textual descriptions with symbols pertaining to plotted entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Legend",
         "type": "object"
      },
      "Legislation": {
         "additionalProperties": false,
         "description": "A legal document proposing or enacting a law or a group of laws.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Legislation",
         "type": "object"
      },
      "Length": {
         "additionalProperties": false,
         "description": "length is the longer dimensional extent along a 2D projection of the object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Length",
         "type": "object"
      },
      "LengthOfPerimeter": {
         "additionalProperties": false,
         "description": "A perimeter is a length of the outline that surrounds a two-dimensional shape.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LengthOfPerimeter",
         "type": "object"
      },
      "Letter": {
         "additionalProperties": false,
         "description": "A letter is a document that contains a personal communication from one part to another.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Letter",
         "type": "object"
      },
      "LifeStatus": {
         "additionalProperties": false,
         "description": "life status is the quality of whether something is alive or dead.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LifeStatus",
         "type": "object"
      },
      "Ligand": {
         "additionalProperties": false,
         "description": "A ligand is a molecule that is part of a complex by weakly interacting with another molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ligand",
         "type": "object"
      },
      "Likelihood": {
         "additionalProperties": false,
         "description": "Likelihood is the hypothetical probability that an event that has already occurred would yield a specific outcome.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Likelihood",
         "type": "object"
      },
      "Line": {
         "additionalProperties": false,
         "description": "A line is curve that extends in a single dimension (e.g. straight line; exhibits no curvature), and is composed of at least two fully connected points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Line",
         "type": "object"
      },
      "Line-barGraph": {
         "additionalProperties": false,
         "description": "A line-bar graph statistical graph that contains both lines and bars.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Line-barGraph",
         "type": "object"
      },
      "LineGraph": {
         "additionalProperties": false,
         "description": "A line graph is a statistical graph in which lines contains the evaluation of functions or individual points connected by line segments.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LineGraph",
         "type": "object"
      },
      "LineSegment": {
         "additionalProperties": false,
         "description": "A line segment is a line and a part of a curve that is (inclusively) bounded by two terminal points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LineSegment",
         "type": "object"
      },
      "LinearPosition": {
         "additionalProperties": false,
         "description": "A linear position is the position of some object against a linear positioning system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LinearPosition",
         "type": "object"
      },
      "LinearValueAxis": {
         "additionalProperties": false,
         "description": "A linear value axis is a value axis that corresponds to a scaling factor of 1 of the value range.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LinearValueAxis",
         "type": "object"
      },
      "Lipid": {
         "additionalProperties": false,
         "description": "A lipid is a water-insoluable organic molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Lipid",
         "type": "object"
      },
      "LipidResidue": {
         "additionalProperties": false,
         "description": "A lipid residue is a part of an organic molecule that was derived from a lipid molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LipidResidue",
         "type": "object"
      },
      "LiquidSolution": {
         "additionalProperties": false,
         "description": "A liquid solution is a heterogeneous substance in a liquid state.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LiquidSolution",
         "type": "object"
      },
      "LiquidSolutionComponent": {
         "additionalProperties": false,
         "description": "A liquid solution component is a part of a liquid solution.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LiquidSolutionComponent",
         "type": "object"
      },
      "List": {
         "additionalProperties": false,
         "description": "A list is any enumeration of a set of items.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "List",
         "type": "object"
      },
      "ListItem": {
         "additionalProperties": false,
         "description": "a list item is an item in a list.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ListItem",
         "type": "object"
      },
      "LiteratureCuration": {
         "additionalProperties": false,
         "description": "literature curation is the process of an agent selecting and extracting terms and phrases from a document.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LiteratureCuration",
         "type": "object"
      },
      "LithiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LithiumAtom",
         "type": "object"
      },
      "Loathing": {
         "additionalProperties": false,
         "description": "loathing is an intense dislike or disgust.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Loathing",
         "type": "object"
      },
      "LocalMaximumStationaryPoint": {
         "additionalProperties": false,
         "description": "A local maximum stationary point is a point that has a higher value in  some axis than adjacent points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LocalMaximumStationaryPoint",
         "type": "object"
      },
      "LocalMinimumStationaryPoint": {
         "additionalProperties": false,
         "description": "A local minimum stationary point is a point that has a lower value in  some axis than adjacent points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LocalMinimumStationaryPoint",
         "type": "object"
      },
      "Locomotion": {
         "additionalProperties": false,
         "description": "The self-propelled movement of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Locomotion",
         "type": "object"
      },
      "LogLikelihood": {
         "additionalProperties": false,
         "description": "Log likelihood is the natural logarithm of the likelihood function",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LogLikelihood",
         "type": "object"
      },
      "LogarithmicValueAxis": {
         "additionalProperties": false,
         "description": "A logarithmic value axis is a scaled value axis that corresponds to a scaling factor of the logarithm of the value range.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LogarithmicValueAxis",
         "type": "object"
      },
      "LogicalOperator": {
         "additionalProperties": false,
         "description": "A logical operator is a unary or binary relation to construct logical expressions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LogicalOperator",
         "type": "object"
      },
      "Loneliness": {
         "additionalProperties": false,
         "description": "Loneliness is an unpleasant emotion in which a person feels a strong sense of emptiness, yearning distress and solitude resulting from inadequate quantity or quality of social relationships.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Loneliness",
         "type": "object"
      },
      "Longitude": {
         "additionalProperties": false,
         "description": "Longitude is a geographic position that refers to  the angle east or west of a reference meridian between the two geographical poles to another meridian that passes through an arbitrary point.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Longitude",
         "type": "object"
      },
      "Love": {
         "additionalProperties": false,
         "description": "love is an emotion of a strong affection and personal attachment.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Love",
         "type": "object"
      },
      "LowBarrierHydrogenBond": {
         "additionalProperties": false,
         "description": "A low barrier hydrogen bond is a shorter, stronger hydrogen bond that is formed between both heteroatoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LowBarrierHydrogenBond",
         "type": "object"
      },
      "Lust": {
         "additionalProperties": false,
         "description": "lust is the strong desire for sex.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Lust",
         "type": "object"
      },
      "LutetiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "LutetiumAtom",
         "type": "object"
      },
      "MRNASpliceVariant": {
         "additionalProperties": false,
         "description": "An mRNA splice variant is an mRNA molecule that varies from another mRNA molecule of the same gene origin but having a different final sequence due to differences in its assembly from splice sites.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MRNASpliceVariant",
         "type": "object"
      },
      "Magazine": {
         "additionalProperties": false,
         "description": "A magazine is a periodical that typically contains essays, stories, poems, etc., by many writers, and often photographs and drawings, frequently specializing in a particular subject or area, as hobbies, news, or sports.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Magazine",
         "type": "object"
      },
      "MagnesiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MagnesiumAtom",
         "type": "object"
      },
      "MaintenanceOfDurationOfProcess": {
         "additionalProperties": false,
         "description": "maintenance of duration of process is a process that regulates a target process to maintain its duration within an expected interval.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaintenanceOfDurationOfProcess",
         "type": "object"
      },
      "MaintenanceOfFrequencyOfProcess": {
         "additionalProperties": false,
         "description": "maintenance of frequency of process is a process that regulates the number of occurences of a target process type to a specified number or interval.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaintenanceOfFrequencyOfProcess",
         "type": "object"
      },
      "MaintenanceOfLevelOfObjectConsumption": {
         "additionalProperties": false,
         "description": "regulation of a participant quantity is the regulation of a process in which the quantity of a selected participant is maintained at a steady level.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaintenanceOfLevelOfObjectConsumption",
         "type": "object"
      },
      "MaintenanceOfQuantityOfObjectProduction": {
         "additionalProperties": false,
         "description": "maintenance of quantity of object production is a regulation of object production in which the number of objects produced is held more or less constant.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaintenanceOfQuantityOfObjectProduction",
         "type": "object"
      },
      "MaintenanceOfSpatialExtentOfProcess": {
         "additionalProperties": false,
         "description": "maintenance of spatial extent of process is a regulation of a process' spatial extent within some specified parameter.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaintenanceOfSpatialExtentOfProcess",
         "type": "object"
      },
      "MajorTickMark": {
         "additionalProperties": false,
         "description": "A major tick mark is a tick mark that indicates the position of a specific numeric value and is adjacent to its value label on the value axis, or is one of a pair of tick marks that delineates the boundary of a categorical value indicated by an adjacent category label on the categorical axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MajorTickMark",
         "type": "object"
      },
      "MajorVersionNumber": {
         "additionalProperties": false,
         "description": "A major version number is a version of a software that exhibits a significant change in functionalilty from a prior version.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MajorVersionNumber",
         "type": "object"
      },
      "Male": {
         "additionalProperties": false,
         "description": "male is a biological sex of an individual with male sexual organs.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Male",
         "type": "object"
      },
      "ManganeseAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ManganeseAtom",
         "type": "object"
      },
      "Manual": {
         "additionalProperties": false,
         "description": "A manual is a document that instructs on the usage of a device.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Manual",
         "type": "object"
      },
      "Manuscript": {
         "additionalProperties": false,
         "description": "A manuscript is a document prior to publication.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Manuscript",
         "type": "object"
      },
      "Map": {
         "additionalProperties": false,
         "description": "A map is a a visual representation of an area that depicts the relationship between elements of that space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Map",
         "type": "object"
      },
      "Mass": {
         "additionalProperties": false,
         "description": "mass is the quality of the amount of substance.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Mass",
         "type": "object"
      },
      "MassSpectrometer": {
         "additionalProperties": false,
         "description": "A mass spectrometer is a device that identifies ions based on their mass to charge ratio using an electromagnetic field.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MassSpectrometer",
         "type": "object"
      },
      "MassSpectrometryExperiment": {
         "additionalProperties": false,
         "description": "A mass spectrometry experiment is an experiment that involves the use of a mass spectrometer.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MassSpectrometryExperiment",
         "type": "object"
      },
      "Master%27sThesis": {
         "additionalProperties": false,
         "description": "A Master's thesis is a thesis prepared as a requirement for a Master's degree.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Master%27sThesis",
         "type": "object"
      },
      "MaterialBoundary": {
         "additionalProperties": false,
         "description": "A material boundary is the boundary of a material entity which exists as a lower dimensional entity at exactly the location where its parts no longer extend into space. Every material entity has a boundary, and a boundary is the boundary of exactly 1 material entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaterialBoundary",
         "type": "object"
      },
      "MaterialEntity": {
         "additionalProperties": false,
         "description": "A material entity is a physical entity that is spatially extended, exists as a whole at any point in time and has mass.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaterialEntity",
         "type": "object"
      },
      "MaterialsAndMethodsSection": {
         "additionalProperties": false,
         "description": "The materials and methods section is a document section containing a description of the materials and methods used in the study.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaterialsAndMethodsSection",
         "type": "object"
      },
      "MaterialsSection": {
         "additionalProperties": false,
         "description": "The materials section is a document section containing a description of the materials used in the study.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaterialsSection",
         "type": "object"
      },
      "MaternallyTransmitted": {
         "additionalProperties": false,
         "description": "maternally transmitted is the quality of a biological organism in which the (genetic) material is transmitted from the maternal line.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaternallyTransmitted",
         "type": "object"
      },
      "MathematicalEntity": {
         "additionalProperties": false,
         "description": "A mathematical entity is an information content entity that are components of a mathematical system or can be defined in mathematical terms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MathematicalEntity",
         "type": "object"
      },
      "Matrix": {
         "additionalProperties": false,
         "description": "a matrix is a rank 2 tensor. It is represented as a rectangular array or table of numbers, symbols, or expressions arranged in rows and columns.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Matrix",
         "type": "object"
      },
      "MatrixChart": {
         "additionalProperties": false,
         "description": "A matrix chart summarizes a multidimensional data set in a grid.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MatrixChart",
         "type": "object"
      },
      "MatureMRNA": {
         "additionalProperties": false,
         "description": "A mature RNA is a ribonucleic acid that contains an untranslated region (UTR) and protein coding sequence and lacks introns.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MatureMRNA",
         "type": "object"
      },
      "MaximalValue": {
         "additionalProperties": false,
         "description": "A maximal value is largest value of an attribute for the entities in the defined set.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MaximalValue",
         "type": "object"
      },
      "Mean": {
         "additionalProperties": false,
         "description": "A mean is the central tendency of a collection of numbers taken as the sum of the numbers divided by the size of the collection.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Mean",
         "type": "object"
      },
      "MeasurementScale": {
         "additionalProperties": false,
         "description": "A measurement scale is a functional specification that specifies an allowed range of categories or values.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MeasurementScale",
         "type": "object"
      },
      "MeasurementValue": {
         "additionalProperties": false,
         "description": "A measurement value is a quantitative description that reflects the magnitude of some attribute.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MeasurementValue",
         "type": "object"
      },
      "Measuring": {
         "additionalProperties": false,
         "description": "measuring is the process of determining the size, amount, or degree of (something) by using an instrument or device marked in standard units",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Measuring",
         "type": "object"
      },
      "Media": {
         "additionalProperties": false,
         "description": "media are audo/visual/audiovisual modes of communicating information for mass consumption.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Media",
         "type": "object"
      },
      "Median": {
         "additionalProperties": false,
         "description": "A median is the numerical value separating the higher half of a sample, a population, or a probability distribution, from the lower half.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Median",
         "type": "object"
      },
      "MedicalData": {
         "additionalProperties": false,
         "description": "medical data is data of interest to medicine.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalData",
         "type": "object"
      },
      "MedicalDiagnosis": {
         "additionalProperties": false,
         "description": "A medical diagnosis (often simply termed diagnosis) refers to the process of attempting to determine or identify a possible disease or disorder.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalDiagnosis",
         "type": "object"
      },
      "MedicalHealthRecord": {
         "additionalProperties": false,
         "description": "A medical health record is a record of a single patient's medical history.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalHealthRecord",
         "type": "object"
      },
      "MedicalHistory": {
         "additionalProperties": false,
         "description": "A medical history is a record of the events of a recipient of medical care.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalHistory",
         "type": "object"
      },
      "MedicalIntervention": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalIntervention",
         "type": "object"
      },
      "MedicalPractitioner": {
         "additionalProperties": false,
         "description": "A medical practioner is an individual that provides medical care.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalPractitioner",
         "type": "object"
      },
      "MedicalProcedure": {
         "additionalProperties": false,
         "description": "A medical procedure is a procedure to identify, examine, alleviate or eliminate an undesirable biological disease or disorder.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalProcedure",
         "type": "object"
      },
      "MedicalReport": {
         "additionalProperties": false,
         "description": "A medical report is a report prepared by a health care practioner about test outcomes or health status of an individual.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalReport",
         "type": "object"
      },
      "MedicalRole": {
         "additionalProperties": false,
         "description": "A medical role is the role of an individual that is a participant in the delivery of medical care.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalRole",
         "type": "object"
      },
      "MedicalScreening": {
         "additionalProperties": false,
         "description": "A medical screening is a medical test or series used to detect or predict the presence of disease in individuals at risk for disease within a defined group, such as a population, family, or workforce",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MedicalScreening",
         "type": "object"
      },
      "MeitneriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MeitneriumAtom",
         "type": "object"
      },
      "MemberCount": {
         "additionalProperties": false,
         "description": "A count of the instances of a class or members in a collection.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MemberCount",
         "type": "object"
      },
      "MembraneTransport": {
         "additionalProperties": false,
         "description": "membrane transport is the movement of molecules across a membrane.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MembraneTransport",
         "type": "object"
      },
      "MendeleviumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MendeleviumAtom",
         "type": "object"
      },
      "MercuryAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MercuryAtom",
         "type": "object"
      },
      "MereologicalChart": {
         "additionalProperties": false,
         "description": "A mereological chart is a chart that illustrates the parts in the context of the whole.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MereologicalChart",
         "type": "object"
      },
      "MereologicalQuality": {
         "additionalProperties": false,
         "description": "a mereological quality is a quality of an entity vis-a-vis containment or parthood",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MereologicalQuality",
         "type": "object"
      },
      "Messenger": {
         "additionalProperties": false,
         "description": "A mesenger is a molecule involved in either signal detection or signal propagation from receptors on the cell surface to target molecules inside the cell, in the cytoplasm or nucleus.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Messenger",
         "type": "object"
      },
      "MessengerRNA": {
         "additionalProperties": false,
         "description": "A messenger RNA is a ribonucleic acid that contains an untranslated region (UTR) and protein coding sequence and lacks introns.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MessengerRNA",
         "type": "object"
      },
      "MetabolicPathway": {
         "additionalProperties": false,
         "description": "A metabolic pathway is a series of biochemical reactions that begins with one or more substrates and ends with one or more products.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MetabolicPathway",
         "type": "object"
      },
      "Metabolism": {
         "additionalProperties": false,
         "description": "Metabolism is the set of chemical processes that occur within a living organism in order to maintain life.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Metabolism",
         "type": "object"
      },
      "Metadata": {
         "additionalProperties": false,
         "description": "metadata is data that provides information about data.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Metadata",
         "type": "object"
      },
      "MethodsSection": {
         "additionalProperties": false,
         "description": "The methods section is a document section containing a description of the methods used in the study.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MethodsSection",
         "type": "object"
      },
      "MicroRNAmiRNA": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MicroRNAmiRNA",
         "type": "object"
      },
      "MicroarrayDevice": {
         "additionalProperties": false,
         "description": "A microarray device is a device that identifies the binding of a target substance to a physically immobile substrate placed in an array or lattice.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MicroarrayDevice",
         "type": "object"
      },
      "MicroarrayExperiment": {
         "additionalProperties": false,
         "description": "A microarray experiment is an experiment that involves a microarray device to measure the expression of one or more genes.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MicroarrayExperiment",
         "type": "object"
      },
      "MicroarrayProbeSetIdentifier": {
         "additionalProperties": false,
         "description": "A microarray probe set identifier is an identifier for a set of probe pairs selected to represent expressed sequences on an array.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MicroarrayProbeSetIdentifier",
         "type": "object"
      },
      "MiddleInitial": {
         "additionalProperties": false,
         "description": "a middle initial is an abbreviated middle name.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MiddleInitial",
         "type": "object"
      },
      "MiddleName": {
         "additionalProperties": false,
         "description": "a middle name is a name assigned to an individual that is not the first or last name.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MiddleName",
         "type": "object"
      },
      "Mild": {
         "additionalProperties": false,
         "description": "mild is a qualitative intensity value that is more intense than weak, but less intense than moderate.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Mild",
         "type": "object"
      },
      "Millennium": {
         "additionalProperties": false,
         "description": "A millenium is a period of 1000 years",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Millennium",
         "type": "object"
      },
      "MinimalValue": {
         "additionalProperties": false,
         "description": "A minimal value is smallest value of an attribute for the entities in the defined set.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MinimalValue",
         "type": "object"
      },
      "MinorTickMark": {
         "additionalProperties": false,
         "description": "A minor tick mark is a tick mark that indicates the position of a specific numeric value but has no adjacent value label, or is one of a pair of tick marks that delineates the boundary of a categorical value but has no adjacent category label on the categorical axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MinorTickMark",
         "type": "object"
      },
      "MinorVersionNumber": {
         "additionalProperties": false,
         "description": "A minor version number is a version of a software that exhibits minor features or significant fix from a prior version.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MinorVersionNumber",
         "type": "object"
      },
      "Minute": {
         "additionalProperties": false,
         "description": "A minute is a period of 60 seconds.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Minute",
         "type": "object"
      },
      "Misery": {
         "additionalProperties": false,
         "description": "misery is a feeling of great unhappiness, suffering and/or pain.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Misery",
         "type": "object"
      },
      "Mode": {
         "additionalProperties": false,
         "description": "A mode is the value that appears most often in a set of data.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Mode",
         "type": "object"
      },
      "Model": {
         "additionalProperties": false,
         "description": "A model is a representation of some thing.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Model",
         "type": "object"
      },
      "Moderate": {
         "additionalProperties": false,
         "description": "moderate is a qualitative intensity value that is more intense than mild, but less intense than strong.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Moderate",
         "type": "object"
      },
      "ModeratelyQuantifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ModeratelyQuantifier",
         "type": "object"
      },
      "Modifying": {
         "additionalProperties": false,
         "description": "modifying is the process by which an entity gains or loses parts, qualities, roles, dispositions, functions, etc, but maintains their identity through these changes.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Modifying",
         "type": "object"
      },
      "MolecularComplex": {
         "additionalProperties": false,
         "description": "A molecular complex is a chemical complex composed of at least one weakly interacting molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularComplex",
         "type": "object"
      },
      "MolecularComplexDissociation": {
         "additionalProperties": false,
         "description": "molecular complex disassociation is the process of dissambly of a molecular complex into its constitutent parts.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularComplexDissociation",
         "type": "object"
      },
      "MolecularComplexFormation": {
         "additionalProperties": false,
         "description": "molecular complex formation is the process of forming a molecular complex from its constituent parts.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularComplexFormation",
         "type": "object"
      },
      "MolecularEntityRole": {
         "additionalProperties": false,
         "description": "A molecular entity role is a chemical entity role held by a molecule.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularEntityRole",
         "type": "object"
      },
      "MolecularIdentifier": {
         "additionalProperties": false,
         "description": "A molecular identifier is an identifier for a molecular entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularIdentifier",
         "type": "object"
      },
      "MolecularModification": {
         "additionalProperties": false,
         "description": "Molecular modification is chemical alteration of a known and previously characterized lead compound for the purpose of enhancing its usefulness as a drug. This could mean enhancing its specificity for a particular body target site, increasing its potency, improving its rate and extent of absorption, modifying to advantage its time course in the body, reducing its toxicity, changing its physical or chemical properties (like solubility) to provide desired features.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularModification",
         "type": "object"
      },
      "MolecularOrbital": {
         "additionalProperties": false,
         "description": "A molecular orbital (or MO) is a mathematical function describing the wave-like behavior of an electron in a molecule.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularOrbital",
         "type": "object"
      },
      "MolecularPocket": {
         "additionalProperties": false,
         "description": "A molecular pocket is a site on a molecule that appears as a depression into the structure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularPocket",
         "type": "object"
      },
      "MolecularRegulator": {
         "additionalProperties": false,
         "description": "A molecular regulator is a molecule that regulates the function of another chemical entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularRegulator",
         "type": "object"
      },
      "MolecularSite": {
         "additionalProperties": false,
         "description": "A moleclar site is a spatial region bounded (in part or in whole) by a molecule and may be occupied by other material entities (e.g. drugs).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularSite",
         "type": "object"
      },
      "MolecularStructure": {
         "additionalProperties": false,
         "description": "molecular structure is the spatial arrangement of atoms in a molecule and the chemical bonds that hold the atoms together.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularStructure",
         "type": "object"
      },
      "MolecularStructureDescriptor": {
         "additionalProperties": false,
         "description": "molecular structure descriptor is data that describes some aspect of the molecular structure (composition) and is about some chemical entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularStructureDescriptor",
         "type": "object"
      },
      "MolecularStructureFile": {
         "additionalProperties": false,
         "description": "A molecular structure file is a file that contains a description of molecular structure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularStructureFile",
         "type": "object"
      },
      "MolecularTracerRole": {
         "additionalProperties": false,
         "description": "A molecular tracer role is a reactant role of a molecular entity that serves as a marker for the presence, abundance, or location of a molecular target that it associates with.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolecularTracerRole",
         "type": "object"
      },
      "Molecule": {
         "additionalProperties": false,
         "description": "A molecule is a single chemical entity composed of fully covalently bonded atoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Molecule",
         "type": "object"
      },
      "MolybdemumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MolybdemumAtom",
         "type": "object"
      },
      "Monomer": {
         "additionalProperties": false,
         "description": "A monomer is a submolecule that is proper part of some polymer, and is a building block for such polymer.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Monomer",
         "type": "object"
      },
      "Monosaccharide": {
         "additionalProperties": false,
         "description": "A monosaccharide is an organic molecule that consists of a single polyhydroxy aldehyde or ketone group.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Monosaccharide",
         "type": "object"
      },
      "Month": {
         "additionalProperties": false,
         "description": "A month is a period of time that divides the year.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Month",
         "type": "object"
      },
      "Morpheme": {
         "additionalProperties": false,
         "description": "A morpheme is the smallest semantically meaningful unit in a language.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Morpheme",
         "type": "object"
      },
      "MostOfTheTime": {
         "additionalProperties": false,
         "description": "most refers to in the majority of occasions",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MostOfTheTime",
         "type": "object"
      },
      "MostlyQuantifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MostlyQuantifier",
         "type": "object"
      },
      "Mouse": {
         "additionalProperties": false,
         "description": "A mouse is a small mammal belonging to the order of rodents, characteristically having a pointed snout, small rounded ears, and a long naked or almost hairless tail.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Mouse",
         "type": "object"
      },
      "Movement": {
         "additionalProperties": false,
         "description": "movement is the process in which an object is spatially displaced.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Movement",
         "type": "object"
      },
      "MovementEquation": {
         "additionalProperties": false,
         "description": "A movement equation describes the displacement of an object in space over time.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MovementEquation",
         "type": "object"
      },
      "Movie": {
         "additionalProperties": false,
         "description": "A movie is a series of images that are displayed in rapid succession  to give the impression of movement.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Movie",
         "type": "object"
      },
      "MulticellularOrganism": {
         "additionalProperties": false,
         "description": "A multi-cellular organism is an organism that consists of more than one cell.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MulticellularOrganism",
         "type": "object"
      },
      "MultipleSequenceAlignment": {
         "additionalProperties": false,
         "description": "A multiple sequence alignment is a sequence alignment involving more than two sequences.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MultipleSequenceAlignment",
         "type": "object"
      },
      "MutualDisposition": {
         "additionalProperties": false,
         "description": "A mutual disposition is a disposition that simulataneously invokes another disposition when realized.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isDispositionOf": {
               "$ref": "#/$defs/Entity",
               "description": "is disposition of is a relation between a disposition and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "MutualDisposition",
         "type": "object"
      },
      "Name": {
         "additionalProperties": false,
         "description": "A name is a label used to identify an entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Name",
         "type": "object"
      },
      "Namespace": {
         "additionalProperties": false,
         "description": "A namespace is an informational entity that defines a logical container for a set of symbols or identifiers.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Namespace",
         "type": "object"
      },
      "NamespaceLabel": {
         "additionalProperties": false,
         "description": "A namespace label is a short name for a namespace.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NamespaceLabel",
         "type": "object"
      },
      "Negationnot": {
         "additionalProperties": false,
         "description": "NOT is a logical operator in that has the value true if its operand is false.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Negationnot",
         "type": "object"
      },
      "Negative": {
         "additionalProperties": false,
         "description": "negative is an assertional qualifier that expresses the falsity or lack of truth of a basic assertion.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Negative",
         "type": "object"
      },
      "NegativeCharge": {
         "additionalProperties": false,
         "description": "A negative charge is a charge where the value is negative.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NegativeCharge",
         "type": "object"
      },
      "NegativeEmotion": {
         "additionalProperties": false,
         "description": "negative emotion is an emotion that does not feel good.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NegativeEmotion",
         "type": "object"
      },
      "NegativeNucleicAcidStrand": {
         "additionalProperties": false,
         "description": "The negative nucleic acid strand is the strand that is that is complimentary to the forward strand and appears from 3' to 5'.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NegativeNucleicAcidStrand",
         "type": "object"
      },
      "NeitherGoodNorPoorQuality": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NeitherGoodNorPoorQuality",
         "type": "object"
      },
      "NeitherSatisfiedOrDissatisfiedQualifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NeitherSatisfiedOrDissatisfiedQualifier",
         "type": "object"
      },
      "NeodymiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NeodymiumAtom",
         "type": "object"
      },
      "NeonAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NeonAtom",
         "type": "object"
      },
      "NeptuniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NeptuniumAtom",
         "type": "object"
      },
      "NetworkDiagram": {
         "additionalProperties": false,
         "description": "A network diagram consists of a set of vertices connected by edges.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NetworkDiagram",
         "type": "object"
      },
      "Never": {
         "additionalProperties": false,
         "description": "never refers to in none of the occasions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Never",
         "type": "object"
      },
      "Newspaper": {
         "additionalProperties": false,
         "description": "A newspaper is a periodical publication containing news regarding current events, informative articles, diverse features, editorials, and advertising.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Newspaper",
         "type": "object"
      },
      "NickelAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NickelAtom",
         "type": "object"
      },
      "NiobiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NiobiumAtom",
         "type": "object"
      },
      "NitrogenAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NitrogenAtom",
         "type": "object"
      },
      "NmrDevice": {
         "additionalProperties": false,
         "description": "A nuclear magnetic resonance (NMR) device is a device that applies a magnetic field to perturb nuclei with an odd number of protons and/or of neutrons in order to hav them absort and re-emit electromagnetic radiation.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NmrDevice",
         "type": "object"
      },
      "NobeliumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NobeliumAtom",
         "type": "object"
      },
      "Node": {
         "additionalProperties": false,
         "description": "a node or vertex is a point in a graph.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Node",
         "type": "object"
      },
      "NomimalScale": {
         "additionalProperties": false,
         "description": "A nominal scale of measurement only specifies a limited set of categories.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NomimalScale",
         "type": "object"
      },
      "Non-cellularOrganism": {
         "additionalProperties": false,
         "description": "A non-cellular organism is an organism that does not contain a cell.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Non-cellularOrganism",
         "type": "object"
      },
      "Non-polar": {
         "additionalProperties": false,
         "description": "non-polar is the quality of not having a dipole.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Non-polar",
         "type": "object"
      },
      "Non-polarAdditionReaction": {
         "additionalProperties": false,
         "description": "A non-polar addition reaction is an addition reaction involving non-polar residues.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Non-polarAdditionReaction",
         "type": "object"
      },
      "Non-proteinCodingRNAncRNA": {
         "additionalProperties": false,
         "description": "A non-protein coding RNA (ncRNA) is a RNA molecular that cannot be used as a template for generating a protein product.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Non-proteinCodingRNAncRNA",
         "type": "object"
      },
      "Non-proteinCodingRNAncRNAGene": {
         "additionalProperties": false,
         "description": "A non-protein coding RNA (ncRNA) gene is a gene that encodes for a RNA transcript that is not further translated into a protein product.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Non-proteinCodingRNAncRNAGene",
         "type": "object"
      },
      "NonToxic": {
         "additionalProperties": false,
         "description": "non toxic is the quality of a substance having no damaging effect to a system.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NonToxic",
         "type": "object"
      },
      "NoneQuantifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NoneQuantifier",
         "type": "object"
      },
      "NonpolarSolvent": {
         "additionalProperties": false,
         "description": "A non-polar solvent is a solvent that exhibits a non-polar quality.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NonpolarSolvent",
         "type": "object"
      },
      "Normal": {
         "additionalProperties": false,
         "description": "A quality that has a value that is normal or average.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Normal",
         "type": "object"
      },
      "Normality": {
         "additionalProperties": false,
         "description": "normality is the quality in which the value may differ from normal or average",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Normality",
         "type": "object"
      },
      "NotStarted": {
         "additionalProperties": false,
         "description": "not started is the status of a process that is predicted to exist but has not yet begun.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NotStarted",
         "type": "object"
      },
      "Notation": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Notation",
         "type": "object"
      },
      "Note": {
         "additionalProperties": false,
         "description": "A note is a brief document.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Note",
         "type": "object"
      },
      "Novel": {
         "additionalProperties": false,
         "description": "A novel is a fictitious prose narrative of book length, typically representing character and action with some degree of realism.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Novel",
         "type": "object"
      },
      "NucleicAcid": {
         "additionalProperties": false,
         "description": "A nucleic acid is an organic polymer composed of a sequence of nucleotide residues.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NucleicAcid",
         "type": "object"
      },
      "NucleicAcidPart": {
         "additionalProperties": false,
         "description": "A nucleic acid part is a component of a nucleic acid.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NucleicAcidPart",
         "type": "object"
      },
      "NucleicAcidSequence": {
         "additionalProperties": false,
         "description": "A nucleic acid sequence is a symbolic representation of the sequence of nucleic acid residues in a nucleic acid.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NucleicAcidSequence",
         "type": "object"
      },
      "NucleicAcidStrand": {
         "additionalProperties": false,
         "description": "A nucleic acid strand is a single-stranded nucleic acid that is part of a double stranded nucleic acid complex.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NucleicAcidStrand",
         "type": "object"
      },
      "NucleophilicAdditionReaction": {
         "additionalProperties": false,
         "description": "A nucleophilic addition reaction is an addition reaction where a pi bond is removed by the creation of two new covalent bonds by the addition from a nucleophile.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NucleophilicAdditionReaction",
         "type": "object"
      },
      "NucleotideResidue": {
         "additionalProperties": false,
         "description": "A nucleotide residue is a part of a molecule that derives from a nucleotide.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NucleotideResidue",
         "type": "object"
      },
      "Nullizygous": {
         "additionalProperties": false,
         "description": "nullizygous is the quality of a biological organism in which there are no allelles at a given locus.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Nullizygous",
         "type": "object"
      },
      "Number": {
         "additionalProperties": false,
         "description": "A number is a tensor of rank 0.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Number",
         "type": "object"
      },
      "NumberOfObjectsConsumed": {
         "additionalProperties": false,
         "description": "number of objects consumed is a count of objects that were consumed in some process.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NumberOfObjectsConsumed",
         "type": "object"
      },
      "NumberOfObjectsProduced": {
         "additionalProperties": false,
         "description": "number of objects produced is a count of objects that were produced in some process.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NumberOfObjectsProduced",
         "type": "object"
      },
      "NumericLabel": {
         "additionalProperties": false,
         "description": "A numeric label is a number used as a label.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NumericLabel",
         "type": "object"
      },
      "NumericScale": {
         "additionalProperties": false,
         "description": "A numeric scale of measurement is one that only specifies numeric values.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NumericScale",
         "type": "object"
      },
      "Nurse": {
         "additionalProperties": false,
         "description": "A nurse is an individual that is involved in the protection, promotion, and optimization of health and abilities, prevention of illness and injury, alleviation of suffering through the diagnosis and treatment of human response, and advocacy in the care of individuals, families, communities, and populations.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Nurse",
         "type": "object"
      },
      "NurseRole": {
         "additionalProperties": false,
         "description": "A nurse role is the role of an individual that is involved in the protection, promotion, and optimization of health and abilities, prevention of illness and injury, alleviation of suffering through the diagnosis and treatment of human response, and advocacy in the care of individuals, families, communities, and populations.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "NurseRole",
         "type": "object"
      },
      "OBOOntology": {
         "additionalProperties": false,
         "description": "An OBO ontology is an ontology document as specified by the Open Biological Ontology community.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OBOOntology",
         "type": "object"
      },
      "OWLOntology": {
         "additionalProperties": false,
         "description": "An OWL ontology is an ontology as specified by the W3C OWL specification.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OWLOntology",
         "type": "object"
      },
      "Object": {
         "additionalProperties": false,
         "description": "An object is an entity that is wholly identifiable at any instant of time during which it exists.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Object",
         "type": "object"
      },
      "ObjectModel": {
         "additionalProperties": false,
         "description": "An object model is a representation of an object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ObjectModel",
         "type": "object"
      },
      "ObjectQuality": {
         "additionalProperties": false,
         "description": "An object quality is quality of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ObjectQuality",
         "type": "object"
      },
      "ObjectRelationalQuality": {
         "additionalProperties": false,
         "description": "object relational quality refers to a quality in relation to one or more objects",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ObjectRelationalQuality",
         "type": "object"
      },
      "Objective": {
         "additionalProperties": false,
         "description": "An objective is a proposition that indicates a planned or anticipated outcome.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Objective",
         "type": "object"
      },
      "ObservationalStudy": {
         "additionalProperties": false,
         "description": "observational study draws inferences about the possible effect of a treatment on subjects, where the assignment of subjects into a treated group versus a control group is outside the control of the investigator",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ObservationalStudy",
         "type": "object"
      },
      "Observing": {
         "additionalProperties": false,
         "description": "observing is a process of passive interaction in which one entity makes note of attributes of one or more entities.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Observing",
         "type": "object"
      },
      "OccupationalRole": {
         "additionalProperties": false,
         "description": "An occupational role is a social role that pertains to an organizational structure.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OccupationalRole",
         "type": "object"
      },
      "Oligopeptide": {
         "additionalProperties": false,
         "description": "an oligopeptide is an organic polymer composed of fewer than 10 or 15 amino acids.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Oligopeptide",
         "type": "object"
      },
      "Ongoing": {
         "additionalProperties": false,
         "description": "ongoing is the status of a process that is not yet complete.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ongoing",
         "type": "object"
      },
      "Ontology": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ontology",
         "type": "object"
      },
      "OntologyDocument": {
         "additionalProperties": false,
         "description": "An ontology document is a document that contains an ontology.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OntologyDocument",
         "type": "object"
      },
      "OpenInterval": {
         "additionalProperties": false,
         "description": "an open interval is an interval that does not include its endpoints.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OpenInterval",
         "type": "object"
      },
      "OpenReadingFrame": {
         "additionalProperties": false,
         "description": "An open reading frame (ORF) is a part of a gene that encodes a protein but does not contain a stop codon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OpenReadingFrame",
         "type": "object"
      },
      "Operon": {
         "additionalProperties": false,
         "description": "An operon is a collection of contiguous genes transcribed as a single (polycistronic) mRNA.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Operon",
         "type": "object"
      },
      "Opinion": {
         "additionalProperties": false,
         "description": "An opinion is a belief that is the result of emotion or interpretation of facts.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Opinion",
         "type": "object"
      },
      "OpticalIsomer": {
         "additionalProperties": false,
         "description": "An optical isomer is a stereoisomer that rotates the plane of polarization of a beam of plane polarized light.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OpticalIsomer",
         "type": "object"
      },
      "OrderedList": {
         "additionalProperties": false,
         "description": "an ordered list is a list in which items are sequentially ordered.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrderedList",
         "type": "object"
      },
      "OrderedListItem": {
         "additionalProperties": false,
         "description": "an ordered list item is an item in an ordered list.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrderedListItem",
         "type": "object"
      },
      "OrdinalPosition": {
         "additionalProperties": false,
         "description": "A ordinal position is a number that designates the position of an entity from the first entity in an ordered sequence of entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrdinalPosition",
         "type": "object"
      },
      "OrdinaryDifferentialEquation": {
         "additionalProperties": false,
         "description": "An ordinary differential equation (ODE) is a differential equation in which the unknown function (also known as the dependent variable) is a function of a single independent variable.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrdinaryDifferentialEquation",
         "type": "object"
      },
      "Organ": {
         "additionalProperties": false,
         "description": "An organ is a collection of tissues joined in structural unit to serve a common function.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Organ",
         "type": "object"
      },
      "OrganicMolecule": {
         "additionalProperties": false,
         "description": "An organic molecule is a molecule composed of organic atoms (at least carbon, hydrogen, and optionally oxygen, phosphorus, nitrogen, sulfur)",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrganicMolecule",
         "type": "object"
      },
      "OrganicPolymer": {
         "additionalProperties": false,
         "description": "An organic polymer is an organic molecule composed of connected set of monomeric units.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrganicPolymer",
         "type": "object"
      },
      "OrganicReaction": {
         "additionalProperties": false,
         "description": "An organic reaction is a chemical reaction involving at least one organic molecule.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrganicReaction",
         "type": "object"
      },
      "OrganicSubmolecule": {
         "additionalProperties": false,
         "description": "An organic submolecule is connected region of a molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OrganicSubmolecule",
         "type": "object"
      },
      "Organism": {
         "additionalProperties": false,
         "description": "A biological organisn is a biological entity that consists of one or more cells and is capable of genomic replication (independently or not).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Organism",
         "type": "object"
      },
      "Organization": {
         "additionalProperties": false,
         "description": "An organization is a collective with a complex articulation of tasks, roles and responsibilities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Organization",
         "type": "object"
      },
      "Orientation": {
         "additionalProperties": false,
         "description": "orientation is an angle between the bearer and an axis, or the angle between the bearer and another object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Orientation",
         "type": "object"
      },
      "OsmiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OsmiumAtom",
         "type": "object"
      },
      "Osmosis": {
         "additionalProperties": false,
         "description": "osmosis is the movement of water molecules through a selectively-permeable membrane down a water potential gradient.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Osmosis",
         "type": "object"
      },
      "Ovopub": {
         "additionalProperties": false,
         "description": "An ovopub is an information content entity that contains and links to one or more resources and/or statements, including those describing its provenance, and is itself a dereferenceable resource.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ovopub",
         "type": "object"
      },
      "OxygenAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "OxygenAtom",
         "type": "object"
      },
      "PDBChainIdentifier": {
         "additionalProperties": false,
         "description": "A PDB chain identifier is a alphabetical label to identify a molecule in a structure provided by the Protein DataBank .",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PDBChainIdentifier",
         "type": "object"
      },
      "PDBFile": {
         "additionalProperties": false,
         "description": "A PDB file is a molecular structure file specified by the Protein DataBank (PDB).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PDBFile",
         "type": "object"
      },
      "PDBRecordIdentifier": {
         "additionalProperties": false,
         "description": "A PDB record identifier is an identifier for a PDB generated record.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PDBRecordIdentifier",
         "type": "object"
      },
      "PH": {
         "additionalProperties": false,
         "description": "pH is a measure of the activity of the (solvated) hydrogen ion.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PH",
         "type": "object"
      },
      "PageNumber": {
         "additionalProperties": false,
         "description": "A page number is the count of a page in a sequence of pages.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PageNumber",
         "type": "object"
      },
      "PageRange": {
         "additionalProperties": false,
         "description": "A page range denotes the start and end page in some document.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PageRange",
         "type": "object"
      },
      "PageTotal": {
         "additionalProperties": false,
         "description": "A page total is a textual entity that is about the number of pages in some informational entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PageTotal",
         "type": "object"
      },
      "Pain": {
         "additionalProperties": false,
         "description": "Pain is an unpleasant sensory and emotional experience associated with actual or potential tissue damage, or described in terms of such damage.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pain",
         "type": "object"
      },
      "PairwiseSequenceAlignment": {
         "additionalProperties": false,
         "description": "A pairwise sequence alignment is the alignment of exactly 2 sequences.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PairwiseSequenceAlignment",
         "type": "object"
      },
      "PalladiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PalladiumAtom",
         "type": "object"
      },
      "Panic": {
         "additionalProperties": false,
         "description": "Panic is a sudden emotion of fear which is so strong as to dominate or prevent reason and logical thinking, replacing it with overwhelming feelings of anxiety and frantic agitation consistent with an animalistic fight-or-flight reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Panic",
         "type": "object"
      },
      "Paragraph": {
         "additionalProperties": false,
         "description": "A paragraph is a self-contained unit of written discourse consisting of one or more sentences.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Paragraph",
         "type": "object"
      },
      "Parameter": {
         "additionalProperties": false,
         "description": "A parameter is variable whose value changes the characteristics of a system or a function.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Parameter",
         "type": "object"
      },
      "ParameterizedDataTransformation": {
         "additionalProperties": false,
         "description": "A parameterized data transformation is a data transformation whose behaviour may be modified by one or more parameters.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ParameterizedDataTransformation",
         "type": "object"
      },
      "ParentalTransmission": {
         "additionalProperties": false,
         "description": "parental transmition is the quality of a biological organism in which the (genetic) material is transmitted from a parental unit.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ParentalTransmission",
         "type": "object"
      },
      "PartialCharge": {
         "additionalProperties": false,
         "description": "The quality of having a charge that is not a full multiple of 1 unit charge.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PartialCharge",
         "type": "object"
      },
      "PartialDifferentialEquation": {
         "additionalProperties": false,
         "description": "A partial differential equation (PDE) is a differential equation in which the unknown function is a function of multiple independent variables and the equation involves its partial derivatives.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PartialDifferentialEquation",
         "type": "object"
      },
      "PartialNegativeCharge": {
         "additionalProperties": false,
         "description": "A partial positive charge is a partial charge where the value of the charge is positive.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PartialNegativeCharge",
         "type": "object"
      },
      "PartialPositiveCharge": {
         "additionalProperties": false,
         "description": "A partial negative charge is a negative charge where the value of the charge is negative.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PartialPositiveCharge",
         "type": "object"
      },
      "Passion": {
         "additionalProperties": false,
         "description": "passion is the intense desire for something.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Passion",
         "type": "object"
      },
      "PassiveMovement": {
         "additionalProperties": false,
         "description": "passive movement is the process in which an object is spatially displaced without an expenditure of energy contained in molecular bonds.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PassiveMovement",
         "type": "object"
      },
      "PassiveTransport": {
         "additionalProperties": false,
         "description": "passive transport is the movement of a substance across a membrane and does not require chemical energy.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PassiveTransport",
         "type": "object"
      },
      "Patent": {
         "additionalProperties": false,
         "description": "A patent is an information entity granted by a patent issuing authority which confers upon the patenter the sole right to make, use and sell an invention for a set period of time.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Patent",
         "type": "object"
      },
      "PaternallyTransmitted": {
         "additionalProperties": false,
         "description": "paternally transmitted is the quality of a biological organism in which the (genetic) material is transmitted from the paternal line.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PaternallyTransmitted",
         "type": "object"
      },
      "Pathogen": {
         "additionalProperties": false,
         "description": "A pathogen or infectious agent  is a microorganism that causes disease in its host.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pathogen",
         "type": "object"
      },
      "Pathway": {
         "additionalProperties": false,
         "description": "A pathway is an effective specification that outlines a set of actions that forms a way to achieve an objective.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pathway",
         "type": "object"
      },
      "Patient": {
         "additionalProperties": false,
         "description": "A patient is an individual that is the recepient of medical care.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Patient",
         "type": "object"
      },
      "PatientRole": {
         "additionalProperties": false,
         "description": "A patient role is the role of an individual that is the recepient of medical care.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PatientRole",
         "type": "object"
      },
      "Pattern": {
         "additionalProperties": false,
         "description": "A pattern is a generalized representation of some repeatable concrete or informational item.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pattern",
         "type": "object"
      },
      "Peer-reviewed": {
         "additionalProperties": false,
         "description": "peer-reviewed is the quality of a textual entity that has been examined and commented by a peer expert reviewer.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Peer-reviewed",
         "type": "object"
      },
      "PeerReviewedArticle": {
         "additionalProperties": false,
         "description": "A peer reviewed article is an article that has undergone peer-review and deemed acceptable for publication.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PeerReviewedArticle",
         "type": "object"
      },
      "Peptide": {
         "additionalProperties": false,
         "description": "a peptide is an organic polymer composed of between two and fifty amino acids.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Peptide",
         "type": "object"
      },
      "Percentage": {
         "additionalProperties": false,
         "description": "A percentage is a number that is a ratio expressed as a fraction of 100. It is denoted using the percent sign \"%\".",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Percentage",
         "type": "object"
      },
      "Percentile": {
         "additionalProperties": false,
         "description": "A percentile (or a centile) is a quantile that divides the given probability distribution, or sample, into 100 equal-sized intervals.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Percentile",
         "type": "object"
      },
      "Perception": {
         "additionalProperties": false,
         "description": "perception is the organization, identification, and interpretation of sensory information in order to fabricate a mental representation through the process of transduction, which sensors in the body transform signals from the environment into encoded neural signals.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Perception",
         "type": "object"
      },
      "Periodical": {
         "additionalProperties": false,
         "description": "A periodical is a publication that appears on a regular schedule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Periodical",
         "type": "object"
      },
      "Person": {
         "additionalProperties": false,
         "description": "A person is an object that has certain capacities or attributes constituting personhood.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Person",
         "type": "object"
      },
      "PersonalName": {
         "additionalProperties": false,
         "description": "A personal name is a name to identify an individual person and usually comprises of a first name and a last name.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PersonalName",
         "type": "object"
      },
      "PharmaceuticalDrug": {
         "additionalProperties": false,
         "description": "A pharmaceutical preparation is a chemical substance that is approved for use in the medical diagnosis, cure, treatment, or prevention of disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PharmaceuticalDrug",
         "type": "object"
      },
      "PharmaceuticalIngredient": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PharmaceuticalIngredient",
         "type": "object"
      },
      "PharmacodynamicPathway": {
         "additionalProperties": false,
         "description": "a pharmacodynamic pathay is a regulatory pathway in which a drug molecule regulates the activity of one or more components organized in a pathway.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PharmacodynamicPathway",
         "type": "object"
      },
      "PharmacokineticPathway": {
         "additionalProperties": false,
         "description": "a pharmacokinetic pathway is a metabolic pathway which describes the metabolism of a drug molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PharmacokineticPathway",
         "type": "object"
      },
      "PhdThesis": {
         "additionalProperties": false,
         "description": "A PhD thesis is a thesis prepared as a requirement for a doctoral degree.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PhdThesis",
         "type": "object"
      },
      "Phenotype": {
         "additionalProperties": false,
         "description": "A phenotype is an observable characteristic of an individual.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Phenotype",
         "type": "object"
      },
      "PhosphorusAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PhosphorusAtom",
         "type": "object"
      },
      "Photograph": {
         "additionalProperties": false,
         "description": "A photograph is an image created by light falling on a light-sensitive surface.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Photograph",
         "type": "object"
      },
      "Phrase": {
         "additionalProperties": false,
         "description": "A phrase is a group of words functioning as a single unit in the syntax of a sentence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Phrase",
         "type": "object"
      },
      "PhraseNetDiagram": {
         "additionalProperties": false,
         "description": "A phrase net diagram illustrates the relationship between different words used in a text.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PhraseNetDiagram",
         "type": "object"
      },
      "PhysicalEntityIdentifier": {
         "additionalProperties": false,
         "description": "A physical entity identifier is an identifier for a physical entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PhysicalEntityIdentifier",
         "type": "object"
      },
      "PieChart": {
         "additionalProperties": false,
         "description": "A pie chart is a circular chart divided into sectors each of whose length  is proportional to the quantity it represents.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PieChart",
         "type": "object"
      },
      "Pity": {
         "additionalProperties": false,
         "description": "Pity is the emotion of sadness or sorrow for another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pity",
         "type": "object"
      },
      "Placebo": {
         "additionalProperties": false,
         "description": "A placebo is a medically ineffectual treatment for a medical condition intended to deceive the recipient.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Placebo",
         "type": "object"
      },
      "Plan": {
         "additionalProperties": false,
         "description": "A plan is a set of intended actions, through which one expects to achieve a goal.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Plan",
         "type": "object"
      },
      "Planned": {
         "additionalProperties": false,
         "description": "planned is a process status for a process that has not yet started, but is referred to in a plan.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Planned",
         "type": "object"
      },
      "Planning": {
         "additionalProperties": false,
         "description": "planning is the agentive process of developing a plan that specifies a set of actions in order to meet a set of goals or objectives.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Planning",
         "type": "object"
      },
      "PlateauLine": {
         "additionalProperties": false,
         "description": "An plateau line is a line segment in which the startpoint and endpoint are ordered along one dimension and the difference of values in a second dimension is zero.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PlateauLine",
         "type": "object"
      },
      "PlatinumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PlatinumAtom",
         "type": "object"
      },
      "Pleasure": {
         "additionalProperties": false,
         "description": "pleasure is an emotion of happy satisfaction and enjoyment.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pleasure",
         "type": "object"
      },
      "Ploidy": {
         "additionalProperties": false,
         "description": "ploidy is the cellular quality relating to the amount of DNA contained in a cell.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ploidy",
         "type": "object"
      },
      "Plot": {
         "additionalProperties": false,
         "description": "A plot is a part of a figure that corresponds to the spatial region between the set of axes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Plot",
         "type": "object"
      },
      "PlutoniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PlutoniumAtom",
         "type": "object"
      },
      "Point": {
         "additionalProperties": false,
         "description": "A point is a geometric entity that is located in a zero-dimensional spatial region and whose position is defined by its coordinates in some coordinate system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Point",
         "type": "object"
      },
      "Poison": {
         "additionalProperties": false,
         "description": "A poison is a drug that is harzardous or toxic to an organism when ingested at a certain quantity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Poison",
         "type": "object"
      },
      "PoisonRole": {
         "additionalProperties": false,
         "description": "A poison role is the role of a substance that causes some negative disturbance in an organism.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PoisonRole",
         "type": "object"
      },
      "Polar": {
         "additionalProperties": false,
         "description": "polar is the quality of having a dipole.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Polar",
         "type": "object"
      },
      "PolarAdditionReaction": {
         "additionalProperties": false,
         "description": "A polar addition reaction is an addition reaction involving polar residues.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolarAdditionReaction",
         "type": "object"
      },
      "PolarCoordinate": {
         "additionalProperties": false,
         "description": "A polar coordinate is a position characterized by a distance from a fixed point and an angle from a fixed direction.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolarCoordinate",
         "type": "object"
      },
      "PolarCoordinateSystem": {
         "additionalProperties": false,
         "description": "A polar coordinate system is a two-dimensional coordinate system in which each point on a plane is determined by a distance from a fixed point and an angle from a fixed direction.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolarCoordinateSystem",
         "type": "object"
      },
      "PolarQuality": {
         "additionalProperties": false,
         "description": "The quality of being polar or not polar.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolarQuality",
         "type": "object"
      },
      "PolarSolvent": {
         "additionalProperties": false,
         "description": "A polar solvent is a solvent that exhibits a polar quality.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolarSolvent",
         "type": "object"
      },
      "PoloniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PoloniumAtom",
         "type": "object"
      },
      "Polygon": {
         "additionalProperties": false,
         "description": "A polygon is a planar entity that is bounded by a closed path or circuit, composed of a finite connected sequence3 of straight line segments.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Polygon",
         "type": "object"
      },
      "PolygonEdge": {
         "additionalProperties": false,
         "description": "A polygon edge is a line segment joining two polygon vertices.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolygonEdge",
         "type": "object"
      },
      "PolygonVertex": {
         "additionalProperties": false,
         "description": "A polygon vertex is a terminal point at which two polygon edges meet and are part of a polygon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolygonVertex",
         "type": "object"
      },
      "PolygonalFace": {
         "additionalProperties": false,
         "description": "A polygonal face is a polygon bounded by a circuit of polygon edges, and includes the flat (plane) region inside the boundary.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolygonalFace",
         "type": "object"
      },
      "PolyhedralSkeleton": {
         "additionalProperties": false,
         "description": "A polyhedral skeleton is a collection of polygon edges.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolyhedralSkeleton",
         "type": "object"
      },
      "PolyhedralSurface": {
         "additionalProperties": false,
         "description": "A polyhedral surface is composed of polygonal faces.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PolyhedralSurface",
         "type": "object"
      },
      "Polyline": {
         "additionalProperties": false,
         "description": "A polyline is a connected sequence of line segments.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Polyline",
         "type": "object"
      },
      "Polymer": {
         "additionalProperties": false,
         "description": "A polymer is a molecule composed of a connected set of monomeric residues.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Polymer",
         "type": "object"
      },
      "Polypeptide": {
         "additionalProperties": false,
         "description": "A polypeptide is an organic polymer composed of amino acid residues, typically of less than 50 amino acids in length.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Polypeptide",
         "type": "object"
      },
      "PoorQuality": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PoorQuality",
         "type": "object"
      },
      "Population": {
         "additionalProperties": false,
         "description": "A population is all the organisms that both belong to the same group or species and live in the same geographical area.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Population",
         "type": "object"
      },
      "Position": {
         "additionalProperties": false,
         "description": "A measurement of a spatial location relative to a frame of reference or other objects.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Position",
         "type": "object"
      },
      "PositionalIdentifier": {
         "additionalProperties": false,
         "description": "A positional description is a description of location using some system or frame of reference.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PositionalIdentifier",
         "type": "object"
      },
      "PositionalRole": {
         "additionalProperties": false,
         "description": "A positional role is an abstract role which holds by comparing position to another object of reference.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PositionalRole",
         "type": "object"
      },
      "PositionallyOrientedLine": {
         "additionalProperties": false,
         "description": "A positionally oriented line is a line that is positioned against some axis of reference.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PositionallyOrientedLine",
         "type": "object"
      },
      "Positive": {
         "additionalProperties": false,
         "description": "positive is an assertional qualifier that expresses the validity or truth of a basic assertion.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Positive",
         "type": "object"
      },
      "PositiveCharge": {
         "additionalProperties": false,
         "description": "A positive charge is a charge where the value is positive.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PositiveCharge",
         "type": "object"
      },
      "PositiveEmotion": {
         "additionalProperties": false,
         "description": "A positive emotion is an emotion that feels good.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PositiveEmotion",
         "type": "object"
      },
      "PositiveNucleicAcidStrand": {
         "additionalProperties": false,
         "description": "The positive nucleic acid strand refers to the strand that is to be read 5' to 3'.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PositiveNucleicAcidStrand",
         "type": "object"
      },
      "Post-translationalModification": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Post-translationalModification",
         "type": "object"
      },
      "PostalCode": {
         "additionalProperties": false,
         "description": "A postal code is a geographic coordinate composed of a series of letters and/or digits appended to a postal address for the purpose of sorting mail.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PostalCode",
         "type": "object"
      },
      "PotassiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PotassiumAtom",
         "type": "object"
      },
      "PraseodymiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PraseodymiumAtom",
         "type": "object"
      },
      "Pre-mRNA": {
         "additionalProperties": false,
         "description": "Precursor mRNA (pre-mRNA) is a single strand of messenger ribonucleic acid (mRNA) that is synthesized from a DNA template throught transcription.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pre-mRNA",
         "type": "object"
      },
      "Predicted": {
         "additionalProperties": false,
         "description": "predicted is the quality of an entity that is thought to exist, as evidenced by some rational procedure.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Predicted",
         "type": "object"
      },
      "PredictedGene": {
         "additionalProperties": false,
         "description": "A predicted gene is a gene that was identified through computational method but has not been experimentally validated.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PredictedGene",
         "type": "object"
      },
      "Predicting": {
         "additionalProperties": false,
         "description": "predicting is the process of formulating a proposition about a state of affairs which might be realized in the future.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Predicting",
         "type": "object"
      },
      "PreferredName": {
         "additionalProperties": false,
         "description": "A preferred name is the name that is generally used by some organization.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PreferredName",
         "type": "object"
      },
      "Premise": {
         "additionalProperties": false,
         "description": "A premise is a proposition of an argument from which the conclusion is drawn.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Premise",
         "type": "object"
      },
      "Pride": {
         "additionalProperties": false,
         "description": "pride is an emotion of satisfaction of attachment toward one's own or another's choices and actions, or toward a whole group of people, and is a product of praise, independent self-reflection, or a fulfilled feeling of belonging.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pride",
         "type": "object"
      },
      "PrimaryActiveTransport": {
         "additionalProperties": false,
         "description": "primary active transport, also called direct active transport, directly uses energy to transport molecules across a membrane.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PrimaryActiveTransport",
         "type": "object"
      },
      "PrimaryCategoryAxis": {
         "additionalProperties": false,
         "description": "A primary category axis is a category axis that either defines the sole value range or holds the larger set of categorical values specified by the secondary category axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PrimaryCategoryAxis",
         "type": "object"
      },
      "PrimaryDatabaseKey": {
         "additionalProperties": false,
         "description": "A primary database key is a database key that identifies every row of a table.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PrimaryDatabaseKey",
         "type": "object"
      },
      "PrimaryGraphTitle": {
         "additionalProperties": false,
         "description": "A primary graph title is a primary title that describes a statistical graph.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PrimaryGraphTitle",
         "type": "object"
      },
      "PrimaryStructureDescriptor": {
         "additionalProperties": false,
         "description": "A primary structure descriptor describes a biomolecular object in terms of a 1D or 2D topology.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PrimaryStructureDescriptor",
         "type": "object"
      },
      "PrimaryTitle": {
         "additionalProperties": false,
         "description": "A primary title is a title that should be first used in describing some entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PrimaryTitle",
         "type": "object"
      },
      "Primer": {
         "additionalProperties": false,
         "description": "A primer is a nucleic acid that enables the synthesis of a complement strand of DNA by binding to it and acting as a point of transcription initiation.",
         "properties": {},
         "required": [],
         "title": "Primer",
         "type": "object"
      },
      "ProbabilityMeasure": {
         "additionalProperties": false,
         "description": "A probability measure is quantity of how likely it is that some event will occur.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProbabilityMeasure",
         "type": "object"
      },
      "ProbabilityValue": {
         "additionalProperties": false,
         "description": "A p-value or probability value is the probability of obtaining a test statistic at least as extreme as the one that was actually observed, assuming that the null hypothesis is true",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProbabilityValue",
         "type": "object"
      },
      "Procedure": {
         "additionalProperties": false,
         "description": "A procedure is a process that attempts to achieve one or more objectives by following an established set of actions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Procedure",
         "type": "object"
      },
      "Process": {
         "additionalProperties": false,
         "description": "A process is an entity that is identifiable only through the unfolding of time, has temporal parts, and unless otherwise specified/predicted, cannot be identified from any instant of time in which it exists.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Process",
         "type": "object"
      },
      "ProcessDown-regulation": {
         "additionalProperties": false,
         "description": "process down-regulation is a process that decreases the frequency, rate or extent of one or more processes in relation to a reference state.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessDown-regulation",
         "type": "object"
      },
      "ProcessMaintenance": {
         "additionalProperties": false,
         "description": "The process of maintaining some the frequency, rate or extent of another process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessMaintenance",
         "type": "object"
      },
      "ProcessModel": {
         "additionalProperties": false,
         "description": "A process model is a representation of some process.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessModel",
         "type": "object"
      },
      "ProcessNumber": {
         "additionalProperties": false,
         "description": "process number is a number associated with a process that denotes its ordinal position in a set of processes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessNumber",
         "type": "object"
      },
      "ProcessQuality": {
         "additionalProperties": false,
         "description": "A process quality is quality that is associated with a process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessQuality",
         "type": "object"
      },
      "ProcessStatus": {
         "additionalProperties": false,
         "description": "process status is a process quality that describes the state of a process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessStatus",
         "type": "object"
      },
      "ProcessUp-regulation": {
         "additionalProperties": false,
         "description": "process up-regulation is a process that increases the frequency, rate or extent of one or more processes in relation to a reference state.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessUp-regulation",
         "type": "object"
      },
      "ProcessualRole": {
         "additionalProperties": false,
         "description": "A processual role is a role that can only be realized in a process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProcessualRole",
         "type": "object"
      },
      "Product": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Product",
         "type": "object"
      },
      "ProductRole": {
         "additionalProperties": false,
         "description": "The role of a chemical entity present at the end of a chemical reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProductRole",
         "type": "object"
      },
      "Professor": {
         "additionalProperties": false,
         "description": "A professor is an individual that is a scholarly teacher.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Professor",
         "type": "object"
      },
      "ProfessorRole": {
         "additionalProperties": false,
         "description": "A professor role is the role of an individual that is involved in teaching of students (undergraduate and/or graduate) at a post-secondary academic institution.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProfessorRole",
         "type": "object"
      },
      "Prognosis": {
         "additionalProperties": false,
         "description": "A prognosis is a proposition about the likely course of a disease, the chance of recovery or recurrence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Prognosis",
         "type": "object"
      },
      "PromethiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PromethiumAtom",
         "type": "object"
      },
      "Proposition": {
         "additionalProperties": false,
         "description": "A proposition is a sentence expressing something true or false.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Proposition",
         "type": "object"
      },
      "ProstheticGroupRole": {
         "additionalProperties": false,
         "description": "A coenzyme role of a chemical entity that is covalently bonded to the  enzyme.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProstheticGroupRole",
         "type": "object"
      },
      "ProtactiniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProtactiniumAtom",
         "type": "object"
      },
      "Protein": {
         "additionalProperties": false,
         "description": "A protein is an organic polymer that is composed of one or more linear polymers of amino acids.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Protein",
         "type": "object"
      },
      "ProteinCodingGene": {
         "additionalProperties": false,
         "description": "A gene that contains an open reading frame which codes for a protein.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinCodingGene",
         "type": "object"
      },
      "ProteinCodingSequence": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinCodingSequence",
         "type": "object"
      },
      "ProteinComplex": {
         "additionalProperties": false,
         "description": "A protein complex is a molecular complex composed of at least two polypeptide chains.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinComplex",
         "type": "object"
      },
      "ProteinDomain": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinDomain",
         "type": "object"
      },
      "ProteinExpressionValue": {
         "additionalProperties": false,
         "description": "A protein expression value is a quantity obtained from a protein expression experiment.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinExpressionValue",
         "type": "object"
      },
      "ProteinFamily": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinFamily",
         "type": "object"
      },
      "ProteinMediatedRegulationOfTranslation": {
         "additionalProperties": false,
         "description": "A process mediated by a protein that modulates the frequency, rate or extent of translation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinMediatedRegulationOfTranslation",
         "type": "object"
      },
      "ProteinSequence": {
         "additionalProperties": false,
         "description": "A protein acid sequence is the character representation of the molecular structure of a protein.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ProteinSequence",
         "type": "object"
      },
      "Province": {
         "additionalProperties": false,
         "description": "A province is a territorial unit, almost always an administrative division, within a country or state.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Province",
         "type": "object"
      },
      "Pseudogene": {
         "additionalProperties": false,
         "description": "A pseudo gene is a region of a nucleic acid that either cannot be transcribed, or its RNA transcript cannot be translated.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Pseudogene",
         "type": "object"
      },
      "Publication": {
         "additionalProperties": false,
         "description": "A publication is a document that has been made available by a publisher.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Publication",
         "type": "object"
      },
      "PublisherRole": {
         "additionalProperties": false,
         "description": "A publisher role is the role of an individual that prepares and issues creative works.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PublisherRole",
         "type": "object"
      },
      "PublishingRole": {
         "additionalProperties": false,
         "description": "A publishing role is the role of an individual that is involved in the preparation and issue of creative works for consumption by a wider audience.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "PublishingRole",
         "type": "object"
      },
      "Purpose": {
         "additionalProperties": false,
         "description": "purpose is the reason for which something is done or created or for which something exists.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Purpose",
         "type": "object"
      },
      "Quadrilateral": {
         "additionalProperties": false,
         "description": "A quadrilateral is a polygon with composed of four points and four line segments, in which each point is fully connected to two other points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Quadrilateral",
         "type": "object"
      },
      "Quality": {
         "additionalProperties": false,
         "description": "A quality is an attribute that is intrinsically associated with its bearer (or its parts), but whose presence/absence and observed/measured value may vary.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Quality",
         "type": "object"
      },
      "QualityDescriptor": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "QualityDescriptor",
         "type": "object"
      },
      "Quantile": {
         "additionalProperties": false,
         "description": "A quantile is a quantity that divides the range of a probability distribution into continuous intervals with equal probabilities, or dividing the observations in a sample in the same way.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Quantile",
         "type": "object"
      },
      "Quantity": {
         "additionalProperties": false,
         "description": "A quantity is an informational entity that gives the magnitude of a property.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Quantity",
         "type": "object"
      },
      "QuantityModifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "QuantityModifier",
         "type": "object"
      },
      "QuaternaryStructure": {
         "additionalProperties": false,
         "description": "A quaternary structure descriptor describes topological patterns in a multi-unit biopolymer complex.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "QuaternaryStructure",
         "type": "object"
      },
      "Question": {
         "additionalProperties": false,
         "description": "A question is a linguistic expression used to make a request for information.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Question",
         "type": "object"
      },
      "Quote": {
         "additionalProperties": false,
         "description": "A quote is a excerpt that is attributed to a particular source.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Quote",
         "type": "object"
      },
      "RDFSOntology": {
         "additionalProperties": false,
         "description": "An RDFS ontology is an ontology that conforms to the syntax and semantics of the Resource Description Framework Schema (RDFS).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RDFSOntology",
         "type": "object"
      },
      "RESTWebService": {
         "additionalProperties": false,
         "description": "a REST web service is a web service that provides functionality according to the Representational State Transfer (REST) specification.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RESTWebService",
         "type": "object"
      },
      "RNATranscript": {
         "additionalProperties": false,
         "description": "An RNA transcript is an RNA molecule that is produced from transcription of a nucleic acid template.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RNATranscript",
         "type": "object"
      },
      "RNATranscriptComponent": {
         "additionalProperties": false,
         "description": "An RNA transcript component is a region of an RNA transcript.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RNATranscriptComponent",
         "type": "object"
      },
      "Race": {
         "additionalProperties": false,
         "description": "race is a characteristic of an individual by heritable phenotypic characteristics, geographic ancestry, physical appearance, ethnicity, and social status.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Race",
         "type": "object"
      },
      "Radar": {
         "additionalProperties": false,
         "description": "A radar is an object-detection system which uses radio waves to determine the range, altitude, direction, or speed of objects.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Radar",
         "type": "object"
      },
      "RadioReceiver": {
         "additionalProperties": false,
         "description": "A radio receiver is a communication device that receives its input from an antenna, uses electronic filters to separate a wanted radio signal from all other signals picked up by this antenna, amplifies it to a level suitable for further processing, and finally converts through demodulation and decoding the signal into a form usable for the consumer, such as sound, pictures, digital data, measurement values, navigational positions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RadioReceiver",
         "type": "object"
      },
      "RadiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RadiumAtom",
         "type": "object"
      },
      "RadonAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RadonAtom",
         "type": "object"
      },
      "Rage": {
         "additionalProperties": false,
         "description": "Rage is a feeling of intense anger that is associated with the Fight-or-flight response.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Rage",
         "type": "object"
      },
      "RankNonzeroTensor": {
         "additionalProperties": false,
         "description": "a rank nonzero tensor is a tensor with a rank greater than zero.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RankNonzeroTensor",
         "type": "object"
      },
      "Rat": {
         "additionalProperties": false,
         "description": "A rat is a medium-sized, long-tailed rodent of the superfamily Muroidea.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Rat",
         "type": "object"
      },
      "RateOfChange": {
         "additionalProperties": false,
         "description": "The amount of change accumulated per unit time.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RateOfChange",
         "type": "object"
      },
      "Ratio": {
         "additionalProperties": false,
         "description": "A ratio is a relationship between two numbers of the same kind expressed arithmetically as a dimensionless quotient of the two which explicitly indicates how many times the first number contains the second.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ratio",
         "type": "object"
      },
      "Ray": {
         "additionalProperties": false,
         "description": "A ray is a line which that is bounded by a startpoint and extends outwards infinitely along one dimension.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ray",
         "type": "object"
      },
      "ReactantRole": {
         "additionalProperties": false,
         "description": "The role of a chemical entity present at the beginning of a chemical reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ReactantRole",
         "type": "object"
      },
      "Reagent": {
         "additionalProperties": false,
         "description": "A reagent is a substance that is added to a system in order to bring about a chemical reaction, or added to see if a reaction occurs.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Reagent",
         "type": "object"
      },
      "ReagentRole": {
         "additionalProperties": false,
         "description": "A role of a chemical substance that participates in a chemical reaction as part of some scientific investigation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ReagentRole",
         "type": "object"
      },
      "Real": {
         "additionalProperties": false,
         "description": "real is the quality of an entity that exists in real space and time.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Real",
         "type": "object"
      },
      "RealizableEntity": {
         "additionalProperties": false,
         "description": "A realizable entity is an attribute that is exhibited under some condition and is realized in some process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RealizableEntity",
         "type": "object"
      },
      "Reason": {
         "additionalProperties": false,
         "description": "A reason is a justification that specifies the motive for an action or a determination.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Reason",
         "type": "object"
      },
      "Reasoning": {
         "additionalProperties": false,
         "description": "reasoning is the agentive process of using knowledge to evaluate the truth value of a proposition.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Reasoning",
         "type": "object"
      },
      "Receptor": {
         "additionalProperties": false,
         "description": "A receptor molecule is a molecule that has the capability to bind to a signal and propogate a response to that signal.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Receptor",
         "type": "object"
      },
      "Recipe": {
         "additionalProperties": false,
         "description": "A recipe is a set of instructions that describe how to prepare or make something.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Recipe",
         "type": "object"
      },
      "Record": {
         "additionalProperties": false,
         "description": "A record is a document containing a collection of statements about some entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Record",
         "type": "object"
      },
      "RecordIdentifier": {
         "additionalProperties": false,
         "description": "A record identifier is an identifier for a database entry.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RecordIdentifier",
         "type": "object"
      },
      "Rectangle": {
         "additionalProperties": false,
         "description": "A rectangle is a quadrilateral in which one pair of line segments are  parallel and the other pair are perpendicular to the first pair.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Rectangle",
         "type": "object"
      },
      "RedoxReaction": {
         "additionalProperties": false,
         "description": "A redox reaction is a chemical reaction in which there is a net movement of electrons from one reactant to another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RedoxReaction",
         "type": "object"
      },
      "Reference": {
         "additionalProperties": false,
         "description": "A reference is a textual entity that describes a single source used in the preparation or development of the work.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Reference",
         "type": "object"
      },
      "ReferencingCell": {
         "additionalProperties": false,
         "description": "A referenceing cell is a cell of a cellular automata that refers to another cell.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ReferencingCell",
         "type": "object"
      },
      "ReferentCell": {
         "additionalProperties": false,
         "description": "A referent cell is a cell that is the referent of some function or pointer.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ReferentCell",
         "type": "object"
      },
      "Regret": {
         "additionalProperties": false,
         "description": "regret is a feeling of sadness, repentance, or disappointment over something that has happened or been done.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Regret",
         "type": "object"
      },
      "Regulating": {
         "additionalProperties": false,
         "description": "regulating is a process that modulates the attributes of an object or process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Regulating",
         "type": "object"
      },
      "RegulationOfBiochemicalProcess": {
         "additionalProperties": false,
         "description": "regulation of biochemical process is a process that changes the frequency, rate or extent of a target biochemical process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfBiochemicalProcess",
         "type": "object"
      },
      "RegulationOfCapability": {
         "additionalProperties": false,
         "description": "regulation of capability is the regulation of the ability of one party by another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfCapability",
         "type": "object"
      },
      "RegulationOfCatalyticCapability": {
         "additionalProperties": false,
         "description": "the regulation of the enzymatic activity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfCatalyticCapability",
         "type": "object"
      },
      "RegulationOfMolecularDegradation": {
         "additionalProperties": false,
         "description": "A process that modulates the frequency, rate or extent of process involved in the destruction of a molecule.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfMolecularDegradation",
         "type": "object"
      },
      "RegulationOfMolecularProduction": {
         "additionalProperties": false,
         "description": "A process that modulates the frequency, rate or extent of process involved in the production of a molecule.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfMolecularProduction",
         "type": "object"
      },
      "RegulationOfMolecularQuantity": {
         "additionalProperties": false,
         "description": "A process that modulates the frequency, rate or extent of process involved in the creation or destruction of a molecule.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfMolecularQuantity",
         "type": "object"
      },
      "RegulationOfObjectConsumption": {
         "additionalProperties": false,
         "description": "A process that modulates the frequency, rate or extent of process involved in the consumption of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfObjectConsumption",
         "type": "object"
      },
      "RegulationOfObjectProduction": {
         "additionalProperties": false,
         "description": "regulation of a participant quantity is the regulation of a process in which the quantity of a selected participant is increased.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfObjectProduction",
         "type": "object"
      },
      "RegulationOfObjectQuantity": {
         "additionalProperties": false,
         "description": "regulation of a participant quantity is the regulation of a process in which the quantity of its partcipants is changed.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfObjectQuantity",
         "type": "object"
      },
      "RegulationOfProcess": {
         "additionalProperties": false,
         "description": "regulation of a process is a process that modulates the duration, frequency, spatial extent of a target process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfProcess",
         "type": "object"
      },
      "RegulationOfProcessDuration": {
         "additionalProperties": false,
         "description": "regulation of a process duration is a process that modulates the duration of another process relative to some reference process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfProcessDuration",
         "type": "object"
      },
      "RegulationOfProcessFrequency": {
         "additionalProperties": false,
         "description": "regulation of a process duration is a process that modulates the frequency of another process relative to some reference process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfProcessFrequency",
         "type": "object"
      },
      "RegulationOfProcessSpatialExtent": {
         "additionalProperties": false,
         "description": "regulation of a process spatial extent is a process that modulates the spatial extent of another process relative to some reference process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfProcessSpatialExtent",
         "type": "object"
      },
      "RegulationOfTranscription": {
         "additionalProperties": false,
         "description": "A process that modulates the frequency, rate or extent of transcription.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfTranscription",
         "type": "object"
      },
      "RegulationOfTranslation": {
         "additionalProperties": false,
         "description": "A process that modulates the frequency, rate or extent of translation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulationOfTranslation",
         "type": "object"
      },
      "RegulatorRole": {
         "additionalProperties": false,
         "description": "The role of a chemical entity that modifies the rate of reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulatorRole",
         "type": "object"
      },
      "RegulatoryAuthority": {
         "additionalProperties": false,
         "description": "A regulatory authority is an organization responsible for  exercising regulatory or supervisory capacity in some area of human activity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulatoryAuthority",
         "type": "object"
      },
      "RegulatoryPathway": {
         "additionalProperties": false,
         "description": "A regulatory pathway is a series of biochemical reactions that lead to the increase or decrease of activity of participating molecular components.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RegulatoryPathway",
         "type": "object"
      },
      "Remorse": {
         "additionalProperties": false,
         "description": "remorse is an emotion of personal regret felt by a person after he or she has committed an act which they deem to be shameful, hurtful, or violent.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Remorse",
         "type": "object"
      },
      "ReplicaQuality": {
         "additionalProperties": false,
         "description": "replica quality is the quality of an object that is an exact copy of another object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ReplicaQuality",
         "type": "object"
      },
      "Replicate": {
         "additionalProperties": false,
         "description": "a replicate is an object that is a facsimile, reproduction, or copy of another item.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Replicate",
         "type": "object"
      },
      "ReplicateRole": {
         "additionalProperties": false,
         "description": "a replicate role is a comparative role that holds  when one item is a reproduction, facsimile, or a copy of another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ReplicateRole",
         "type": "object"
      },
      "Report": {
         "additionalProperties": false,
         "description": "A report is a textual document made that present focused, salient content to a specific audience.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Report",
         "type": "object"
      },
      "Representation": {
         "additionalProperties": false,
         "description": "A representation is a entity that in some way represents another entity (or attribute thereof).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Representation",
         "type": "object"
      },
      "Reproducing": {
         "additionalProperties": false,
         "description": "reproducing is a process characterized by creation of an entity that is similar or exactly the same as the template from which it is derived.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Reproducing",
         "type": "object"
      },
      "Resentment": {
         "additionalProperties": false,
         "description": "resentment is disgust directed toward a higher status individual.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Resentment",
         "type": "object"
      },
      "ResultsSection": {
         "additionalProperties": false,
         "description": "The results section is a document section describing the main findings of the study.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ResultsSection",
         "type": "object"
      },
      "Reviewed": {
         "additionalProperties": false,
         "description": "reviewed is the quality of a textual entity that has been examined and commented on by another party.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Reviewed",
         "type": "object"
      },
      "RevisionNumber": {
         "additionalProperties": false,
         "description": "A revision number is a version of a software in which bugs have been fixed from a prior version.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RevisionNumber",
         "type": "object"
      },
      "RheniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RheniumAtom",
         "type": "object"
      },
      "RhodiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RhodiumAtom",
         "type": "object"
      },
      "RibonucleicAcid": {
         "additionalProperties": false,
         "description": "A ribonucleic acid is an organic polymer composed of a sequence of ribonucleotide residues.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RibonucleicAcid",
         "type": "object"
      },
      "RibonucleicAcidSequence": {
         "additionalProperties": false,
         "description": "A ribonucleic acid sequence is a symbolic representation of the sequence of ribonucleic acid residues in a ribonucleic acid.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RibonucleicAcidSequence",
         "type": "object"
      },
      "RibonucleotideResidue": {
         "additionalProperties": false,
         "description": "A ribonucleotide residue is a part of a molecule that derives from a ribonucleotide.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RibonucleotideResidue",
         "type": "object"
      },
      "RibosomalRNAGene": {
         "additionalProperties": false,
         "description": "A ribosomal RNA gene is a gene that codes for a ribosomal RNA molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RibosomalRNAGene",
         "type": "object"
      },
      "Right-handedHelical": {
         "additionalProperties": false,
         "description": "right-handed helical quality is the quality in which a clockwise screwing motion moves the helix away from the observed along the line of sight along the helix's axis.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Right-handedHelical",
         "type": "object"
      },
      "RightClosedInterval": {
         "additionalProperties": false,
         "description": "a right closed interval is an interval in which there is a real number that is larger than all of its elements.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RightClosedInterval",
         "type": "object"
      },
      "RightOpenInterval": {
         "additionalProperties": false,
         "description": "a right open interval is an interval in which there is no element that is greater than all other elements.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RightOpenInterval",
         "type": "object"
      },
      "RightValueAxis": {
         "additionalProperties": false,
         "description": "A right value axis is a value axis that is spatially positioned to the right of the plot area.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RightValueAxis",
         "type": "object"
      },
      "Rigid": {
         "additionalProperties": false,
         "description": "rigid is the quality of maintaining structural integrity (and not bending) under pressure.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Rigid",
         "type": "object"
      },
      "Ring": {
         "additionalProperties": false,
         "description": "A ring is a submolecule with a circular topology.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Ring",
         "type": "object"
      },
      "RnaGene": {
         "additionalProperties": false,
         "description": "A gene that is located on RNA.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RnaGene",
         "type": "object"
      },
      "RnaMediatedRegulationOfTranslation": {
         "additionalProperties": false,
         "description": "A process mediated by rna that modulates the frequency, rate or extent of translation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RnaMediatedRegulationOfTranslation",
         "type": "object"
      },
      "RoentgeniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RoentgeniumAtom",
         "type": "object"
      },
      "Role": {
         "additionalProperties": false,
         "description": "A role is a realizable entity that describes behaviours, rights and obligations of an entity in some particular circumstance.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Role",
         "type": "object"
      },
      "Row": {
         "additionalProperties": false,
         "description": "A row represents a single, implicitly structured data item in a table.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Row",
         "type": "object"
      },
      "RubidiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RubidiumAtom",
         "type": "object"
      },
      "RutheniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RutheniumAtom",
         "type": "object"
      },
      "RutherfordiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "RutherfordiumAtom",
         "type": "object"
      },
      "SADISemanticWebService": {
         "additionalProperties": false,
         "description": "a SADI semantic web service is a semantic web service that follows the SADI specification",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SADISemanticWebService",
         "type": "object"
      },
      "SADIWebServiceInvocation": {
         "additionalProperties": false,
         "description": "A SADI web service invocation is the excution of a SADI web service.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SADIWebServiceInvocation",
         "type": "object"
      },
      "SOAPWebService": {
         "additionalProperties": false,
         "description": "a SOAP web service is a web service that implements Simple Object Access Protocol (SOAP).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SOAPWebService",
         "type": "object"
      },
      "Sadness": {
         "additionalProperties": false,
         "description": "sadness is emotional pain associated with, or characterized by feelings of disadvantage, loss, despair, helplessness, sorrow, and rage.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sadness",
         "type": "object"
      },
      "SamariumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SamariumAtom",
         "type": "object"
      },
      "Sample": {
         "additionalProperties": false,
         "description": "A sample is a limited quantity of something (e.g. an individual or set of individuals from a population, or a portion of a substance) to be used for testing, analysis, inspection, investigation, demonstration, or trial use.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sample",
         "type": "object"
      },
      "SampleQuality": {
         "additionalProperties": false,
         "description": "sample quality is the quality of an object that is drawn from a larger population.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SampleQuality",
         "type": "object"
      },
      "Sampling": {
         "additionalProperties": false,
         "description": "sampling is the act of obtaining a sample, whether through selection, collection or preparation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sampling",
         "type": "object"
      },
      "Satisfaction": {
         "additionalProperties": false,
         "description": "satisfaction is an emotion of fulfillment of one's wishes, expectations, or needs, or the pleasure derived from this.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Satisfaction",
         "type": "object"
      },
      "SatisfactionQualifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SatisfactionQualifier",
         "type": "object"
      },
      "SatisfiedQualifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SatisfiedQualifier",
         "type": "object"
      },
      "Scalar": {
         "additionalProperties": false,
         "description": "a scalar is a rank 0 tensor and is an element of a field that is used to define a vector space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Scalar",
         "type": "object"
      },
      "ScaledValueAxis": {
         "additionalProperties": false,
         "description": "A scaled value axis is a value axis in which the value range was subject to a mathematic transformation.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ScaledValueAxis",
         "type": "object"
      },
      "ScandiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ScandiumAtom",
         "type": "object"
      },
      "Scatterplot": {
         "additionalProperties": false,
         "description": "A scatterplot is a statistical graph which uses Cartesian coordinates to display values for two variables for a set of data. The data is displayed as a collection of points, each having the value of one variable determining the position on the horizontal axis and the value of the other variable determining the position on the vertical axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Scatterplot",
         "type": "object"
      },
      "ScientificData": {
         "additionalProperties": false,
         "description": "scientific data is data obtained from some scientific procedure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ScientificData",
         "type": "object"
      },
      "ScientificName": {
         "additionalProperties": false,
         "description": "A scientific name is a name given through scientific nomenclature.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ScientificName",
         "type": "object"
      },
      "SeaborgiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SeaborgiumAtom",
         "type": "object"
      },
      "Second": {
         "additionalProperties": false,
         "description": "A second (symbol: s) is the base unit of time in the International System of Units (SI) and is the second division of the hour by sixty, the first division by 60 being the minute.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Second",
         "type": "object"
      },
      "SecondMessenger": {
         "additionalProperties": false,
         "description": "A second messenger is a molecule that relay signals from receptors on the cell surface to target molecules inside the cell, in the cytoplasm or nucleus.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SecondMessenger",
         "type": "object"
      },
      "SecondaryActiveTransport": {
         "additionalProperties": false,
         "description": "secondary active transport or co-transport uses electrochemical potential difference created by pumping ions out of the cell to transport molecules across a membrane.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SecondaryActiveTransport",
         "type": "object"
      },
      "SecondaryCategoryAxis": {
         "additionalProperties": false,
         "description": "A secondary category axis is a category axis that defines a finer granular part (or subset) of the value range of the primary category axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SecondaryCategoryAxis",
         "type": "object"
      },
      "SecondaryGraphTitle": {
         "additionalProperties": false,
         "description": "A secondary graph title is a secondary title that describes a statistical graph.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SecondaryGraphTitle",
         "type": "object"
      },
      "SecondaryStructureDescriptor": {
         "additionalProperties": false,
         "description": "A secondary structure descriptor describes local topological patterns in  a biopolymer.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SecondaryStructureDescriptor",
         "type": "object"
      },
      "SecondaryTitle": {
         "additionalProperties": false,
         "description": "A secondary title is a title of lesser importance that should be used after the first title in describing some entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SecondaryTitle",
         "type": "object"
      },
      "SecretaryRole": {
         "additionalProperties": false,
         "description": "A secretary role is the role of an individual that performs administrative tasks to support one or more individuals of the same organization.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SecretaryRole",
         "type": "object"
      },
      "SeleniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SeleniumAtom",
         "type": "object"
      },
      "SemanticWebService": {
         "additionalProperties": false,
         "description": "a semantic web service is a web service that provides a formal, machine understanble description of its functionality.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SemanticWebService",
         "type": "object"
      },
      "Sentence": {
         "additionalProperties": false,
         "description": "A sentence is a grammatical unit consisting of one or more words that bear minimal syntactic relation to the words that precede or follow it.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sentence",
         "type": "object"
      },
      "Sequence": {
         "additionalProperties": false,
         "description": "A sequence is an ordered list of entities. Like a set, it contains members (also called elements, or terms).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sequence",
         "type": "object"
      },
      "SequenceAlignment": {
         "additionalProperties": false,
         "description": "A sequence alignment is the character-based alignment of sequences using some method.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceAlignment",
         "type": "object"
      },
      "SequenceAssembly": {
         "additionalProperties": false,
         "description": "A sequence assembly is a sequence that is produced as by the alignment of two or more sequences.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceAssembly",
         "type": "object"
      },
      "SequenceElementPosition": {
         "additionalProperties": false,
         "description": "A sequence element position is the position of an element of a linear sequence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceElementPosition",
         "type": "object"
      },
      "SequenceEndPosition": {
         "additionalProperties": false,
         "description": "A sequence end position is the position of the last character in a sequence of characters relative to some linear frame of reference.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceEndPosition",
         "type": "object"
      },
      "SequenceMotif": {
         "additionalProperties": false,
         "description": "A sequence motif is a pattern of nucleotides in a DNA sequence or amino acids in a protein.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceMotif",
         "type": "object"
      },
      "SequenceProfile": {
         "additionalProperties": false,
         "description": "A sequence profile is provides the preference for a character at each position of an abstracted sequence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceProfile",
         "type": "object"
      },
      "SequenceStartPosition": {
         "additionalProperties": false,
         "description": "A sequence start position is the start position for a sequence of characters.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceStartPosition",
         "type": "object"
      },
      "SequenceVariantRole": {
         "additionalProperties": false,
         "description": "A sequence variant role is a comparative role in which the composition of characters in a sequence differs when compared to another entity of similar type.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceVariantRole",
         "type": "object"
      },
      "SequenceVariationNotation": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SequenceVariationNotation",
         "type": "object"
      },
      "Set": {
         "additionalProperties": false,
         "description": "A set is a collection of entities, for which there may be zero members.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Set",
         "type": "object"
      },
      "SetItem": {
         "additionalProperties": false,
         "description": "set item is an item in a set.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SetItem",
         "type": "object"
      },
      "Severe": {
         "additionalProperties": false,
         "description": "severe is a qualitative intensity value that is more intense than strong, but less intense than fatal.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Severe",
         "type": "object"
      },
      "Shame": {
         "additionalProperties": false,
         "description": "shame is the emotion borne from feeling responsible for the commission of an offense.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Shame",
         "type": "object"
      },
      "Shape": {
         "additionalProperties": false,
         "description": "shape is the quality of a bearer that relates to its spatial extent.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Shape",
         "type": "object"
      },
      "Shock": {
         "additionalProperties": false,
         "description": "shock is an emotion of sudden upset or surprise.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Shock",
         "type": "object"
      },
      "Shyness": {
         "additionalProperties": false,
         "description": "shyness is an emotion of apprehension, lack of comfort, or awkwardness experienced when in proximity to, approaching, or being approached by other individuals, especially in new situations or with unfamiliar individuals.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Shyness",
         "type": "object"
      },
      "Sick": {
         "additionalProperties": false,
         "description": "sick is the status of a living organism that is behaving at a sub-optimal level.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sick",
         "type": "object"
      },
      "SignLanguage": {
         "additionalProperties": false,
         "description": "A sign language (also signed language) is a language that involves manual communication and body language to convey meaning. This can involve simultaneously combining hand shapes, orientation and movement of the hands, arms or body, and facial expressions to fluidly express a speaker's thoughts.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SignLanguage",
         "type": "object"
      },
      "Signal": {
         "additionalProperties": false,
         "description": "A signal is an object that initiates a sequence of events.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Signal",
         "type": "object"
      },
      "SignalTransducer": {
         "additionalProperties": false,
         "description": "A signal transducer is a molecule that responds to and amplifies a signal in a signalling system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SignalTransducer",
         "type": "object"
      },
      "SiliconAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SiliconAtom",
         "type": "object"
      },
      "SilverAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SilverAtom",
         "type": "object"
      },
      "SingleArrowedLineSegment": {
         "additionalProperties": false,
         "description": "A single arrowed line is directed line in which the endpoint is tangentially part of a triangle that bisects the line.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SingleArrowedLineSegment",
         "type": "object"
      },
      "SingleBond": {
         "additionalProperties": false,
         "description": "A single bond is a covalent bond between a pair of atoms in which one pair of electrons are shared.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SingleBond",
         "type": "object"
      },
      "SingleDisplacementReaction": {
         "additionalProperties": false,
         "description": "A single displacement reaction where one atom is transferred out of one molecule and into another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SingleDisplacementReaction",
         "type": "object"
      },
      "SingleNucleotidePolymorphism": {
         "additionalProperties": false,
         "description": "a single nucleotide variation (SNV) is a nucleotide residue that is a variant compared to some reference nucleic acid sequence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SingleNucleotidePolymorphism",
         "type": "object"
      },
      "Site": {
         "additionalProperties": false,
         "description": "A site is a spatial region bounded (in part or in whole) by material entities and may be occupied by material entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Site",
         "type": "object"
      },
      "Slide": {
         "additionalProperties": false,
         "description": "A slide is an visual representation meant to communicate some information.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Slide",
         "type": "object"
      },
      "Slideshow": {
         "additionalProperties": false,
         "description": "A slideshow is a visual presentation of information contained within a collection of slides.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Slideshow",
         "type": "object"
      },
      "Slope": {
         "additionalProperties": false,
         "description": "A slope or gradient of a line describes its steepness, incline, or grade. A higher slope value indicates a steeper incline. Slope is normally described by the ratio of the \"rise\" divided by the \"run\" between two points on a line.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Slope",
         "type": "object"
      },
      "SmallCytoplasmicRNAscRNA": {
         "additionalProperties": false,
         "description": "A small cytoplasmic RNA (scRNA) molecule is a small (7S; 129 nucleotides) RNA molecule found in the cytosol and rough endoplasmic reticulum that are normally associated with proteins that are involved in specific selection and transport of other proteins.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SmallCytoplasmicRNAscRNA",
         "type": "object"
      },
      "SmallCytoplasmicRNAscRNAGene": {
         "additionalProperties": false,
         "description": "A small cytoplasmic RNA (scRNA) gene is a gene that encodes a small (7S; 129 nucleotides) RNA molecule found in the cytosol and rough endoplasmic reticulum that are normally associated with proteins that are involved in specific selection and transport of other proteins.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SmallCytoplasmicRNAscRNAGene",
         "type": "object"
      },
      "SmallNuclearRNAsnRNA": {
         "additionalProperties": false,
         "description": "A small nuclear RNA (snRNA) is a small RNA molecule that is located in the nucleus of a cell.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SmallNuclearRNAsnRNA",
         "type": "object"
      },
      "SmallNuclearRNAsnRNAGene": {
         "additionalProperties": false,
         "description": "A small nuclear RNA (snRNA) gene is a gene that encodes a small niuclear RNA molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SmallNuclearRNAsnRNAGene",
         "type": "object"
      },
      "SmallNucleolarRNAsnoRNA": {
         "additionalProperties": false,
         "description": "A small nucleolar RNA (snoRNA) is a small RNA that are associated with the eukaryotic nucleus as components of small nucleolar ribonucleoproteins.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SmallNucleolarRNAsnoRNA",
         "type": "object"
      },
      "SmallNucleolarRNAsnoRNAGene": {
         "additionalProperties": false,
         "description": "A small nucleolar RNA (snoRNA) gene is a gene that encodes a small RNA that are associated with the eukaryotic nucleus as components of small nucleolar ribonucleoproteins.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SmallNucleolarRNAsnoRNAGene",
         "type": "object"
      },
      "SocialEntity": {
         "additionalProperties": false,
         "description": "A social entity pertains to the interaction among individuals and groups.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SocialEntity",
         "type": "object"
      },
      "SocialRelation": {
         "additionalProperties": false,
         "description": "A social relation is a social entity that describes a relationship  between two or more individuals or groups.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SocialRelation",
         "type": "object"
      },
      "SocialRole": {
         "additionalProperties": false,
         "description": "A social role is a role that is ascribed to individuals in a community.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SocialRole",
         "type": "object"
      },
      "SocialStructure": {
         "additionalProperties": false,
         "description": "A social structure is a social entity which consists of relationships between two or more entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SocialStructure",
         "type": "object"
      },
      "SodiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SodiumAtom",
         "type": "object"
      },
      "SoftwareApplication": {
         "additionalProperties": false,
         "description": "A software application is software that can be directly executed by some processing unit.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareApplication",
         "type": "object"
      },
      "SoftwareEntity": {
         "additionalProperties": false,
         "description": "A software entity is a computational entity that can be interpreted by or directly executed by a processing unit.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareEntity",
         "type": "object"
      },
      "SoftwareInterpreter": {
         "additionalProperties": false,
         "description": "A software interpreter is a software application that executes some specified input software.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareInterpreter",
         "type": "object"
      },
      "SoftwareLibrary": {
         "additionalProperties": false,
         "description": "A software library is software composed of a collection of software modules and/or software methods in a form that can be statically or dynamically linked to some software application.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareLibrary",
         "type": "object"
      },
      "SoftwareMethod": {
         "additionalProperties": false,
         "description": "A software method (also called subroutine, subprogram, procedure, method, function, or routine) is software designed to execute a specific task.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareMethod",
         "type": "object"
      },
      "SoftwareModule": {
         "additionalProperties": false,
         "description": "A software module is software composed of a collection of software methods.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareModule",
         "type": "object"
      },
      "SoftwareProcessIdentifier": {
         "additionalProperties": false,
         "description": "A software process identifier is an identifier for a software process in some operating system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareProcessIdentifier",
         "type": "object"
      },
      "SoftwareScript": {
         "additionalProperties": false,
         "description": "A software script is software whose instructions can be executed using a software interpreter.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareScript",
         "type": "object"
      },
      "SoftwareVersionLabel": {
         "additionalProperties": false,
         "description": "A software version label is a version label for a piece of software.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoftwareVersionLabel",
         "type": "object"
      },
      "SofwareExecution": {
         "additionalProperties": false,
         "description": "software execution is the process of executing software on a computing device.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SofwareExecution",
         "type": "object"
      },
      "SolidStateHardDrive": {
         "additionalProperties": false,
         "description": "A solid-state drive (SSD) is a data storage device that uses solid-state memory to store persistent data.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SolidStateHardDrive",
         "type": "object"
      },
      "Solute": {
         "additionalProperties": false,
         "description": "A solute is a substance that becomes dissolved in a solvent.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Solute",
         "type": "object"
      },
      "Solvent": {
         "additionalProperties": false,
         "description": "A solvent is a substance that can dissolve other substances (solutes).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Solvent",
         "type": "object"
      },
      "SomeOfTheTime": {
         "additionalProperties": false,
         "description": "some refers to in the minority of occasions",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SomeOfTheTime",
         "type": "object"
      },
      "Sorrow": {
         "additionalProperties": false,
         "description": "sorrow is the emotion that is characterized by a long term state of intense sadness, distress and a degree of resignation (not accepting).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sorrow",
         "type": "object"
      },
      "SoundArgument": {
         "additionalProperties": false,
         "description": "A sound argument is a valid argument with true premises.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoundArgument",
         "type": "object"
      },
      "SoundWave": {
         "additionalProperties": false,
         "description": "A sound wave is a mechanical wave that is an oscillation of pressure transmitted through a solid, liquid, or gas, composed of frequencies within the range of hearing.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SoundWave",
         "type": "object"
      },
      "SpatialBoundary": {
         "additionalProperties": false,
         "description": "A spatial boundary is the closure minus the interior of a subset of a topological space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpatialBoundary",
         "type": "object"
      },
      "SpatialQuantity": {
         "additionalProperties": false,
         "description": "A spatial quantity is a quantity obtained from measuring the spatial extent of an entity",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpatialQuantity",
         "type": "object"
      },
      "SpatialRegion": {
         "additionalProperties": false,
         "description": "A spatial region is an object contained in some region of space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpatialRegion",
         "type": "object"
      },
      "SpatialSpecification": {
         "additionalProperties": false,
         "description": "A specification for spatial location is an effective specification towards representation spatial position or spatial data.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpatialSpecification",
         "type": "object"
      },
      "SpecializedMaterialEntity": {
         "additionalProperties": false,
         "description": "A specialized material entity is a material entity that is defined by having some quality, role or capability.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpecializedMaterialEntity",
         "type": "object"
      },
      "SpecializedObject": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpecializedObject",
         "type": "object"
      },
      "SpecificGravity": {
         "additionalProperties": false,
         "description": "Specific gravity is the ratio of the density of a substance to the density of a reference substance; equivalently, it is the ratio of the mass of a substance to the mass of a reference substance for the same given volume.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpecificGravity",
         "type": "object"
      },
      "Specification": {
         "additionalProperties": false,
         "description": "A specification is a description of the essential technical attributes/requirements for an object or procedure, and may be used to determine that the object / procedure meets its requirements/attributes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Specification",
         "type": "object"
      },
      "Specimen": {
         "additionalProperties": false,
         "description": "A specimen is a portion of material for use in testing, examination, or study.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Specimen",
         "type": "object"
      },
      "Speculation": {
         "additionalProperties": false,
         "description": "speculation is an opinion based on incomplete evidence.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Speculation",
         "type": "object"
      },
      "Speech": {
         "additionalProperties": false,
         "description": "speech is the expression of thoughts and feelings by sound.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Speech",
         "type": "object"
      },
      "Speed": {
         "additionalProperties": false,
         "description": "Speed is the rate of change of an object's position.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Speed",
         "type": "object"
      },
      "SphericalCoordinateSystem": {
         "additionalProperties": false,
         "description": "A spherical coordinate system is a coordinate system for three-dimensional space where the position of a point is specified by three numbers: the radial distance of that point from a fixed origin, its polar angle measured from a fixed zenith direction, and the azimuth angle of its orthogonal projection on a reference plane that passes through the origin and is orthogonal to the zenith, measured from a fixed reference direction on that plane.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SphericalCoordinateSystem",
         "type": "object"
      },
      "SpliceSite": {
         "additionalProperties": false,
         "description": "A splice site is a region required for the excision of an intron and connection to another exon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SpliceSite",
         "type": "object"
      },
      "StackGraph": {
         "additionalProperties": false,
         "description": "A stack graph is a statistical graph which presents multiple series in which the distance between one series and another indicates the relative contribution to the total for any x-value.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StackGraph",
         "type": "object"
      },
      "StackedBarGraph": {
         "additionalProperties": false,
         "description": "A stacked bar graph is a bar graph in which each rectangular bar is partioned by the categorical value of each series of data.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StackedBarGraph",
         "type": "object"
      },
      "Standard": {
         "additionalProperties": false,
         "description": "A standard is a socially-agreed upon specification.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Standard",
         "type": "object"
      },
      "StandardDeviation": {
         "additionalProperties": false,
         "description": "A standard deviation (represented by the symbol \u03c3) is the quantity of  variation from the average (mean, or expected value).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StandardDeviation",
         "type": "object"
      },
      "StandardOperatingProcedure": {
         "additionalProperties": false,
         "description": "A standard operating procedure is a specification approved for use in specific environments.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StandardOperatingProcedure",
         "type": "object"
      },
      "StandardScore": {
         "additionalProperties": false,
         "description": "A standard score is the (signed) number of standard deviations an observation or datum is above the mean.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StandardScore",
         "type": "object"
      },
      "StartCodon": {
         "additionalProperties": false,
         "description": "A start codon is the first codon of a messenger RNA (mRNA) transcript translated by a ribosome. The start codon is almost always preceded by an untranslated region 5' UTR.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StartCodon",
         "type": "object"
      },
      "StartDate": {
         "additionalProperties": false,
         "description": "A start date is a time instant pertaining to the date of the beginning of a process.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isEndTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isStartTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isTimeBoundaryOf": {
               "$ref": "#/$defs/Entity"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StartDate",
         "type": "object"
      },
      "StartPoint": {
         "additionalProperties": false,
         "description": "A start point is a terminal point which is the first of an ordered \npair of points.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StartPoint",
         "type": "object"
      },
      "StartPosition": {
         "additionalProperties": false,
         "description": "A start position is the proximal position of an object relative to an origin in a linear system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StartPosition",
         "type": "object"
      },
      "StartTime": {
         "additionalProperties": false,
         "description": "A start time is a time instant pertaining to the time at which a process begins.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isEndTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isStartTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isTimeBoundaryOf": {
               "$ref": "#/$defs/Entity"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StartTime",
         "type": "object"
      },
      "State": {
         "additionalProperties": false,
         "description": "A state is a set of governing and supportive institutions that have sovereignty over a definite territory and population.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "State",
         "type": "object"
      },
      "Statement": {
         "additionalProperties": false,
         "description": "A statement is a proposition that is either (a) a meaningful declarative sentence that is either true or false, or (b) that which a true or false declarative sentence asserts.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Statement",
         "type": "object"
      },
      "StatementOfConsequence": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StatementOfConsequence",
         "type": "object"
      },
      "StationaryPoint": {
         "additionalProperties": false,
         "description": "A stationary point is a point that is part of a curve in which the derivative at that point is zero, and hence its value is at least a local maximum or minimum.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StationaryPoint",
         "type": "object"
      },
      "StatisticalAssociation": {
         "additionalProperties": false,
         "description": "A statistical association is any relationship between two measured quantities that renders them statistically dependent.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StatisticalAssociation",
         "type": "object"
      },
      "StatisticalGraph": {
         "additionalProperties": false,
         "description": "A statistical graph is a figure that displays the relationship among numeric data and/or mathematical functions.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StatisticalGraph",
         "type": "object"
      },
      "StatisticalGraphLine": {
         "additionalProperties": false,
         "description": "A statistical graph line is a line used in a statistical graph to communicate some trend or feature of the embedded data.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StatisticalGraphLine",
         "type": "object"
      },
      "StatusDescriptor": {
         "additionalProperties": false,
         "description": "status descriptor is a descriptor for the state of a process or object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StatusDescriptor",
         "type": "object"
      },
      "Statute": {
         "additionalProperties": false,
         "description": "A statute is a formal written enactment of a legislative authority that governs a state, city or country.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Statute",
         "type": "object"
      },
      "Stereoisomer": {
         "additionalProperties": false,
         "description": "A stereoisomer is an isomer in which the atomic connectivity is the same, but differs in its spatial arrangement of atoms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Stereoisomer",
         "type": "object"
      },
      "StopCodon": {
         "additionalProperties": false,
         "description": "A stop codon (or termination codon) is a nucleotide triplet within messenger RNA that signals a termination of translation.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StopCodon",
         "type": "object"
      },
      "Straight": {
         "additionalProperties": false,
         "description": "straight is a quality of a bearer that is free of curves, bends, or angles.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Straight",
         "type": "object"
      },
      "Strain": {
         "additionalProperties": false,
         "description": "A strain is a genetic variant or kind of microorganism.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Strain",
         "type": "object"
      },
      "Streamgraph": {
         "additionalProperties": false,
         "description": "A streamgraph is a multi-line stacked graph that yields the appearance of continuous y-values across the x-axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Streamgraph",
         "type": "object"
      },
      "StreetName": {
         "additionalProperties": false,
         "description": "A street name is the token given to identify a particular street.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StreetName",
         "type": "object"
      },
      "Strong": {
         "additionalProperties": false,
         "description": "strong is a qualitative intensity value that is more intense than moderate, but less intense than severe.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Strong",
         "type": "object"
      },
      "StrongChemicalSalt": {
         "additionalProperties": false,
         "description": "a strong chemical salt is a chemical salt that is composed of strong electrolytes. Strong chemical salts contain Na,K,NH4 or NO3, CIO4, CH3COO.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StrongChemicalSalt",
         "type": "object"
      },
      "StrongSubmolecularComponent": {
         "additionalProperties": false,
         "description": "A strong submolecular component is a submolecular component that strongly connects submolecular components.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StrongSubmolecularComponent",
         "type": "object"
      },
      "StrontiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StrontiumAtom",
         "type": "object"
      },
      "StructuralIsomer": {
         "additionalProperties": false,
         "description": "A structural isomer is an isomer in which the atoms are joined together in different ways.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StructuralIsomer",
         "type": "object"
      },
      "StructuralMotif": {
         "additionalProperties": false,
         "description": "A structural motif is a pattern in a structure formed by a spatial arrangement of objects (e.g. atoms).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StructuralMotif",
         "type": "object"
      },
      "StructuralQuality": {
         "additionalProperties": false,
         "description": "A structural quality is a quality of an object that describes its structure.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StructuralQuality",
         "type": "object"
      },
      "Structure": {
         "additionalProperties": false,
         "description": "structure is the specification that refers to the composition and arrangement of parts of an object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Structure",
         "type": "object"
      },
      "Student": {
         "additionalProperties": false,
         "description": "A student is an individual who is attends an educational institution.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Student",
         "type": "object"
      },
      "StudentAdvisorRole": {
         "additionalProperties": false,
         "description": "A student advisor role is the role of an individual employed at an academic organization that is involved in advising students.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StudentAdvisorRole",
         "type": "object"
      },
      "StudentRole": {
         "additionalProperties": false,
         "description": "A student role is the role of an individual that is enrolled in courses at an academic institution.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StudentRole",
         "type": "object"
      },
      "Study": {
         "additionalProperties": false,
         "description": "A study is a process that realizes the steps of a study design.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Study",
         "type": "object"
      },
      "StudyDesign": {
         "additionalProperties": false,
         "description": "A study design is a protocol for the proper execution of a study which normally requires a carefullly crafted research question or hypothesis and at least one variable under observation and observed values for that variable.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StudyDesign",
         "type": "object"
      },
      "StudyGroup": {
         "additionalProperties": false,
         "description": "A study group is a group of individuals that are subjects in an observational or intervention study.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StudyGroup",
         "type": "object"
      },
      "StudySubject": {
         "additionalProperties": false,
         "description": "A study subject is an individual that is the subject of the study.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "StudySubject",
         "type": "object"
      },
      "SubcellularEntity": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SubcellularEntity",
         "type": "object"
      },
      "SubjectRole": {
         "additionalProperties": false,
         "description": "A subject role is the role of an individual that is the target of the study.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SubjectRole",
         "type": "object"
      },
      "SubmolecularEntity": {
         "additionalProperties": false,
         "description": "A submolecular entity is a chemical entity that is a part of a molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SubmolecularEntity",
         "type": "object"
      },
      "Submolecule": {
         "additionalProperties": false,
         "description": "A submolecule is any part of a molecule.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Submolecule",
         "type": "object"
      },
      "Substrate": {
         "additionalProperties": false,
         "description": "A substrate is a molecule that is consumed in the course of a biochemical reaction.",
         "properties": {},
         "required": [],
         "title": "Substrate",
         "type": "object"
      },
      "SubstrateRole": {
         "additionalProperties": false,
         "description": "The role of a chemical entity that is modified in a chemical reaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SubstrateRole",
         "type": "object"
      },
      "Suffering": {
         "additionalProperties": false,
         "description": "Suffering is the unpleasant emotion and aversion associated with the perception of harm or threat of harm in an individual.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Suffering",
         "type": "object"
      },
      "Suicidal": {
         "additionalProperties": false,
         "description": "suicidal is the emotion of being deeply unhappy or depressed with thoughts of killing one's self.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Suicidal",
         "type": "object"
      },
      "SulfurAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SulfurAtom",
         "type": "object"
      },
      "Sum": {
         "additionalProperties": false,
         "description": "A sum is the result of adding a set of values together.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Sum",
         "type": "object"
      },
      "Supernatant": {
         "additionalProperties": false,
         "description": "A supernatent is a liquid substance that remains after centrifugation.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Supernatant",
         "type": "object"
      },
      "SurfaceArea": {
         "additionalProperties": false,
         "description": "The surface area of a is a measure of the total area that the surface of the object occupies.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SurfaceArea",
         "type": "object"
      },
      "SurfaceNormal": {
         "additionalProperties": false,
         "description": "A surface normal is a vector that is perpendicular to a flat surface.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SurfaceNormal",
         "type": "object"
      },
      "Surprise": {
         "additionalProperties": false,
         "description": "surprise is a brief emotion experienced as the result of an unexpected event.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Surprise",
         "type": "object"
      },
      "Suspended": {
         "additionalProperties": false,
         "description": "suspended is the status of a process that is no longer progressing towards completion.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Suspended",
         "type": "object"
      },
      "Syllable": {
         "additionalProperties": false,
         "description": "A syllable is a verbal entity of language having one vowel sound, with or without surrounding consonants, forming the whole or a part of a word.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Syllable",
         "type": "object"
      },
      "Symbol": {
         "additionalProperties": false,
         "description": "A symbol is a proposition about what an entity represents.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Symbol",
         "type": "object"
      },
      "SymportEnabledSecondaryActiveTransport": {
         "additionalProperties": false,
         "description": "symport enabled secondary active transport is a secondary active transfort in which both ion and molecule are transported in the same direction simultaneously.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SymportEnabledSecondaryActiveTransport",
         "type": "object"
      },
      "Syndrome": {
         "additionalProperties": false,
         "description": "A syndrome is composed of a set of several clinically recognizable features, signs (observed by someone other than the patient), symptoms (reported by the patient), phenomena or characteristics that often occur together.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Syndrome",
         "type": "object"
      },
      "Synonym": {
         "additionalProperties": false,
         "description": "A synonym is a word with the same or very similar meanings.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Synonym",
         "type": "object"
      },
      "SynthesisReaction": {
         "additionalProperties": false,
         "description": "A synthesis reaction is an inorganic reaction in which two or more molecules are chemically bonded together to produce a single product.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SynthesisReaction",
         "type": "object"
      },
      "SyntheticQuality": {
         "additionalProperties": false,
         "description": "synthetic quality is the quality of a manufactured object that has similar properties as naturally occuring objects.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "SyntheticQuality",
         "type": "object"
      },
      "T-statistic": {
         "additionalProperties": false,
         "description": "A t-statistic is a ratio of the departure of an estimated parameter from its notional value and its standard error.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "T-statistic",
         "type": "object"
      },
      "T-statisticBasedDecreasedDifferentialGeneExpression": {
         "additionalProperties": false,
         "description": "A t-statistic based decreased differential gene expression is a differential gene expression ratio in which the t-statistic is less than zero.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "T-statisticBasedDecreasedDifferentialGeneExpression",
         "type": "object"
      },
      "T-statisticBasedIncreasedDifferentialGeneExpression": {
         "additionalProperties": false,
         "description": "A t-statistic based increased differential gene expression is a differential gene expression ratio in which the t-statistic is greater than zero.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "T-statisticBasedIncreasedDifferentialGeneExpression",
         "type": "object"
      },
      "Table": {
         "additionalProperties": false,
         "description": "A table is a figure that consists of an ordered arrangement of columns and rows.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Table",
         "type": "object"
      },
      "TableOfContents": {
         "additionalProperties": false,
         "description": "The table of contents is a document section that lists all sections (and optionally subsections) in a sequential order along with their page number.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TableOfContents",
         "type": "object"
      },
      "TagCloud": {
         "additionalProperties": false,
         "description": "A tag cloud is a visualization of word frequencies.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TagCloud",
         "type": "object"
      },
      "TantalumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TantalumAtom",
         "type": "object"
      },
      "Target": {
         "additionalProperties": false,
         "description": "None",
         "properties": {},
         "required": [],
         "title": "Target",
         "type": "object"
      },
      "TechnetiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TechnetiumAtom",
         "type": "object"
      },
      "TechnicalReport": {
         "additionalProperties": false,
         "description": "A technical report is a publication published by a school or other institution, usually numbered within a series.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TechnicalReport",
         "type": "object"
      },
      "Telephone": {
         "additionalProperties": false,
         "description": "The telephone is a communications device that transmits and receives sounds, and are minimally composed of a microphone to speak into, a speaker'which reproduces the voice of the other person and a ringer which makes a sound to alert the owner when a call is coming in.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Telephone",
         "type": "object"
      },
      "TelephoneNumber": {
         "additionalProperties": false,
         "description": "a telephone number is an identifier used to connect to a physical device capable of transfering voice or data over a network.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TelephoneNumber",
         "type": "object"
      },
      "TelevisionProgram": {
         "additionalProperties": false,
         "description": "A television program is a audiovisual media that is produced and broadcast using a television.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TelevisionProgram",
         "type": "object"
      },
      "TelluriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TelluriumAtom",
         "type": "object"
      },
      "TemporalQualifier": {
         "additionalProperties": false,
         "description": "a temporal qualifier is pertains to the frequency of the event of interest.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TemporalQualifier",
         "type": "object"
      },
      "Tensor": {
         "additionalProperties": false,
         "description": "a tensor is a n-dimensional array.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Tensor",
         "type": "object"
      },
      "TerbiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TerbiumAtom",
         "type": "object"
      },
      "Term": {
         "additionalProperties": false,
         "description": "A term is a word or phrase used to denote one or more entities.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Term",
         "type": "object"
      },
      "TermVariant": {
         "additionalProperties": false,
         "description": "A term variant is a term that is a variant of another term.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TermVariant",
         "type": "object"
      },
      "TerminalPoint": {
         "additionalProperties": false,
         "description": "A terminal point is a point that defines the finite extension of a line.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TerminalPoint",
         "type": "object"
      },
      "Terminology": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Terminology",
         "type": "object"
      },
      "Territory": {
         "additionalProperties": false,
         "description": "A territory is a non-sovereign geographic region.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Territory",
         "type": "object"
      },
      "Terror": {
         "additionalProperties": false,
         "description": "terror is the extreme feeling of fear.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Terror",
         "type": "object"
      },
      "TertiaryStructureDescriptor": {
         "additionalProperties": false,
         "description": "A tertiary structure descriptor describes 3D topological patterns in a biopolymer.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TertiaryStructureDescriptor",
         "type": "object"
      },
      "TestRole": {
         "additionalProperties": false,
         "description": "A test role is the role of an individual that is a participant in the study and is the target of the intervention.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TestRole",
         "type": "object"
      },
      "TextQuality": {
         "additionalProperties": false,
         "description": "text quality is the quality of a textual entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TextQuality",
         "type": "object"
      },
      "TextSpan": {
         "additionalProperties": false,
         "description": "A text span is a subset of contiguous sequence of characters of a textual entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TextSpan",
         "type": "object"
      },
      "TextSpanEndPosition": {
         "additionalProperties": false,
         "description": "text span end position is the position (offset) of the last character of a text span in relation the text it is from.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TextSpanEndPosition",
         "type": "object"
      },
      "TextSpanStartPosition": {
         "additionalProperties": false,
         "description": "text span start position is the position (offset) of the first character of a text span in relation the text it is from.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TextSpanStartPosition",
         "type": "object"
      },
      "TextualChart": {
         "additionalProperties": false,
         "description": "A textual chart is a chart containing text.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TextualChart",
         "type": "object"
      },
      "TextualEntity": {
         "additionalProperties": false,
         "description": "A textual entity is language entity that is manifested as a sequence of one or more distinct characters.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TextualEntity",
         "type": "object"
      },
      "ThalliumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ThalliumAtom",
         "type": "object"
      },
      "TherapeuticGene-diseaseAssociation": {
         "additionalProperties": false,
         "description": "A gene disease association in which the gene is a therapeutic marker for the disease.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TherapeuticGene-diseaseAssociation",
         "type": "object"
      },
      "ThesisDocument": {
         "additionalProperties": false,
         "description": "A thesis document is the written research component of a post-secondary institution  that contains a statement supported by arguments.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ThesisDocument",
         "type": "object"
      },
      "Thickness": {
         "additionalProperties": false,
         "description": "thickness is the shortest dimensional extent of a 3D projection of an object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Thickness",
         "type": "object"
      },
      "ThoriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ThoriumAtom",
         "type": "object"
      },
      "ThuliumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ThuliumAtom",
         "type": "object"
      },
      "TickMark": {
         "additionalProperties": false,
         "description": "A tick mark is a line segment that is spatially positioned perpendicular to the axis of a statistical graph and indicates the position of a specific numeric value (which may be indicated by an adjacent value label) on a value axis, or is one of a pair of tick marks that delineates the boundary of a categorical value (which may be indicated by an adjacent category label) on the categorical axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TickMark",
         "type": "object"
      },
      "TimeInstant": {
         "additionalProperties": false,
         "description": "A time instant is a temporal region which occurs instantaneously, e.g. having no duration.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isEndTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isStartTimeOf": {
               "$ref": "#/$defs/Entity"
            },
            "isTimeBoundaryOf": {
               "$ref": "#/$defs/Entity"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TimeInstant",
         "type": "object"
      },
      "TimeInterval": {
         "additionalProperties": false,
         "description": "A time internval is a contiguous temporal region having some duration.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TimeInterval",
         "type": "object"
      },
      "TimeMeasurement": {
         "additionalProperties": false,
         "description": "time measurement is a measurement value of the duration of some interval of time or a particular instant of time (against some frame of reference).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TimeMeasurement",
         "type": "object"
      },
      "TinAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TinAtom",
         "type": "object"
      },
      "Tissue": {
         "additionalProperties": false,
         "description": "A tissue is a mereologically maximal collection of cells that together perform some function.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Tissue",
         "type": "object"
      },
      "TitaniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TitaniumAtom",
         "type": "object"
      },
      "Title": {
         "additionalProperties": false,
         "description": "A title is a textual entity that summarily describes some entity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Title",
         "type": "object"
      },
      "ToActivelyInteractWith": {
         "additionalProperties": false,
         "description": "to actively interact with is the capability to interact with another entity in a way that requires physical contact.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToActivelyInteractWith",
         "type": "object"
      },
      "ToAddACovalentBond": {
         "additionalProperties": false,
         "description": "to add a covalent bond is the capability to covalently modify a chemical entity by adding a covalent bond.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToAddACovalentBond",
         "type": "object"
      },
      "ToAssemble": {
         "additionalProperties": false,
         "description": "to assemble is the capability to combine entities together into a larger object that persists in time.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToAssemble",
         "type": "object"
      },
      "ToAssociate": {
         "additionalProperties": false,
         "description": "to associate is the capability to physically interact with another object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToAssociate",
         "type": "object"
      },
      "ToBeAMemberOf": {
         "additionalProperties": false,
         "description": "to be a member of is the capability to be considered a part of a collection.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeAMemberOf",
         "type": "object"
      },
      "ToBeAPartOf": {
         "additionalProperties": false,
         "description": "to be a part of is the capability to be assembled into a larger structure that persists in time.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeAPartOf",
         "type": "object"
      },
      "ToBeActivated": {
         "additionalProperties": false,
         "description": "to be activated is the capability to be modified in such a way that the conformational change leads to an increase in another capability.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeActivated",
         "type": "object"
      },
      "ToBeActivelyInteractedWith": {
         "additionalProperties": false,
         "description": "to be actively interacted with is the capability to be manipulated by some device or agent.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeActivelyInteractedWith",
         "type": "object"
      },
      "ToBeCleaved": {
         "additionalProperties": false,
         "description": "to be cleaved is the capability to be modified in a way that splits one part of the object from the other.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeCleaved",
         "type": "object"
      },
      "ToBeCombined": {
         "additionalProperties": false,
         "description": "to be combined is the capability to be modified in a way that the object is merged with another object to form a new object or substance.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeCombined",
         "type": "object"
      },
      "ToBeCompared": {
         "additionalProperties": false,
         "description": "to be compared is the capability of an object to be examined in order to note the similarities or differences among a set of objects.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeCompared",
         "type": "object"
      },
      "ToBeConformationallyChanged": {
         "additionalProperties": false,
         "description": "to be conformationally changed is the capability to be modified in such a way that the object's conformation is changed.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeConformationallyChanged",
         "type": "object"
      },
      "ToBeCovalentlyModified": {
         "additionalProperties": false,
         "description": "to be covalently modified is the capability of a chemical entity to have bonds added or removed",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeCovalentlyModified",
         "type": "object"
      },
      "ToBeElectronicallyModified": {
         "additionalProperties": false,
         "description": "to be electronically modified is the capability of a chemical entity to have electrons added or removed.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeElectronicallyModified",
         "type": "object"
      },
      "ToBeExamined": {
         "additionalProperties": false,
         "description": "to be examined is the capability of an object to be observed in a detailed manner.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeExamined",
         "type": "object"
      },
      "ToBeInhibited": {
         "additionalProperties": false,
         "description": "to be inhibited is the capability to be modified in such a way that the conformational change leads to an decrease in another capability.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeInhibited",
         "type": "object"
      },
      "ToBeInteractedWith": {
         "additionalProperties": false,
         "description": "to be interacted with is the capability of an object to be target of a physical interaction.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeInteractedWith",
         "type": "object"
      },
      "ToBeModified": {
         "additionalProperties": false,
         "description": "to be modified is the capability to be actively interacted with in such a way that it leads to a physical reconfiguration.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeModified",
         "type": "object"
      },
      "ToBeObserved": {
         "additionalProperties": false,
         "description": "to be observed is the capability of an object to be perceived.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeObserved",
         "type": "object"
      },
      "ToBePassivelyInteractedWith": {
         "additionalProperties": false,
         "description": "to be passively interacted with is the capability of an object to be observed.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBePassivelyInteractedWith",
         "type": "object"
      },
      "ToBeRecorded": {
         "additionalProperties": false,
         "description": "to be recorded is the capability of an object to be observed in such a way that information about it can be transcribed in a specified format on some physical medium.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeRecorded",
         "type": "object"
      },
      "ToBeTranslocated": {
         "additionalProperties": false,
         "description": "to be translocated is the capability to be physically displaced from one location to another",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeTranslocated",
         "type": "object"
      },
      "ToBeTransported": {
         "additionalProperties": false,
         "description": "to be transported is the disposition to undergo motion.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBeTransported",
         "type": "object"
      },
      "ToBindTo": {
         "additionalProperties": false,
         "description": "to bind to is the capability to physically interact with another object through a set of non-covalent interactions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBindTo",
         "type": "object"
      },
      "ToBoil": {
         "additionalProperties": false,
         "description": "to boil is the capability to increase the internal kinetic energy of a material such that it changes state from a solid or liquid to a gas.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBoil",
         "type": "object"
      },
      "ToBreathe": {
         "additionalProperties": false,
         "description": "to breathe is the capability to inhale and exhale air into the body during respiration.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToBreathe",
         "type": "object"
      },
      "ToCauseDisease": {
         "additionalProperties": false,
         "description": "to cause disease is the capability to materially change a biological object in that it functions abnormally.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToCauseDisease",
         "type": "object"
      },
      "ToChangeAppearance": {
         "additionalProperties": false,
         "description": "to change appearance is the capability to change the visual attributes of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToChangeAppearance",
         "type": "object"
      },
      "ToChangeEnergetically": {
         "additionalProperties": false,
         "description": "to change energetically is the capability to change the energetic aspects of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToChangeEnergetically",
         "type": "object"
      },
      "ToChangeMaterially": {
         "additionalProperties": false,
         "description": "to change appearance is the capability to change the material composition of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToChangeMaterially",
         "type": "object"
      },
      "ToChangeSpatially": {
         "additionalProperties": false,
         "description": "to change spatially is the capability to affect the physical movement of some entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToChangeSpatially",
         "type": "object"
      },
      "ToChangeTheActivationEnergy": {
         "additionalProperties": false,
         "description": "to change the activation energy is to change the amount of energy required to form or break a chemical bond.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToChangeTheActivationEnergy",
         "type": "object"
      },
      "ToCharacterize": {
         "additionalProperties": false,
         "description": "to characterize is the capability to classify the attributes or features of an entity against a reference classification.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToCharacterize",
         "type": "object"
      },
      "ToCleave": {
         "additionalProperties": false,
         "description": "to cleave is to split or sever an object along a natural line or grain.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToCleave",
         "type": "object"
      },
      "ToCombine": {
         "additionalProperties": false,
         "description": "to combine is the capability to modify a set of objects in a way that the object is merged with another object to form a new object or substance.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToCombine",
         "type": "object"
      },
      "ToCompare": {
         "additionalProperties": false,
         "description": "to compare is the capability to examine in order to note the similarities or differences among a set of objects.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToCompare",
         "type": "object"
      },
      "ToConformationallyActivate": {
         "additionalProperties": false,
         "description": "to conformationally activate is to modify the conformation of an entity in such a way that it becomes activated or functional.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToConformationallyActivate",
         "type": "object"
      },
      "ToConformationallyInhibit": {
         "additionalProperties": false,
         "description": "to conformationally inhibit is to modify the conformation of an entity in such a way that it functionally is reduced or inhibited.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToConformationallyInhibit",
         "type": "object"
      },
      "ToConsume": {
         "additionalProperties": false,
         "description": "to consume is the capability to internalize a material entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToConsume",
         "type": "object"
      },
      "ToContain": {
         "additionalProperties": false,
         "description": "to contain is the capability to bound or constrain a physical entity in some site.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToContain",
         "type": "object"
      },
      "ToCool": {
         "additionalProperties": false,
         "description": "to cool is the capability to decrease the internal kinetic energy of a material.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToCool",
         "type": "object"
      },
      "ToCovalentlyModify": {
         "additionalProperties": false,
         "description": "to covalently modify is to materially change a molecule by adding or removing covalent bonds between atoms.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToCovalentlyModify",
         "type": "object"
      },
      "ToDecodeInformation": {
         "additionalProperties": false,
         "description": "the ability to reverse an encoding operation",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToDecodeInformation",
         "type": "object"
      },
      "ToDecreaseTheRateOfFormation": {
         "additionalProperties": false,
         "description": "to decrease the rate of formation is to regulate the rate of formation in a manner that decreases this rate relative to a reference process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToDecreaseTheRateOfFormation",
         "type": "object"
      },
      "ToDemagnify": {
         "additionalProperties": false,
         "description": "to demagnify is the capability to decrease the appearance of the size of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToDemagnify",
         "type": "object"
      },
      "ToDescribe": {
         "additionalProperties": false,
         "description": "to describe is the capabilty to communicate facts about an entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToDescribe",
         "type": "object"
      },
      "ToDisassemble": {
         "additionalProperties": false,
         "description": "to disassemble is to physically separate the parts of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToDisassemble",
         "type": "object"
      },
      "ToDisassociate": {
         "additionalProperties": false,
         "description": "to disassociate is to cease or break association with some thing.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToDisassociate",
         "type": "object"
      },
      "ToDistort": {
         "additionalProperties": false,
         "description": "to distort is the capability to change the appearance of an entity by some transformation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToDistort",
         "type": "object"
      },
      "ToEmit": {
         "additionalProperties": false,
         "description": "to emit is the capability to release some physical entity (light, pollution, etc).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToEmit",
         "type": "object"
      },
      "ToEncodeInformation": {
         "additionalProperties": false,
         "description": "the capability to encode information in a different representation",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToEncodeInformation",
         "type": "object"
      },
      "ToExamine": {
         "additionalProperties": false,
         "description": "to examine is the capability to make detailed observation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToExamine",
         "type": "object"
      },
      "ToExcite": {
         "additionalProperties": false,
         "description": "to excite is the capability to supply energy to a materila by bombarding it with energetic particles (e.g., photons).",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToExcite",
         "type": "object"
      },
      "ToExtract": {
         "additionalProperties": false,
         "description": "to extract is the capability to remove certain entities based on selected attribute(s) while allowing other entities to remain.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToExtract",
         "type": "object"
      },
      "ToFilter": {
         "additionalProperties": false,
         "description": "to filter is the capability to retain certain entities based on selected attribute(s) while allowing other entities to pass through.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToFilter",
         "type": "object"
      },
      "ToFluoresce": {
         "additionalProperties": false,
         "description": "to fluoresce is to emit light as a result of absorbing light or other electromagnetic radiation.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToFluoresce",
         "type": "object"
      },
      "ToFreeze": {
         "additionalProperties": false,
         "description": "to freeze is the capability to decrease the internal kinetic energy of a material such that it changes state from a gas or liquid to a solid.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToFreeze",
         "type": "object"
      },
      "ToGainACovalentBond": {
         "additionalProperties": false,
         "description": "to gain a covalent bond is the capability of a chemical entity to have bonds added.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToGainACovalentBond",
         "type": "object"
      },
      "ToGainAnElectron": {
         "additionalProperties": false,
         "description": "to gain an electron is the capability of a chemical entity to receive an electron.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToGainAnElectron",
         "type": "object"
      },
      "ToHeat": {
         "additionalProperties": false,
         "description": "to heat is a capability to increase the internal kinetic energy of a material.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToHeat",
         "type": "object"
      },
      "ToIdentify": {
         "additionalProperties": false,
         "description": "to identify is the capability to determine the identity of something.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToIdentify",
         "type": "object"
      },
      "ToImmobilize": {
         "additionalProperties": false,
         "description": "to immobilize is the capability to contain an entity in such a way that it may not move in space.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToImmobilize",
         "type": "object"
      },
      "ToIncreaseTheActivationEnergy": {
         "additionalProperties": false,
         "description": "to increase the activation energy is to require a larger amount of energy in order to form or break a chemical bond.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToIncreaseTheActivationEnergy",
         "type": "object"
      },
      "ToIncreaseTheRateOfFormation": {
         "additionalProperties": false,
         "description": "to increase the rate of formation is to regulate the rate of formation in a manner that increases this rate relative to a reference process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToIncreaseTheRateOfFormation",
         "type": "object"
      },
      "ToInfect": {
         "additionalProperties": false,
         "description": "to infect is the capability to administer a disease-causing organism into some object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToInfect",
         "type": "object"
      },
      "ToIngest": {
         "additionalProperties": false,
         "description": "to ingest is the capability to take into the body by the mouth for digestion or absorption.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToIngest",
         "type": "object"
      },
      "ToInject": {
         "additionalProperties": false,
         "description": "to inject is the capability to administer a substance into some object through its external barrier.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToInject",
         "type": "object"
      },
      "ToInteractAndToBeInteractedWith": {
         "additionalProperties": false,
         "description": "to interact and to be interacted with is a mutual disposition of interacting objects.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isDispositionOf": {
               "$ref": "#/$defs/Entity",
               "description": "is disposition of is a relation between a disposition and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToInteractAndToBeInteractedWith",
         "type": "object"
      },
      "ToInteractWith": {
         "additionalProperties": false,
         "description": "to interact with is a capabililty that involves another object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToInteractWith",
         "type": "object"
      },
      "ToInvestigate": {
         "additionalProperties": false,
         "description": "to investigate is the capability to uncover facts.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToInvestigate",
         "type": "object"
      },
      "ToIonize": {
         "additionalProperties": false,
         "description": "to ionize is the capability to physically convert an atom or molecule into an ion by adding or removing charged particles such as electrons or other ions.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToIonize",
         "type": "object"
      },
      "ToLoseACovalentBond": {
         "additionalProperties": false,
         "description": "to lose a covalent bond is the capability of a chemical entity to have bonds removed.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToLoseACovalentBond",
         "type": "object"
      },
      "ToLoseAnElectron": {
         "additionalProperties": false,
         "description": "to lose an electron is the capability of a chemical entity to lose an electron.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToLoseAnElectron",
         "type": "object"
      },
      "ToLuminesce": {
         "additionalProperties": false,
         "description": "to luminesce is to emit light through cold body radiation",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToLuminesce",
         "type": "object"
      },
      "ToMagnify": {
         "additionalProperties": false,
         "description": "to magnify is the capability to increase the appearance of the size of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToMagnify",
         "type": "object"
      },
      "ToMaintainInformation": {
         "additionalProperties": false,
         "description": "the capability to maintain information such that it can be retrieved in the future.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToMaintainInformation",
         "type": "object"
      },
      "ToMeasure": {
         "additionalProperties": false,
         "description": "to measure is the capability to obtain information about some entity by examining its attributes in relation to some reference metric.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToMeasure",
         "type": "object"
      },
      "ToModify": {
         "additionalProperties": false,
         "description": "to modify is the capability to change some entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToModify",
         "type": "object"
      },
      "ToModifyConformationOf": {
         "additionalProperties": false,
         "description": "to modify conformation of is to affect the spatial arrangement of an entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToModifyConformationOf",
         "type": "object"
      },
      "ToModifyElectronically": {
         "additionalProperties": false,
         "description": "to modify electronically is the capability to change the electronic properties of an object.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToModifyElectronically",
         "type": "object"
      },
      "ToModifyOxidationStateOf": {
         "additionalProperties": false,
         "description": "to modify the oxidation state of is to change the number of electrons of a molecule, atom or ion.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToModifyOxidationStateOf",
         "type": "object"
      },
      "ToNegativelyCharge": {
         "additionalProperties": false,
         "description": "to negatively charge is the capability to add an electron or negatively charged ion to a chemical entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToNegativelyCharge",
         "type": "object"
      },
      "ToObserve": {
         "additionalProperties": false,
         "description": "to observe is the capability to watch attentively.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToObserve",
         "type": "object"
      },
      "ToOxidize": {
         "additionalProperties": false,
         "description": "to oxidize is the capability to remove an electron or an increase in oxidation state of a chemical entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToOxidize",
         "type": "object"
      },
      "ToPassivelyInteractWith": {
         "additionalProperties": false,
         "description": "to passively interact with is the capability to interact with another entity in a way that does not require physical contact.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToPassivelyInteractWith",
         "type": "object"
      },
      "ToPositivelyCharge": {
         "additionalProperties": false,
         "description": "to positively charge is the capability to remove an electron or add a  positively charged ion to a chemical entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToPositivelyCharge",
         "type": "object"
      },
      "ToProduce": {
         "additionalProperties": false,
         "description": "to produce is the capability to create new objects",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToProduce",
         "type": "object"
      },
      "ToProvide": {
         "additionalProperties": false,
         "description": "to provide is the capability to make available some object to another that requires it.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToProvide",
         "type": "object"
      },
      "ToRecord": {
         "additionalProperties": false,
         "description": "to record is the capability to detect and transcribe information in a specified format on some physical medium.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToRecord",
         "type": "object"
      },
      "ToReduce": {
         "additionalProperties": false,
         "description": "to reduce is the capability to add an electron or an decrease in oxidation state of a chemical entity.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToReduce",
         "type": "object"
      },
      "ToReduceEnergy": {
         "additionalProperties": false,
         "description": "to reduce energy is the capability to remove energy from a source.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToReduceEnergy",
         "type": "object"
      },
      "ToReduceTheActivationEnergy": {
         "additionalProperties": false,
         "description": "to reduce the activation energy is to require a smaller amount of energy in order to form or break a chemical bond.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToReduceTheActivationEnergy",
         "type": "object"
      },
      "ToRegulate": {
         "additionalProperties": false,
         "description": "to regulate is to control or maintain the rate or speed of an object or process.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToRegulate",
         "type": "object"
      },
      "ToRegulateTheRateOfFormation": {
         "additionalProperties": false,
         "description": "to regulate the rate of formation is to modify the rate at which an object is formed.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToRegulateTheRateOfFormation",
         "type": "object"
      },
      "ToRemoveACovalentBond": {
         "additionalProperties": false,
         "description": "to remove a covalent bond is the capability to covalently modify a chemical entity by removing a covalent bond.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToRemoveACovalentBond",
         "type": "object"
      },
      "ToRetrieve": {
         "additionalProperties": false,
         "description": "the capability to retrieve a (digitial/physical/mental) object from a location.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToRetrieve",
         "type": "object"
      },
      "ToSeparate": {
         "additionalProperties": false,
         "description": "to separate is the capability to i) distinguish some entities based on some attribute(s) and ii) subsequently physically displace them.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToSeparate",
         "type": "object"
      },
      "ToServeAs": {
         "additionalProperties": false,
         "description": "to serve as is the capability to act in a manner corresponding to some role.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToServeAs",
         "type": "object"
      },
      "ToServeAsAHost": {
         "additionalProperties": false,
         "description": "to serve as host is the capability to act in a manner that provides hospitality, serves to harbour an organism in or on itself.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToServeAsAHost",
         "type": "object"
      },
      "ToServeAsAPrimerForDNASynthesis": {
         "additionalProperties": false,
         "description": "to serve as a primer for DNA synthesis is the capability of a short nucleic acid to bind to the 5' end of single strand of DNA template and help initiate DNA replication.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToServeAsAPrimerForDNASynthesis",
         "type": "object"
      },
      "ToServeAsATemplateForDNASynthesis": {
         "additionalProperties": false,
         "description": "to serve as a template for DNA synthesis is the capability of a chemical entity to provide the necessary information or scaffold by which a DNA molecule may be produced.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToServeAsATemplateForDNASynthesis",
         "type": "object"
      },
      "ToServeAsATemplateForMolecularSynthesis": {
         "additionalProperties": false,
         "description": "to serve as a template for molecular synthesis is the capability of a chemical entity to provide the necessary information or scaffold by which another molecule may be produced.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToServeAsATemplateForMolecularSynthesis",
         "type": "object"
      },
      "ToServeAsATemplateForProteinSynthesis": {
         "additionalProperties": false,
         "description": "to serve as a template for protein synthesis is the capability of a chemical entity to provide the necessary information or scaffold by which a protein may be produced.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToServeAsATemplateForProteinSynthesis",
         "type": "object"
      },
      "ToServeAsATemplateForRNASynthesis": {
         "additionalProperties": false,
         "description": "to serve as a template for RNA synthesis is the capability of a chemical entity to provide the necessary information or scaffold by which an RNA molecule may be produced.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToServeAsATemplateForRNASynthesis",
         "type": "object"
      },
      "ToStore": {
         "additionalProperties": false,
         "description": "to store is the capability to place an object into a medium in which it can be retrieved in the future.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToStore",
         "type": "object"
      },
      "ToSupplyElectricity": {
         "additionalProperties": false,
         "description": "to supply electricity is the capability to transfer electricity from a source to a sink.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToSupplyElectricity",
         "type": "object"
      },
      "ToSupplyEnergy": {
         "additionalProperties": false,
         "description": "to supply energy is the capability to transfer energy from a source to a sink.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToSupplyEnergy",
         "type": "object"
      },
      "ToTestAHypothesis": {
         "additionalProperties": false,
         "description": "to test a hypothesis is the capability to evaluate the truth value of a proposition based on gathered evidence.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToTestAHypothesis",
         "type": "object"
      },
      "ToTranslocate": {
         "additionalProperties": false,
         "description": "to translocate is the capability to displace oneself from one location to another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToTranslocate",
         "type": "object"
      },
      "ToTransport": {
         "additionalProperties": false,
         "description": "to transport is the capability to displace a material from one location to another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCapabilityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is capability of is a relation between a capability and the entity that bears it.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToTransport",
         "type": "object"
      },
      "TopValueAxis": {
         "additionalProperties": false,
         "description": "A top value axis is a value axis that is spatially positioned to the top of the plot area.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TopValueAxis",
         "type": "object"
      },
      "Township": {
         "additionalProperties": false,
         "description": "A township is a rural or sub-urban settlement.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Township",
         "type": "object"
      },
      "Toxic": {
         "additionalProperties": false,
         "description": "toxic is the quality of a substance imparing the normal functioning of a  system.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Toxic",
         "type": "object"
      },
      "ToxicRole": {
         "additionalProperties": false,
         "description": "A toxic role is the role of a chemical substance that is poisonous.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToxicRole",
         "type": "object"
      },
      "Toxicity": {
         "additionalProperties": false,
         "description": "toxicity is the quality of a chemical substance to cause injury to an organism in a dose dependent manner.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Toxicity",
         "type": "object"
      },
      "ToxinRole": {
         "additionalProperties": false,
         "description": "A toxin role is a toxic role of a chemical substance that is poisonous and is produced by an organism.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ToxinRole",
         "type": "object"
      },
      "Trans-regulatoryElement": {
         "additionalProperties": false,
         "description": "A trans-regulatory element is a DNA sequence associated with the regulation of a gene located outside the genomic region supporting the corresponding structural DNA region of the trans-regulatory element (i.e., a different DNA strand or different chromosome).",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Trans-regulatoryElement",
         "type": "object"
      },
      "Transcription": {
         "additionalProperties": false,
         "description": "transcription is the process of creating a complementary RNA copy of a sequence of DNA.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Transcription",
         "type": "object"
      },
      "TransferRNAtRNA": {
         "additionalProperties": false,
         "description": "A transfer RNA (tRNA) is an RNA molecule that aids in the translation of a messenger RNA (mRNA) to produce a protein product.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TransferRNAtRNA",
         "type": "object"
      },
      "TransferRNAtRNAGene": {
         "additionalProperties": false,
         "description": "A transfer RNA (tRNA) gene is a gene that codes for a tRNA used in the translation of a messenger RNA (mRNA) to produce a protein product.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TransferRNAtRNAGene",
         "type": "object"
      },
      "Translation": {
         "additionalProperties": false,
         "description": "translation is the process of producing a polypeptide from a ribonucleic acid by a ribosome.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Translation",
         "type": "object"
      },
      "Transporting": {
         "additionalProperties": false,
         "description": "transporting is a  process in which one object physically moves another object from one location to another.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Transporting",
         "type": "object"
      },
      "TreeDiagram": {
         "additionalProperties": false,
         "description": "A tree diagram is a hierarchical network diagram in which a root vertex is connected to one or more other vertices through a directed edge, which in turn may be connected to other vertices.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TreeDiagram",
         "type": "object"
      },
      "Treemap": {
         "additionalProperties": false,
         "description": "A treemap is a chart that fully partitions the area into a set of rectangles whose area represents its relative value.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Treemap",
         "type": "object"
      },
      "TrendLine": {
         "additionalProperties": false,
         "description": "A trend line is a line, line segment or ray that is part of a statistical graph which indicates a statistical or visual direction across categorical or value data.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TrendLine",
         "type": "object"
      },
      "Triangle": {
         "additionalProperties": false,
         "description": "A triangle is a polygon composed of three points and three line segments, in which each point is fully connected to another point along through the line segment.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Triangle",
         "type": "object"
      },
      "TripleBond": {
         "additionalProperties": false,
         "description": "A triple bond is a covalent bond between a pair of atoms in which three pairs of electrons are shared.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TripleBond",
         "type": "object"
      },
      "True": {
         "additionalProperties": false,
         "description": "true is a truth value that indicates that it holds under all possible worlds.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "True",
         "type": "object"
      },
      "TruthValue": {
         "additionalProperties": false,
         "description": "truth value is a quality of information that is claimed/verified to be true or false.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TruthValue",
         "type": "object"
      },
      "TungstenAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "TungstenAtom",
         "type": "object"
      },
      "URL": {
         "additionalProperties": false,
         "description": "A Uniform Resource Locator or Universal Resource Locator (URL) is a specific character string that constitutes a reference to an Internet resource.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "URL",
         "type": "object"
      },
      "UncertaintyValue": {
         "additionalProperties": false,
         "description": "The uncertainty value (margin of error) of a measurement is a range of values likely to enclose the true value.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UncertaintyValue",
         "type": "object"
      },
      "Uncharged": {
         "additionalProperties": false,
         "description": "The quality of not having a charge.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Uncharged",
         "type": "object"
      },
      "UndergraduateStudentAdvisorRole": {
         "additionalProperties": false,
         "description": "An undergraduate student advisor role is the role of an individual employed at an academic organization that is involved in advising undergraduate students.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UndergraduateStudentAdvisorRole",
         "type": "object"
      },
      "UnicellularOrganism": {
         "additionalProperties": false,
         "description": "A unicellular organism is a organism that is composed of a single cell.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnicellularOrganism",
         "type": "object"
      },
      "UnigeneCluster": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnigeneCluster",
         "type": "object"
      },
      "Union": {
         "additionalProperties": false,
         "description": "A union is a list of all of the values of an attribute for the entities in the defined set.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Union",
         "type": "object"
      },
      "UniqueCell": {
         "additionalProperties": false,
         "description": "A unique cell is a cell that contains a unique value in the cellular automaton.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UniqueCell",
         "type": "object"
      },
      "UniqueIdentifier": {
         "additionalProperties": false,
         "description": "A unique identifier is an identifier that uniquely identifies some thing.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isUniqueIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "is unique identifier for is a relation between an identifier and an entity that it uniquely identifies\n"
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UniqueIdentifier",
         "type": "object"
      },
      "UnitOfMeasurement": {
         "additionalProperties": false,
         "description": "A unit of measurement is a definite magnitude of a physical quantity, defined and adopted by convention and/or by law, that is used as a standard for measurement of the same physical quantity.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isUnitOf": {
               "$ref": "#/$defs/Entity",
               "description": "is unit of is a relation between a unit and a quantity that it is a multiple of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnitOfMeasurement",
         "type": "object"
      },
      "University": {
         "additionalProperties": false,
         "description": "A university is an institution of higher education and research which grants academic degrees in a variety of subjects and provides both undergraduate education and postgraduate education.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "University",
         "type": "object"
      },
      "Unsupported": {
         "additionalProperties": false,
         "description": "unsupported is an existence quality in which there is no evidence to support the existence of the entity in any world (real or hypothetical)",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Unsupported",
         "type": "object"
      },
      "UnunhexiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnunhexiumAtom",
         "type": "object"
      },
      "UnunoctiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnunoctiumAtom",
         "type": "object"
      },
      "UnunpentiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnunpentiumAtom",
         "type": "object"
      },
      "UnunquadiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnunquadiumAtom",
         "type": "object"
      },
      "UnunseptiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnunseptiumAtom",
         "type": "object"
      },
      "UnutriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UnutriumAtom",
         "type": "object"
      },
      "UraniumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UraniumAtom",
         "type": "object"
      },
      "UserAccount": {
         "additionalProperties": false,
         "description": "user account allows a user to authenticate to system services and be granted authorization to access them.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "UserAccount",
         "type": "object"
      },
      "ValidArgument": {
         "additionalProperties": false,
         "description": "A valid argument is an argument where the truth of the conclusion is a logical consequence of the premises and (consequently) its corresponding conditional is a necessary truth.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ValidArgument",
         "type": "object"
      },
      "ValidatedGene": {
         "additionalProperties": false,
         "description": "An experimentally validated gene is a gene whose existence has been demonstrated through experimental methods.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ValidatedGene",
         "type": "object"
      },
      "ValueAxis": {
         "additionalProperties": false,
         "description": "A value axis is an axis in which the position along the line is partioned into numeric values.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ValueAxis",
         "type": "object"
      },
      "VanDerWaalsInteraction": {
         "additionalProperties": false,
         "description": "van der Waals' interaction is an a weak submolecular interaction between an instantaneous dipole and induced dipole.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VanDerWaalsInteraction",
         "type": "object"
      },
      "VanadiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VanadiumAtom",
         "type": "object"
      },
      "Variable": {
         "additionalProperties": false,
         "description": "A variable is a value that may change within the scope of a given problem or set of operations.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Variable",
         "type": "object"
      },
      "VariantRole": {
         "additionalProperties": false,
         "description": "A variant role is a comparative role in which the value of an attribute differs when compared to another entity",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasBasis": {
               "$ref": "#/$defs/Quality",
               "description": "has basis is a relation between a realizable entity and the quality that forms the basis for it.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "isRoleOf": {
               "$ref": "#/$defs/Entity",
               "description": "is role of is a relation between a role and the entity that it is a property of."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VariantRole",
         "type": "object"
      },
      "Vector": {
         "additionalProperties": false,
         "description": "a vector is a rank 1 tensor that is described by n-dimension of scalars.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Vector",
         "type": "object"
      },
      "VectorLine": {
         "additionalProperties": false,
         "description": "A vector line is a line which that is bounded by a startpoint and extends outwards along one dimension.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VectorLine",
         "type": "object"
      },
      "VectorSpace": {
         "additionalProperties": false,
         "description": "a vector space is a set of vectors.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VectorSpace",
         "type": "object"
      },
      "Velocity": {
         "additionalProperties": false,
         "description": "The rate of change of an object's position and the direction of that change",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Velocity",
         "type": "object"
      },
      "VennDiagram": {
         "additionalProperties": false,
         "description": "A Venn diagram is a chart that illustrates all possible logical relations between a finite collection of sets as overlapping circles.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VennDiagram",
         "type": "object"
      },
      "VerbalLanguage": {
         "additionalProperties": false,
         "description": "A verbal language is a language that uses sounds to communicate.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VerbalLanguage",
         "type": "object"
      },
      "VerbalLanguageEntity": {
         "additionalProperties": false,
         "description": "A verbal language entity is a language entity that is manifested through sound.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VerbalLanguageEntity",
         "type": "object"
      },
      "VersionLabel": {
         "additionalProperties": false,
         "description": "A version label is a label for a particular form or variation of an earlier or original type.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VersionLabel",
         "type": "object"
      },
      "VersionedDataset": {
         "additionalProperties": false,
         "description": "a versioned dataset is a dataset with a particular release date or release number.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasDataItem": {
               "$ref": "#/$defs/Entity",
               "description": "has data item is a relation between a dataset and any described / referrenced entity.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VersionedDataset",
         "type": "object"
      },
      "VersionedRecord": {
         "additionalProperties": false,
         "description": "A versioned record is a record for which there exists another variant based that was derived via modification of the facts.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VersionedRecord",
         "type": "object"
      },
      "VertexNormal": {
         "additionalProperties": false,
         "description": "A vertext normal is the normalized average of the surface normals of the faces that contain that vertex.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VertexNormal",
         "type": "object"
      },
      "VerticalBarGraph": {
         "additionalProperties": false,
         "description": "A vertical bar graph is a bar graph in which the rectangular bars are vertically oriented in space.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VerticalBarGraph",
         "type": "object"
      },
      "VerticalLine": {
         "additionalProperties": false,
         "description": "A vertical line is a line that is positionally oriented perpendicular to the horizon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VerticalLine",
         "type": "object"
      },
      "VeryDissatisfiedQualifier": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VeryDissatisfiedQualifier",
         "type": "object"
      },
      "VeryGoodQuality": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VeryGoodQuality",
         "type": "object"
      },
      "VeryPoorQuality": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VeryPoorQuality",
         "type": "object"
      },
      "Viroid": {
         "additionalProperties": false,
         "description": "A viroid is a molecule of RNA that does not code for and is not protected by a protein coat.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Viroid",
         "type": "object"
      },
      "Virtual": {
         "additionalProperties": false,
         "description": "virtual is the quality of an entity that exists only in a virtual setting such as a simulation or game environment.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Virtual",
         "type": "object"
      },
      "Virus": {
         "additionalProperties": false,
         "description": "A virus is a non-cellular organism that can replicate only inside the living cells of an organism.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Virus",
         "type": "object"
      },
      "VisualLanguageEntity": {
         "additionalProperties": false,
         "description": "A visual language entity is a language entity that is manifested within the spectrum of light and can be pereceived and processed by a visual system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VisualLanguageEntity",
         "type": "object"
      },
      "Vocabulary": {
         "additionalProperties": false,
         "description": "A vocabulary is a collection of terms.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Vocabulary",
         "type": "object"
      },
      "Volume": {
         "additionalProperties": false,
         "description": "volume is the quantity of three-dimensional space enclosed by some closed boundary.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Volume",
         "type": "object"
      },
      "VolumeNumber": {
         "additionalProperties": false,
         "description": "volume number is a count of a sequence of periodicals.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "VolumeNumber",
         "type": "object"
      },
      "Vowel": {
         "additionalProperties": false,
         "description": "A vowel is a verbal entity of language that is pronounced with an open vocal tract so that there is no build-up of air pressure at any point above the glottis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Vowel",
         "type": "object"
      },
      "Wave": {
         "additionalProperties": false,
         "description": "A wave is a physical entity that travels through space and time, consist of oscillations or vibrations and may be accompanied by the transfer of energy.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Wave",
         "type": "object"
      },
      "Weak": {
         "additionalProperties": false,
         "description": "weak is a qualitative intensity value that is more intense than none, but less intense than mild.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Weak",
         "type": "object"
      },
      "WeakChemicalSalt": {
         "additionalProperties": false,
         "description": "a weak chemical salt is a chemical salt composed of weak eletrolytes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WeakChemicalSalt",
         "type": "object"
      },
      "WeakSubmolecularComponent": {
         "additionalProperties": false,
         "description": "A weak submolecular component is a submolecular component that weakly connects submolecular components.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WeakSubmolecularComponent",
         "type": "object"
      },
      "WebPage": {
         "additionalProperties": false,
         "description": "A web page is a document that is published according to World Wide Web standards.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WebPage",
         "type": "object"
      },
      "WebService": {
         "additionalProperties": false,
         "description": "A web service is a software application that can be accessed over a network, such as the Internet, and executed on a remote system hosting the requested services.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WebService",
         "type": "object"
      },
      "WebServiceInvocation": {
         "additionalProperties": false,
         "description": "A web service invocation involves the execution of a web service.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WebServiceInvocation",
         "type": "object"
      },
      "Website": {
         "additionalProperties": false,
         "description": "A website is a collection of documents published on the World Wide Web.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasMember": {
               "$ref": "#/$defs/Entity",
               "description": "has member is a mereological relation between a collection and an item."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Website",
         "type": "object"
      },
      "Week": {
         "additionalProperties": false,
         "description": "A week is a period of 7 consecutive days.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Week",
         "type": "object"
      },
      "Width": {
         "additionalProperties": false,
         "description": "width is the shorter dimensional extent perpendicular to a 2D projection of the object.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Width",
         "type": "object"
      },
      "Wonder": {
         "additionalProperties": false,
         "description": "wonder is an emotion of perceiving something very rare or unexpected, but not threatening.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Wonder",
         "type": "object"
      },
      "Word": {
         "additionalProperties": false,
         "description": "A word is the smallest free form (an item that may be expressed in isolation with semantic or pragmatic content) in a language.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Word",
         "type": "object"
      },
      "WordEndPosition": {
         "additionalProperties": false,
         "description": "word end position is the position of the last character in a word as an offset from the first character of the text in which it is found.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WordEndPosition",
         "type": "object"
      },
      "WordStartPosition": {
         "additionalProperties": false,
         "description": "The position of the first character in a word as an offset from the first character of the text in which it is found.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WordStartPosition",
         "type": "object"
      },
      "WordTree": {
         "additionalProperties": false,
         "description": "A word tree is a chart that links phrases with contexts through a tree-like branching structure.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WordTree",
         "type": "object"
      },
      "WorkPhoneNumber": {
         "additionalProperties": false,
         "description": "a work phone number is the number to connect to an phone at a place of work.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isBroaderThant": {
               "$ref": "#/$defs/Term",
               "description": "A is broader than B (t) iff for A is broader than B."
            },
            "isIdentifierFor": {
               "$ref": "#/$defs/Entity",
               "description": "a relation between an identifier and an entity."
            },
            "isMatchTo": {
               "$ref": "#/$defs/Term",
               "description": "A is match to B iff A and B are terms with similar (related, broad, close, exact) meanings\n"
            },
            "isNarrowerThant": {
               "$ref": "#/$defs/Term",
               "description": "A is narrower than B (t) iff for A is narrower than B."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WorkPhoneNumber",
         "type": "object"
      },
      "Workflow": {
         "additionalProperties": false,
         "description": "A workflow is an algorithm that is is a depiction of a sequence of operations to achieve one or more objectives.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Workflow",
         "type": "object"
      },
      "Worm": {
         "additionalProperties": false,
         "description": "A worm is a non-arthropod invertebrate animal that typically have a long cylindrical tube-like body and no legs.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Worm",
         "type": "object"
      },
      "Worry": {
         "additionalProperties": false,
         "description": "worry is the emotion characterized by concer over a real or imaginary issue.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasParticipant": {
               "$ref": "#/$defs/Entity",
               "description": "has participant is a relation that describes the participation of the object in the (processual) subject.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Worry",
         "type": "object"
      },
      "Written": {
         "additionalProperties": false,
         "description": "written is the quality of information that is embodied as visual glyphs in some material form.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Written",
         "type": "object"
      },
      "WrittenLanguage": {
         "additionalProperties": false,
         "description": "written language is a language that is communicated through a writing system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isAnnotationOf": {
               "$ref": "#/$defs/Entity",
               "description": "is annotation of is a relation between some textual entity and the entity that it annotates.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "WrittenLanguage",
         "type": "object"
      },
      "X-axis": {
         "additionalProperties": false,
         "description": "An x-axis is a Cartesian coordinate axis that is aligned with the horizon.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "X-axis",
         "type": "object"
      },
      "XCartesianCoordinate": {
         "additionalProperties": false,
         "description": "An x cartesian coordinate is the coordinate of an object onto the x-axis of a cartesian coordinate system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "XCartesianCoordinate",
         "type": "object"
      },
      "XenonAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "XenonAtom",
         "type": "object"
      },
      "Y-axis": {
         "additionalProperties": false,
         "description": "A y-axis is a Cartesian coordinate axis that is spatially oriented perpendicular to the x-axis.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Y-axis",
         "type": "object"
      },
      "YCartesianCoordinate": {
         "additionalProperties": false,
         "description": "An y cartesian coordinate is the coordinate of an object onto the y-axis of a cartesian coordinate system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "YCartesianCoordinate",
         "type": "object"
      },
      "Year": {
         "additionalProperties": false,
         "description": "A year is a period of time taken by a planet to make one revolution around the sun.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isNumericallyComparableTo": {
               "$ref": "#/$defs/Quantity",
               "description": "is numerically comparable to is a comparison relation between two quantities whose datatype value can be compared.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Year",
         "type": "object"
      },
      "YtterbiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "YtterbiumAtom",
         "type": "object"
      },
      "YttriumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "YttriumAtom",
         "type": "object"
      },
      "Z-axis": {
         "additionalProperties": false,
         "description": "A z-axis is a Cartesian coordinate axis that is spatially oriented normal to the plane formed by the x- and y-axes.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Z-axis",
         "type": "object"
      },
      "ZCartesianCoordinate": {
         "additionalProperties": false,
         "description": "A z cartesian coordinate is the coordinate of an object onto the z-axis of a cartesian coordinate system.",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "hasUnit": {
               "$ref": "#/$defs/UnitOfMeasurement",
               "description": "has unit is a relation between a quantity and the unit it is a multiple of."
            },
            "isMeasurementValueOf": {
               "$ref": "#/$defs/Entity",
               "description": "is measurement value of is a relation between a value and the entity that it is a measurement of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ZCartesianCoordinate",
         "type": "object"
      },
      "ZincAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ZincAtom",
         "type": "object"
      },
      "ZirconiumAtom": {
         "additionalProperties": false,
         "description": "None",
         "properties": {
            "derivesInto": {
               "$ref": "#/$defs/Object",
               "description": "a derives to b if and only if a or some part thereof is consumed in the formation of b.\n"
            },
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isCovalentlyConnectedTotransitive": {
               "$ref": "#/$defs/Atom"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "ZirconiumAtom",
         "type": "object"
      },
      "Zygosity": {
         "additionalProperties": false,
         "description": "zygosity is the quality pertaining to the allelic complement of a biological system at a single locus on the DNA.",
         "properties": {
            "hasAttribute": {
               "$ref": "#/$defs/Entity",
               "description": "has attribute is a relation that associates a entity with an attribute where an attribute is an intrinsic characteristic such as a quality, capability, disposition, function, or is an externally derived attribute determined from some descriptor (e.g. a quantity, position, label/identifier) either directly or indirectly through generalization of entities of the same type.\n"
            },
            "hasCreator": {
               "$ref": "#/$defs/Entity",
               "description": "has creator is a relation between an entity and that which created it."
            },
            "isBaseFor": {
               "$ref": "#/$defs/RealizableEntity",
               "description": "is base for is a relation between a quality and the realizable entity that it is the basis for.\n"
            },
            "isQualityOf": {
               "$ref": "#/$defs/Entity",
               "description": "is quality of is a relation between a quality and the entity that it is a property of.\n"
            },
            "isRelatedTo": {
               "$ref": "#/$defs/Entity",
               "description": "A is related to B iff there is some relation between A and B."
            },
            "satisfies": {
               "$ref": "#/$defs/Description",
               "description": "satisfies is a relation between an entity and the specification or objective that it conforms to.\n"
            }
         },
         "required": [],
         "title": "Zygosity",
         "type": "object"
      }
   },
   "$id": "https://semanticscience.org",
   "$schema": "http://json-schema.org/draft-07/schema#",
   "additionalProperties": true,
   "properties": {},
   "required": [],
   "title": "semanticscience-model",
   "type": "object"
}

